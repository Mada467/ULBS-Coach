Macarie BREAZU

Programare Orientata pe Obiecte.

Principii
\ i. ZL u U

Editura Universitatii "Lucian Blaga" din Sibiu
2002

Referenti:
Prof. dr. ing. Valer ROSCA

Universitatea “Lucian Blaga” din Sibiu
Facultatea de Stiinte

Prof. dr. mat. loana MOISIL
S.L. ing. Dorin SIMA

Universitatea “Lucian Blaga” din Sibiu
Facultatea de Inginerie

Tehnoredactare: Macarie BREAZU

Copyright: Macarie BREAZU

Descrierea CIP a Bibliotecii Nationale a Romaniei
BREAZU, MACARIE
Programare orientata pe obiecte : principii / Macarie

Breazu. — Sibiu : Editura Universitatii “Lucian Blaga” din
Sibiu, 2002.

123 p.; 24 cm.

Bibliogr.

ISBN 973-651-465-X

004.43


In memoriam

In memoria dascalului si prietenului meu
Dan Roman *

* Dan Roman (1960-1996), sef lucrari doctorand, Universitatea Tehnicd Cluj-Napoca



Prefata

Prefata

Kernighan si Ritchie scriau, in prefata cartii lor "The C Programming Language". Second
Edition, Prentice Hall, 1988, de referint4 in ceea ce priveste limbajul C, urmatoarele:
"C is not a big language, and is not well served by a big book". Chiar daca
afirmatia nu este valabila, poate, in litera ei, si pentru limbajul C++, consider cd ea

ramane valabila, cel putin in spiritul ei, cartea de fata fiind realizatd in aceasta idee.

Nu am realizat aici o prezentare completa a limbajului C++. Oricum, nici nu mi-am
propus acest lucru. Am preferat sé prezint doar acele facilitati de limbaj in stransi
legatura cu programarea orientata pe obiecte. In aceast’ idee, exemplele prezentate nu s-
au dorit a fi utile in sine ci doar in forma minimal, strict necesarad pentru a demonstra
subiectul in discutie. Totusi, exemplele sunt functionale, rezultatele prezentate in lucrare
fiind obfinute prin rulare. De asemenea, am presupus 0 cunoastere bund a principiilor

programarii structurate si, in special, a limbajului C.

Cartea de fafa este destinata in principal a fi suport de curs la disciplina "Programare
Orientaté pe Obiecte", disciplina cu rol formativ, existenté in planul de invatamént la
diferite specializari in primii ani. S-a pus accentul pe o intelegere riguroasa, profunda a
principiilor programéarii orientate pe obiecte si a mecanismelor intalnite. fn acest fel,

trecerea ulterioara la alt limbaj (de exemplu Java) nu va fi o problema.

Trebuie atrasé de asemenea atentia asupra faptului cd nu se abordeazi analiza, proiectare
sau modelare orijentata obiect, ci doar principiile programarii orientate pe obiecte asa cum
apar ele in limbajul C++. Analiza, proiectarea si modelarea orientaté obiect vor face,

sper, subiectul unui volum ulterior.

Multumesc pe aceasta cale tuturor colegilor, dascali si studenti, care, prin observatiile lor
la adresa diferitelor variante intermediare care au circulat, au facut ca acum numarul
erorilor, de editare si formulare, si fie mai redus. Cum nu pot spera ca erorile au fost
eliminate fn totalitate si cum, cu siguranté, formularile pot fi imbund&tatite, multumesc

anticipat pentru orice critici si sugestii in legdtura cu aceasta carte.

Macarie BREAZU
Sibiu, 01.10.2001 macarie.breazu@ulbsibiu.ro

pag. 1

Programare Orientat& pe Obiecte - Principii

pag. 2

Cuprins

CUPPiNS sscsssessasssssocsssesss aedesesasentins aes

Cap. 1 - incapsularea adencesexeraneantoaas

1.1 Supraincarcarea numelui functiilor. Functii cu parametri avand valori implicite.. 5
1.2 Amintiri si generalitati despre structuri
1.3 Introducerea notiunii de clasi...........
1.4 Cum se leaga un apel de metoda de datele unui obiect anume (cel apelant)
15 incapsularea datelo:
1.6 Constructori si destructo
1.7 Operatorii new si delete...
1.8 Cazuri particulare de constructori care merit remarcate.
1.9 Exemplu pentru constructori / destructori.......cccceee
1.10 Membrii statici ..... see

1.11 Variabile membru de tip __ property

Cap. 2 - Mostenirea .........

2.1 Instante incuibarite .
2.2 Mostenirea....
2.3 Constructori si destructori in contextul derivarii.
2.4 Apel explicit al unui constructor al clasei de baza.
2.5 Conversii de tip intre clase derivate
2.6 Exemplu
2.7 Mostenirea multip|
2.8 Mostenire multipla virtuala

Cap. 3 - Polimorfismul .......c.ssssesseeseeee sasasscussonasensiausinasseseasaisasssoesocssserses 43

3.1 Supraincarcarea metodelor in contextul mostenirii

3.2 Polimorfism — generalitati........eccecsseessscssteessecssseecseeee
3.3 Legare staticd si dinamica
3.4 Metode virtuale............
3.5 Cum se implementeaza legarea dinamica (polimorfismul) ?
3.6 Ce poate fi virtual si ce nu
3.7 Un exemplu distractiv si nu numai
3.8 Metode virtuale gi ierarhii de clase.
3.9 Metode pure si clase abstracte...
3.10 Tratarea uniforma a masivelor eterogene.


Programare Orientatd pe Obiecte - Principii

Cap.4 - Redefinirea operatorilor

4.1 Apel prin valoare, adresa (pointer) si referinta pentru tipuri de baz&............se 60
4.2 Functii friend, metode friend si clase friend (prietene)
4.3 Redefinirea operatorilor - introducere...............
4.4 Redefinirea operatorilor ca si metode sau ca si functii friend...
4.5 Distingerea intre formele prefixate si postfixate ale operatorilor.
4.6 Operatorul de atribuire (= )
4.7 Operatorii new gi delete
4.8 Alte exemple de redefinire a operatorilor.
4.9

Cap.
§:1
5.2

.3 Operatii de intrare-iesire faré
5.4 Operafii de intrare-iesire cu format...
5.5 Format de insertie-extractie. Stare a formatului.
5.6 Manipulatori predefiniti ...
5.7 Manipulatori definifi de utilizato
5.8 Intrare — iesire pentru tipuri definite de utilizato:
5.9 Legarea streamurilor (“Tying of streams”) ..
5.10 Buffer-area streamurilor ee
5.11 Streamuri si siruri.
5.12 Streamuri si fisiere.
5.13 COmClUZii ....escsecseeecsseeessseesseecsseccsseecssueeeneeeseteennees

format.

Cap. 6 - Tratarea exceptiilor ...ssssscsessseseeseee

6.1 Originea problemei..

6.2 Solutia traditionala de tratare a erorilor
6.3 Solutia C++ de tratare a exceptiilor.

6.4 Discriminarea excepfiilor.......

6.5 Detalii despre implementarea mecanismului de tratare a exceptiilor................ 112
6.6 Tratare polimorfica a exceptiilo:
6.7 Achizitie si eliberare de resurse.
6.8 Optiuni la tratarea unei exceptii
6.9 Observatii privind mecanismul de tratare a exceptiilo

Anexa.

Bibliografie.......sssesseee ae

pag. 4

Cap. 1 - incapsularea

Cap. 1 - incapsularea

Limbajul C++ este de fapt un superset, 0 dezvoltare a limbajului “C”, pastrand compatibilitatea
“in jos”, adica orice constructie valabila in “C” ramane valabila si in C++, dezvoltarea constind |

in adaugarea de facilitati noi.

1.1 Supraincdrcarea numelui functiilor. Functii cu parametri
ayand valori implicite
in “C++” este permis s4 avem mai multe functii cu acelasi nume dar diferind prin numarul sau
pul parametrilor (“supraincdrearea numelui functiilor”). Aceasti facilitate nu este strict
legata de orientarea pe obiecte dar este des intalnita, asa incat o prezentam in continuare.

Facilitatea este descrisi de exemplul urmator, in care supraincdrcém numele functiei Calc1:

long Calcl(long a, long b, long c) // prima varianta

{
return (atbtc);

}

long Calcl(ltong a, long b) // a doua varianta

{ // difera prin numarul
return (atb); // parametrilor

}

long Calci(long a, int b) // a treia varianta

{ // difera prin tipul
return (atb); // parametrilor

}

compilatorul decizénd in momentul apelului despre care functie este vorba, tindnd cont de
numarul si tipul parametrilor implicati la apel:

long m=5, n=10, o=20, r;

int q*7;

(m, n, O)7 // v=35, s-a apelat prima varianta
(m, nn)? // v=15, s-a apelat varianta a doua
r=Calcl(m, q); // r=12, s-a apelat varianta a treia

pag. 5

Programare Orientaté pe Obiecte - Principii

Trebuie facute doua remarci, evidente dealtfel:
e compilatorul nu distinge intre functii pe baza tipului returnat (care nu se poate indica la

apel).
e functiile supraincarcate avand acelasi numar de parametri trebuie sa difere cel putin prin

tipul unei perechi de parametri.

ambele restrictii fiind necesare compilatorului pentru a putea lua decizia corecta (in caz contrar

se genereaza eroare la compilare).

De asemenea se permite in “C++” si avem functii cu parametri avand valori implicite
(valorile implicite pot fi doar constante):

long Calc2(long a, long b=7, long c=9) // paramatrii b sic
{ // pot lua valori

return (atb+c); // implicite
}

cu apeluri posibile

long m=5, n=10, o=20, xr;

r=Calc2(m, n, 0); // r=35, nu se considera valori implicite
r=Calc2(m, n); // r=24, c ia valoarea implicita
r=Calc2 (m); // v=21, b si c iau valorile implicite

Si aici rimane de facut o remarcd, evident dealtfel:
© parametrii pot lua valori implicite doar dinspre ultimul inspre primul, fara a s&ri vreunul.

Considerand atat supraincarcarea numelui functiilor cat si functiile avand valori implicite, mai

atragem atentia asupra faptului ca:
© nu este permis si avem atat o functie (supraincarcata) cu un anumit numéar de parametri
cat si o functie cu valori implicite care si permit apel cu acelasi numéar si tip de

parametri.

ja fiind necesara compilatorului pentru a putea lua decizia corecta (in caz contrar din nou

restrict
se genereaza eroare la compilare).

1.2 Amintiri si generalitati despre structuri

incepem introducerea notiunii de clas din limbajul "C++" pornind de la nofiunea de structura

pag. 6

Cap. 1 - incapsularea

din limbajul "C" cu care se aseamani si pe care o consideram (cel putin in mare) asimilata.

Sa presupunem ca avem de gestionat intr-un program 100 pozitii in plan. Cea mai simpla solutie

este de a declara doua tablouri

int x[100] // pozitiile pe axa x
int y[100] // pozitiile pe axa y

Aceasta solutie a grupat toate variabilele avand aceeasi semnificatie in tablouri.

O solutie ceva mai avansat& este de a grupa toate informafiile referitoare la un punct intr-o
structura SPos

struct SPos

{
int x; // pozitia pe axa x
int y; // pozitia pe axa y
Me

instantierea celor 100 perechi de variabile facéndu-se acum astfel:
struct SPos poz[100] // tablou de 100 structuri SPos

Aceasta a doua solutie are avantajul de a face si la nivelul limbajului o legitura intre datele
unei anumite pozitii (legdtura evidenta la nivelul conceptual), De asemenea, suntem siguri ca se
aloca la fel de multe variabile x respectiv y (fapt realizabil si prin dimensiune simbolica).

Solutia se poate simplifica in “C* prin folosirea operatorului typedef

typedef struct
{

init: a; // pozitia pe axa x
int y; // pozitia pe axa y
}SPos;

instantierea facdndu-se acum mult mai simplu:
SPos poz[100] // tablou de 100 variabile de tip SPos

totul operand ca si cand limbajul s-a imbogatit cu un nou tip de date, din care se pot face

simplu instantieri de variabile.

Pentru accesarea unei variabile membru a unei structuri se foloseste operatorul “.”

pag. 7

Programare Orientata pe Obiecte - Principii

poz[5].x // accesarea variabilei membru x din poz[5]

Evident, dacé avem la dispozitie adresa unei variabile de tip structura adresarea se face cu

operatorul “->”.

in “C++” situatia se simplificd, declararea unei structuri incluzind (implicit) si definitia unui

nou tip ca in exemplul urmator:

struct SPos
{
int x; // pozitia pe axa x
int yi // pozitia pe axa
Mi

SPos pos[100] // tablou de 100 variabile de tip SPos

Reamintim faptul ci dimensiunea unei structuri este egal cu suma dimensiunii elementelor

sale, adica:

dimensiune structura = 2 dimensiune membri

Sa presupunem ca avem nevoie de o functie care sa modifice continutul variabilelor membru ale
structurii SPos (si mute pozifia reprezentata de 0 asemenea variabila), Deoarece un simplu apel
prin valoare nu permite modificare valorii de apel (ci doar a copiilor de pe stiva a valorii
parametrilor — problema obligatoriu a fi deja cunoscuta) solutia este de a avea un apel prin
adresd, adicd o functie (de exemplu “Muta”) care si primeasca adresele variabilelor membru

sau, mai simplu, adresa structurii respective (si valorile “pasului” pe cele 2 directii)

void Muta(SPos *p, int dx, int dy)

{
p->x += dx; // matarea pe directia x
p->y += dy; // mutarea pe directia y

apelul fac4ndu-se (de exemplu) astfel (pentru a muta pe axa x cu 100 iar pe axa y cu 50)

Muta( &pos[5], 100, 50);

Aceasta solutie (cu parametru adresa structurii asupra careia se opereaza) se poate aplica tuturor
functiilor care se refera la 0 asemenea structura. Solutia este putin greoaie si nu are eleganta

unei exprimari de genul

pag. 8

Cap. 1 - fncapsularea

pos[5].x

1.3 Introducerea notiunii de clasa

O noutate adusa de limbajul “C++” este aceea ca o structura poate contine nu numai date ci si |

functii. Astfel functia Muta poate fi declarata ca apartinand structurii SPos

struct SPos

{

int x; // pozitia pe axa x
int 4 // pozitia pe axa y
void Muta(SPos *p, int dx, int dy); // functia Muta

yi

void SPos::Muta(SPos *p, int dx, int dy)
{

p->x t= dx; // matarea pe directia x
p->y += dy; // mutarea pe directia y
}
SPos pos[100] // tablou de 100 variabile de tip SPos

Remarcim distinctia intre declararea (prototipului) functiei in cadrul structurii si

implementarea ei (indicand cu operatorul “::”, de rezolutie, structura cdreia fi apartine functia).

Avem acum de a face atat cu variabile membru (ex. x si y) cat si cu functii membru (ex.

Muta). Functiile membru se numesc si metode (notatie preferata, recomandata).

Apelul unei metode se face in acest caz ca gi adresarea unei variabile membru cu operatorul “.”

pos[5}.Muta( &pos[5], 100, 50 );

indicand astfel si la nivelul limbajului cd avem de a face cu un apel de metoda pentru obiectul
pos[5]. Se remarca utilizarea redundant, greoaie a constructiei pos[5] atat in stdnga lui “.” cat si

ca parametru.

Observatia ci formula

dimensiune structura = dimensiune variabile membru

faptul cA corpul metodei nu este chiar inclus in structura, neafectand dimensiunea acesteia, ci

mai degraba este tinut in zona de cod, separat, o singura data, si nu pentru (in) fiecare structura

pag. 9

Programare Orientata pe Obiecte - Principii

in parte. Apare astfel o intrebare fundamentala:
1.4 Cum se leaga un apel de metoda de datele unui obiect
anume (cel apelant) ?

Rezolvarea acestei probleme are la baz& generarea, de catre compilator, la fiecare apel de
metoda, a unui parametru ascuns ¢his (cuvant rezervat al limbajului) care este de fapt un
pointer spre obiectul pentru care s-a apelat metoda. Pointerul this este accesibil programatorului
in corpul metodei. in acest fel se permite accesarea datelor obiectului respectiv (corespunzator)

prin intermediul lui this.
Un apel de genul:
obiect.metoda( parametri ... )
este implementat de c&tre compilator astfel:
metoda( &obiect, parametri ... )

dupa cum vedem locul parametrului ascuns (dar existent!!) this este luat de adresa obiectului

apelant. Desi pointerul this nu se declara explicit el se poate folosi in corpul metodei.

in acest caz este evident ci metoda Muta nu mai are nevoie de parametrul SPos *p (locul sau
fiind luat de pointerul this ascuns) iar implementarea metodei devine:

void SPos::Muta(int dx, int d

{
this->x += dx; // se modifica x-ul coresp. apelului

this->y += dy; // se modifica y-ul coresp. apelului

}

Forma prezentataé mai sus (cu acces la variabilele membru folosind this) este foarte utilé din
punct de vedere didactic dar este greoaie, incomoda pentru utilizarea ei frecventa. Pentru
simplitate compilatorul genereaza implicit apel prin ¢his pentru accesul variabilelor membru

programatorul putand renunta la folosirea lui this:

void SPos::Muta(int dx, int dy)

{
x += dx; // se modifica x-ul coresp. apelului

y += dy; // se modifica y-ul coresp. apelului
}

Desi nu se mai poate observa direct, nu uitém rolul esential al lui s#is in legarea apelului unei

pag. 10

Cap. 1 - incapsularea

metode de datele structurii pentru care s-a apelat metoda.

pos[5] -Muta(100,50); // x+= 100; yt= 50 pt. struct. pos[5]
pos [3] .Muta(200,150); // x+= 200; y+=150 pt. struct. pos [3]

Utilizarea explicit’ a pointerului this este esentialé in situatiile in care ne intereseazi adresa

structurii in cauza (care trebuie de exemplu introdusa intr-o lista fnlanquita).

O alta facilitate introdusa de "C++" este posibilitatea restrictionarii accesului la membri. {n
acest scop fiecare membru are atributul "public" sau "private" cu urmatoarea semunificatie:
© public — acesti membri pot fi accesati din “interiorul” structurii si pot fi accesati din

“exteriorul” structurii.
® private — acesti membri pot fi accesati din “interiorul” structurii si nu pot fi accesati din

“exteriorul” structurii.

in cele de mai sus numim “interiorul” structurii corpul metodelor structurii iar "exteriorul"

structurii acele sectiuni de cod care utilizeaz& instante ale structurii (dar nu sunt membre in

structura).

Remarcam ca atributul public / private se refera doar la modul in care pot fi accesati membrii
structurii din exteriorul acesteia (metodele avand acces la membrii indiferent de atributul

membrilor).
Un exemplu de declarare a structurii SPos poate fi urmatorul (fira a fi inca util practic)

struct Sros
{

private:
int // pozitia pe axa x
int y; // pozitia pe axa y
public:
Muta (int int dy); // metoda Muta

i
in care variabilele membru x si y sunt declarate private (pot fi accesate doar din Muta) iar
metoda Muta este declaraté publica. Remarcim faptul c& atributul privat / public nu trebuie
specificat pentru fiecare membru in parte el fiind valabil pentru toti membrii urmatori pana la
stabilirea unui nou atribut. In acest caz tentativa de accesare a membrului x din afara structurii

este eroare de compilare.

pag. 11

Programare Orientata pe Obiecte - Principii

Cu aceste notiuni pregatite putem introduce notiunea de clasa:

O claséi (“class“) este o entitate asemanitoare cu o structurd singura diferentit

constand in aceea cé membrii unei structuri sunt implicit publici iar membrii

unei clase sunt implicit privati.

Programarea Orientata pe Obiecte (“Object Oriented Programming” - OOP) este de fapt
programare bazata pe clase (teoretic pe structuri gi clase, dar practic aproape intotdeauna pe
clase). De aceea limbajul “C++” se numea la inceput "C with Classes".

Subliniem cd toate caracteristicile prezentate anterior (in “C++”) pentru structuri sunt valabile si
pentru clase. Ele au fost enuntate pentru structuri pentru ca introducerea notiunii de clas& si se
faci treptat, pe baza cunostinfelor avute despre structuri. Desi in mod intentionat am evitat pand
in acest punct folosirea notiunii de clasa (preferand pe cea de structura) tot materialul anterior

referitor la “C++” poate fi recitit inlocuind structura (struct) cu clasa (class).

1.5 Incapsularea datelor

Sintaxa de definire a unui nou tip devine acum:

specificator nume

{
[[tip_acces:] lista_membri_1]

[[tip_acces:] lista_membri_2]
ff
Me
in care specificator poate fi class, struct sau union iar tip_acces poate fi public sau private.
Diferenta intre class, struct si union constd (in principal) doar in tipul de acces implicit al
membrilor: privat pentru class si public pentru struct si union. In expresia anterioara am indicat

prin paranteze drepte cAmpuri care pot lipsi.

Proprietatea unui membru de a fi privat este cea care sta la baza conceptului de incapsulare.
Acesta se refer la posibilitatea de a crea obiecte incapsulate, obiecte la care nu se pot accesa
(vedea) anumiti membri. Se obtine astfel o capsula inchisi ermetic, utilizatorul obiectului
neavand acces la interiorul acestuia. Mai putin formal, se poate face asocierea cu cutia neagra
de la electrotehnica (la care accesul se face doar pe la borne — adica prin membrii publici) sau
cu o nuca (la o nucd de cocos orificiile putand de asemenea fi asociate cu membrii publici).

Recomandarea este aceea de a avea datele private si de a asigura un set de metode publice

pag. 12

Cap. 1 - incapsularea

necesare manipulrii datelor (atat ct este permis de catre proiectantul clasei). |

O problema ridicata de caracterul privat al datelor este imposibilitatea atribuirii spre ele a unor |
valori initiale (in lipsa acestei initializari datele confinand valori pe care nu ne putem baza —
gunoi, “garbage”). Aceast& problema se poate rezolva in principiu in doua feluri:

e prin utilizarca unor metode publice de atribuire / citire a valorii variabile

class SPos

{

int x; // implicit private

int y; // implicit private
public:

void Muta(int dx, int dy); // metoda Muta

int GetX(){return x;} // citire variabila x

void SetX(int x0) {x=x0;)} // atvibuire variabila x
he

e sau prin existenta unei metode publice doar de atribuire (initializare) a datelor private

class SPos
{

int x; // implicit private
int y; // implicit private
public:
void Muta(int dx, int dy); // metoda Muta
void Atribuie(int x0, int y0) // metoda de atribuire
{x=x0; y=y0}

Me

fn primul caz avem acces complet la variabila x (chiar daca indirect prin metodele de GetX si
Setx) iar in al doilea putem doar atribui valori (initiale) membrilor privati. Desi nu este
important pentru subiectul incapsularii, implementarea metodelor imediat in cadrul declaririi
obiectului este echivalenté cu declararea acestor metode ca si in-line (care nu se apeleaza
efectiv ci se expandeaza). Dupa cum se stie, se recomanda a se implementa in-line metode
(functii) la care corpul este foarte scurt (cum ar fi GetX respectiv SetX). Am ales aceasta solutie

in exemplele anterioare doar pentru a introduce si notiunea de in-line.

Solujia a doua de initializare (cu o singuri metoda de initializare a intregului obiect) este

importanta in sensul ca va evolua in constructor (dupa cum vom vedea mai tarziu).

inca o remarca: incapsularea ne permite nu numai blocarea accesului la o variabilA membru
(totala) ci si posibilitatea controlarii accesului la variabilele membru ca in exemplul urmator:

pag. 13

Programare Orientata pe Obiecte - Principii

void CPos::Atribuie(int x0, int ¥0)

{
if( x0 < 0) x0 = 0;
if( x0 >= 800 ) x0 = 799;
if( yO < 0) yO = 0;
if( yO >= 600 ) yO = 599;
x=x0;y=y0;

}

in care am impus limite pentru valorile permise pentru x si y (de exemplu limitele coordonatelor
ecranului). in acest fel se poate controla ce anume atribuiri se fac inspre variabilele obiectului
(posibilitatea numitorului 0 intr-o clasé CNumarRational este evident dezastruoasa) scapand de
o multime de griji ulterioare in ceea ce priveste posibilitatea introducerii de date eronate de

catre utilizatorul clasei.

De cele mai multe ori se prefer aceasta solutie de a avea o singura metoda de atribuire pentru

toti membrii clasei. Acest lucru ne obliga la prezentarea urmatoarei facilitati a limbajului C++:

1.6 Constructori si destructori

Atragem atentia asupra faptului cd declararea unei clase este echivalenta cu declararea unui
nou tip de date. Alocarea de obiecte de acest tip se face DOAR la instantierea unor asemenea
obiecte (definire de variabile de acest tip). Deci, prin obiect se intelege o instanta a unei clase.

Dupa& cum am discutat si anterior, odatd instantiat un obiect este esentiala initializarea
acestuia, fapt care impune o oarecare incalcare a principiului incapsularii prin folosirea unor

metode publice pentru initializarea variabilelor membru.

Desi facilitatile de suprainc4rcare a numelui functiilor si funcfiile cu parametri luand valori
implicite au fost prezentate pentru functii oarecare, ele sunt valabile si pentru metode ale

obiectelor (in fapt tot niste functii, dar care au in plus parametrul ascuns this).

Tocmai pentru a nu ne permite s4 uitém apelul unei metode de inifializare C++ introduce
notiunea de constructor:

Un constructor este o metodé avand acelasi nume cu al clasei
c&reia fi apartine si care prezint{ urm&toarele proprietati:

© nuare tip returnat (nici macar void)
© poate beneficia de parametri impliciti si de supraincdrcarea numelui (ca orice alta

pag. 14

Cap. | - incapsularea

metoda). Putem deci avea mai multi (oricati) constructori pentru o clasa.

¢ la instantierea unui obiect se apeleazd constructorul potrivit (conform parametrilor de
apel).

© daca programatorul nu declara explicit nici un constructor pentru o clas4 compilatorul
genereaza unul public, fara parametri si care nu face nimic. Din acest motiv se puteau

instan{ia structuri fara a avea constructori declarati explicit.

Remarcém ca, la orice instanticre de obiect (fie ea staticd sau dinamica) se_apeleaza
IMPLICIT constructorul potrivit (conform parametrilor furnizati). Daca acest constructor nu

exist se genereaz& eroare de compilare. Constructorii variabilelor globale se apeleaza inainte
de apelul lui main(), cei ai variabilelor locale la intrarea in functia respectiva iar cei a

variabilelor alocate dinamic (cu new) in momentul alocarii.

Rolul principal al constructorului este acela de a initializa obiectul pentru care a fost apelat.

Acest lucru inseamna, de obicei,
© inifializarea tuturor variabilelor membru la valori determinate (prin parametri sau implicit)
aducand astfel obiectul intr-o stare determinats. Memoria pentru obiectul propriu-zis
(pentru variabilele membru) a fost deja alocati, acest lucru nu este sarcina

constructorului.
e alte initializari necesare obiectului (aloc&ri pentru pointerii din cadrul obiectului, de

exemplu, aici se poate aloca memorie pentru bufferele necesare).

Se numeste constructor implicit un constructor fara parametri, obtinut fie prin declararea
explicitd a unui constructor fara parametri fie prin generarea implicita de catre compilator a unui
constructor fara parametri (daca nu este definit explicit nici un constructor). Existenta unui

constructor implicit este esentialé pentru a putea initializa tablouri (unde nu se pot apela

explicit constructorii).

Ca si complement al constructorului s-a introdus notiunea de destructor:

Un destructor este o metodé avand acelasi nume cu al clasei dar precedat de ~

si care prezinta urmatoarele proprietati
e nuare tip returnat (nici macar void)
© nuare parametri, deci nu putem deci avea mai multi destructori pentru o clasa.

e la distrugerea unui obiect se apeleaza destructorul sau.

Remarcam faptul ca, Ja orice disparitie de obiect (fie el static sau dinamic) se_apeleazi

pag. 15

Programare Orientata pe Obiecte - Principii

IMPLICIT destructorul sau. Destructorii variabilelor globale se apeleazi dupa iesirea din
functia main(), cei ai variabilelor locale la iesirea din functia respectiva iar cei ai variabilelor
alocate dinamic (cu new) in momentul distrugerii (eliberarii) explicite a obiectului (cu delete).

Rolul principal al destructorului este opus rolului constructorului (de unde si notatia sa bazata
pe ~, care ca si operator are sens de complement), de obicei acela de eliberare a memoriei

suplimentare alocate de c&tre constructor.

Considerand 0 clasa CX avand atat constructor fara parametri cat si constructor cu 2 parametri
intregi, alocarea de obiecte statice pe baza acestor constructori se face astfel:

CX a, b(1,2); // a foloseste constructorul fara parametri
// » foloseste constructorul cu 2 parametri

1.7 Operatorii new si delete

in “C++” se introduce operatorul new ca gi alternativa la alocarea de memorie folosind functia

malloc. Putem deci aloca o variabila intreaga astfel:

int *p;
p =
p = new int;

(int*)malloc(sizeof (int) ); // folosind malloc
// folosind new

La fel se poate aloca si un obiect de tip declarat de programator (de exemplu CPos):

CPos *p;
p = (CPOs*)malloc(sizeof(CPos) );

Pp = new CPos;

// fslosind malloc
// folosind nev
Desi ambele solutii instantiazé un nou obiect de tipul dorit remarcém avantajele folosirii lui

new:
© se cunoaste dimensiunea obiectului (simplificdnd expresia de la alocare).

e se apeleaza automat un constructor al clasei respective, asigurand initializarea automata

a obiectului.

Pereche cu new s-a introdus operatorul delete in locul apelului functiei free:

// eliberare folosind free

free(p);
// eliberare folosind delete

delete p;

Desi ambele solutii distrug obiect remarcim avantajul folosirii lui delete:

pag. 16

Cap. 1 - Incapsularea

© se apeleazi automat destructorul clasei respective

1.8 Cazuri particulare de constructori care meritd remarcate

Desi cazul cel mai general al unui constructor este cel care are parametri de tipuri de date
oarecare (asemandtor cu cei prezentati anterior) meritA prezentate unele cazuri particulare.

Consideram o clasa in discutie CX gi alte clase oarecare CT.

¢ Constructorul general este, cu aceste notatii:
CX: {CR (CT1, CPZ 0 0)
e Constructorul implicit este:

CX::CX()

si prezintaé, dupa cum am amintit anterior, particularitatea ci un asemenea constructor (avand

corpul vid) este generat implicit de compilator atunci cand clasa CX nu are nici un constructor

declarat explicit.

e Constructorul de copicre este:

CX::CX(CX&,...)
unde, daca mai exist parametri, acestia trebuie sa ia valori implicite in cazul apelului respectiv.

Se foloseste la declaratii de tipul:

CX a;
CX b=a; // aici se apeleazd constructorul de copiere

sau la apeluri prin valoare si returnare de valori de tip obiect:

CX Functie(CX ol, ...)
{

a diverse prelucraéri
return 02;
}

cu apelul posibil

b=Functie(a);

pag. 17

Programare Orientata pe Obiecte - Principii

care va apela constructorul de copiere pentru copierea obiectelor parametru (in exemplu a in
ol si, la fel, pentru eventuale alte argumente de tip obiect) si pentru copierea obiectului intors

(in exemplu se copiaz& 02 intr-un obiect temporar si apoi acesta este atribuit lui b, folosind

operatorul =).

Atragem atentia asupra urmatoarei greseli frecvente: se confunda constructorul de copiere cu
operatorul de atribuire (care va fi prezentat ulterior la redefinirea operatorului “=-”)

in ceea ce priveste constructorul de copiere mai facem urmatoarele remarci:
e daca nu se furnizeaza explicit un constructor de copiere compilatorul genereaza implicit
unul care copiazi membru cu membru datele obiectelor.
¢ atenfie la copicrea membru cu membru, care este periculoasa cdnd se copiazi membri de
tip pointeri fara a face o copie zonei pointate. Aceasta posibilitate impune o data in
plus necesitatea constructorilor de copiere (si a redefinirii operatorului de atribuire).

e Constructorul de conversie este:

CX: :CX(TX&,
CX::CK(TX,...)

unde, dac mai exista parametri, acestia trebuie sa ia valori implicite in cazul apelului respectiv.
Este asemanator cu cel de copiere doar c& obiectul parametru are alt tip. Cu cele precizate

inainte consideram ca rolul si caracteristicile acestui constructor sunt evidente.

1.9 Exemplu pentru constructori / destructori

Prezentam urmatorul exemplu in scopul de a demonstra apelul constructorilor si destructorilor:

#include <stdio.h>

class CA
{
int x,
public:
CA() {x=0; y=O;printf£("\nConstr implicit");
CA(int m,int n=0) {x=m;y=n;printf("\nConstr general ");}
A(CA &0) {xX=O.x;y=o.y;printf£("\nConstr copiere ");}
~CA() { printf ("\nDestructor ©) pF
Li
CA a,b(1,2); // 2 obiecte globale

void main()

pag. 18

Cap. 1 - incapsularea

{
CA *c,d(2),*e, £[2],g=b, *h; // 4 obiecte locale

printf("\nInceput cod main");
c=new CA(5);

CF
print£("\nSfirsit cod main");
}

care va afisa urmatorul rezultat:

Constr implicit // pentru a
Constr general // pentru b
Constr general // pentru d
Constr implicit // pentru £[0}
Constr implicit // pentru £[1]
Constr copiere // pentru g
Inceput cod main

Constr general // pentru c
Constr implicit // pentru e
Destructor // pentru c
Sfirsit cod main

Destructor // pentru g
Destructor // pentru £[1]
Destructor // pentru f[0]
Destructor // pentru d
Destructor // pentru b
Destructor // pentru a

1.10 Membrii statici

O alta facilitate introdusa in C++ este posibilitatea de a defini membri statici, o clasi putand

avea atat variabile membru statice ct si functii statice:
4 o singura data pe clas si nu pentru fiecare

e Variabilele membru statice se instantiz
instant (obicct) in parte, deci nu maresc dimensiunea obiectului. Se pot inifializa
explicit in modulul fn care a fost definita clasa.

e Functiile statice nu au parametrul ascuns this deci preferim si nu le numim metode.

Ele se pot apela insa atat ca si metodele (pentru un obiect anume) ct si nelegate de un
obiect. Neavind pointerul this, nu pot accesa variabile membru nestatice (decdt dacd

furnizam explicit un parametru referin{a spre obiect).

Principalul rol al membrilor statici este acela de a reduce variabilele si functiile globale care au

legatura cu aceasta clasa prin includerea lor aparenta in cadrul clasei.

pag. 19

Programare Orientata pe Obiecte - Principii

Pentru exemplificare consideram urmatorul program:

#include <stdio.h>

class X
{
int x; // nefolosit
static int y; // numar instantieri
public:
void Met1(){print£("\nNumar instante = %d",y);}

static void Met2(){printf("\nNumar instante = id",y);)}
// static void Met3(){x++;} // eroare de compilare, nu se
// pot accesa membrii ne-statici

X() fyt+7} // incrementare numar instantieri
~K() {y--7}

Mi

int X::y=0; // initializare variabila membru STATICA

// se poate face chiar daca este privata !!
X a,b;

void main()

{
X c,d,*e;
e=new X;
a.Met1l();
delete e;
d.Met2(); // apel legat de un obiect
X::Met2(); // apel nelegat de un obiect (legat de clasa)
ff Kisytt; se genereaza eroare y fiind priveta
}

care va produce urmatorul rezultat:

Numar instante = 5 // pentru a,b,c,d,e
Numar instante = 4 // s-a distrus e
Numar instante

Membrii statici sunt de fapt variabile si functii globale care au insi domeniul de vizibilitate

redus la clasa din care fac parte.
Consideram ca analiza rezultatelor afisate dovedeste proprietatile enuntate anterior.

in problema dimensiunii unui obiect trebuie facut urmatoarea detaliere:

pag. 20

Cap. 1 - Incapsularea

dimensiune obiect = © dimensiune variabile membru
(fara a considera si variabilele membru statice)

evident (sperim) in contextul celor prezentate anterior.

1.11 Variabile membru de tip property

in cele ce urmeaza prezentém simplificat una din extensiile limbajului C-++, asa cum a fost ea
introdusi de firma Borland. Desi nu este inclus& in standardul C+t, considerim ca este
interesanté si meriti prezentaté, ea fiind valabila (cu modificiri minore) si pentru alte

compilatoare (ideile bune se “regasesc” la toat lumea).

S-a introdus un cuvént cheie suplimentar __Property care poate fi “aplicat” unei variabile
membru. Deoarece nofiunea de proprietate se refera de obicei la toate variabilele membru ale
unui obiect vom folosi pentru cele bazate pe __property denumirea de “variabilele membru de
tip __property” (pentru a evita eventualele confuzii care ar putea apare),

Definirea unei variabilele membru de tip __ Property se face conform urmatoarei sintaxe:

__ property TipMembru NumeMembru = { read =

MembruFolositLaCitire , write = MembruFolositLaScriere };

in care TipMembru si NumeMembru au semnificatia obisnuita iar intre acolade se indic& cum se

realizeaza citirea si scrierea variabilei.

Pentru exemplificare (si infelegere) prezentam in continuare un exemplu semnificativ (pentru

diferitele moduri de utilizare):

#include <stdio.h>

class X
(
public:
int x,y;
private:
int Mcitire();

void Mscriere(int v);
char* AflaElementul(int Indexl, float Index2) ;
publi
property int v = {read = x, write = vie
_ property int Medie = {read = Mcitire, write = Mscriere};

pag. 21

Programare Orientat& pe Obiecte - Principii

__ Property char* Tablou[int Index1] [float Index2] =
{read=AflaElementul)};

Job; // aici deja se pot defini obiecte glebale

int X::Mcitire()

{
printf("\n MetCitire x=id y=id ret='d",«,y, (x+y) /2);
return (xty)/2;

Mi

void X::Mscriere(int ValNouaMedie)

{
int shift = ValNouaMedie - (x+y) /2;

printf("\n MetScriere td",ValNouaMedie) ;
x t= shift; y += shift;
Me

char* X::AflaElementul(int Indexl, flcat index2)
{
if (Indexl==0 && Index a return "zero";
else if(Indexl . return "intreg";
else if (Index1==0 && Index2!=0.0) return "flotant";
else return "neimpl";

}

void main()
{
int i; float £;

ob.x=5; ob. y=7;

printf£("\nMAIN_1 1 x=%d y=%d d",ob.x, ob.y, ob.v);
ob. v=10;

printf("\nMAIN_1_2 x=%d y=$d ",ob.x, ob.y);

ob.vt+t;

printf("\nMAIN_ 1 3 x=%d y=%d \n",ob.x, ob.y)?

ob.x=5; ob. y=7;

printf ("\nMAIN_ 2.1 x=$d y=id",ob.x, ob.y);
printf ("\nMAIN_2 2 Medic=%d",ob.Medie) ;
ob.Medie=10;

printf ("\nMAIN_ 2.3 x=ed y=3d ",ob.x, cb.y);
ob.Mediet+;

printf ("\nMAIN_2_4 x=td y=%d \n",ob.x, ob.y);

i=2; £=3.1;
printf ("\nMAIN_3_1 %2d %5.2f %s",i,f,ob.Tablou[i] [f]);
i=0; £=0;

pag. 22

Cap. 1 - incapsularea

Vi

La rulare se

MAIN
Me
MAIN
Me
MAIN
Me
Me
MAIN

MAIN
MAIN
MAIN
MAIN
MAIN
MAIN

printf£("\nMAIN_3 2 %2d %5.2f
i=l; £=2.0;
print£("\nMAIN_3_3 12d $5.2
i=S; f=0.0;

printf ("\nb
i=0; £=3.7;

printf£("\nMAIN_3 5 %2d %5.2f
i=3; £=2.1 ~

printf ("\ni4

1AIN_3_4 2d %5.2£

.IN_3_6 02d %5.2£

va genera urmatorul rezultat:

5 //
21 x=5 y=7 //
tCitire x=5 y=7 ret=6
| 2.2 Medie=6
tScris 10
| 2 3 x
tCitire x=9 y=11 ret=10 Lf
tScriere 11 //
2 4 x=10 y=12
3.1 2 3.10 neimpl Jf
3.2 0 0.00 zero
) 3.3 2 2.00 neimpl
3.4 5 0.00 intreg
3_5 0 3.70 flotant
13.6 3 2.10 neimpl

ts",1,f£,ob.Tablou[i] [£]);
ts",1i,f£,ob.Tablou[i] [f]);
%s",i,f,ob.Tablou[i] [f£]);
%s",i,f,ob.Tablou[i] [f]);

&s",i,£,ob.Tablou[i] TED}

cazul variabilei v

cazul variabilei Medie

pentru incrementare
pentru incrementare

cazul variabilei Tablou

Daca analizim exemplul prezentat remarcam declararea in cadrul clasei a trei variabile membru

de tip __ property (v, Medie, Tablou) fiecare reprezentand un caz deosebit. Si le ludm pe

rand:

in_cazul variabilei v MembruFolositLaCitire si MembruFolositLaScriere sunt
variabile membru ale obiectului. Remarcim faptul c4 variabila membru v se comporta ca si un
membru oarecare din punct de vedere al interfe{ei obiectului, ceea ce este diferit (si specific)

este modul de implementare a acesteia. Pentru citire se foloseste x (valoarea variabilei membru
x) iar la scriere se foloseste y (se face atribuirea in variabila membru y). Aceasta observ atie ar

justifica denumirea acestui tip de variabila ca si variabila virtuala. Deoarece cuvantul virtual
(cuvant cheie) are o semnificatic bine definita in C++ (semnificatie care nu se refera la acest

aspect) vom evita folosirea sa in acest context dar retinem cd o variabilé de tip property

pag. 23

Programare Orientaté pe Obiecte - Principii

este virtuala in sensul comun al cuvdntului: ea exista doar aparent, pe interfata, fara a exista

efectiv in interiorul obiectului.

fn cazul variabilei Medie se exemplificd adevarata “putere” a mecanismului care apare atunci
cand MembruFolositLaCitire si MembruFolositLaScriere nu mai sunt variabile
membru ci sunt metode (Mcitire respectiv Mscriere). Din nou variabila de tip __ property
(Medie) exist (doar) aparent pe interfata obiectului dar scrierea si respectiv citirea ei se fac
acum prin intermediul metodelor specificate. Apelul acestora se face automat, de catre
compilator, la momentul necesar. in mod logic, metoda de scriere necesita un parametru de tipul

membrului__ property iar metoda de citire intoarce un asemenea tip.

Este posibilé si declararea unor variabile de tip __property care si foloseasca solutii diferite

pentru implementarea scrierii $i citirii (folosind o variabila membru si o metoda).

In acest caz

in cazul variabilei Tablou s-a dorit declararea unui tablou de tip property
metodele de citire si scriere trebuie s4 aiba parametri suplimentari corespunzatori indicilor
necesari. Daca analizim rezultatele afisate observam ci metoda AflaElementul (avand doi
parametri) este cea care este apelata la citirea fiecarui element si care decide (in functie de
indecsii particulari) continutul tabloului. Un exemplu in care acest lucru ar fi util (si elegant) ar
fi cel al implementarii unei matrici rare (cu elementele mentinute eventual intr-o lista), dar in

care acest lucru este ascuns utilizatorului obiectului.

O posibilitate interesanta (valabila pentru toate cele trei situafii prezentate) este aceea de a avea
specificata doar una dintre actiuni (de exemplu read) iar cealalta sa lipseasca (ca in cazul Tablou
din exemplu). in acest caz tentativa de a face astfel de referiri spre acel membru (in exemplul
prezentat - atribuiri) va fi detectaté de catre compilator ca eroare de tipul “variabila nu este
accesibila”. Acest fapt deschide perspectiva posibilitatii realizarii unei incapsulari controlate
(valabila doar pentru scriere sau citire). Evident acest lucru s-ar fi putut face prin intermediul
variabilei membru private si furnizarea doar a unei metode de citire sau scriere dar cu pretul

‘sacrificarii elegantei sintaxei de accesare (care acum continua sa fie cea naturala).

Atragem atenfia asupra pericolului care apare atunci cand in interiorul unei metode de citire sau
scriere a unei variabile de tip __ property s-ar accesa din nou acea variabila. in mod logic
compilatorul apeleazi din nou metoda respectiva efectul fiind depdsirea stivei (tipic pentru

situatia unor apeluri recursive fara conditie de oprire).

in problema dimensiunii unui obiect trebuie facut urmatoarea detaliere:

pag. 24

Cap. 1 - Incapsularea

dimensiune obiect = 2 dimensiune variabile membru
(fara a considera si variabilele de tip __ property)

evident (speram) in contextul celor prezentate anterior.

Variabilele de tip _ property ascund detaliile de implementare. Ceea ca araté ca un simplu
membru poate duce in momentul accesirii sale la operatii complexe (de exemplu cautari in baze
de date). Accentuam inca o data ca, din punct de vedere al utilizatorului obiectului, variabilele
de tip __ property se comporta ca si variabile obigsnuite. Singurul lucru care nu se poate face
asupra acestora este de a sc lua adresa lor si, deci, nu se pot transmite functiilor ca si parametru

prin referinta.

Amintim (anticipand polimorfismul) si faptul ci metodele folosite pentru citirea gi scrierea
variabilelor de tip property pot fi virtuale deci vor putea fi implementate diferit

(polimorfic) in ierarhii de clase.

Rezumat - de tinut minte :-)

° supraincarcarea numelui functiilor, functii cu parametri luand valori implicite

° structuré ~ variabile membru
° structurile pot include si functii —> functii membru = metode
° legarea metodelor de datele unui obiect prin pointerul ascuns this

e atributele public si private
° class echivalent cu struct dar membrii implicit privati
° incapsularea datelor si controlul accesului

° initializarea variabilelor membru — constructori

° constructori generali, cazuri particulare: impliciti, de copiere, de conversie
e destructori

° membrii statici

° __property — variabile membru aparente

pag. 25

Programare Orientata pe Obiecte - Principii

Cap. 2 - Mostenirea

2.1 Instante incuibarite

Desi instanfele incuibarite nu sunt un caz de mostenire, considerim utild explicarea pentru
inceput a acestei facilitati. In fapt nu este ceva deosebit, doar faptul ca 0 clas& are variabilé

membru o instant a altei clase.

S& consideram cazul urmator in care clasa CB are ca si membru un obiect de tipul CA.

class CA
{
public:
int x;
CA() {x=0;} // constructor implicit
CA(int x0) {x=x0;} // constructor explicit
Ve

class CB

{

public:
CA a; // instanta incuibarita
int y;
CB(); // constructor implicit
CB(int x0,int yO); // constructor explicit

Ve

si o instant a clasei CB

CB b(1,2);

fn mod evident variabilele x si y se pot accesa folosind urmatoarea sintaxa (care ar fi valabila si

pentru cazul apelului eventualelor metode):

bey: // variabila membrvu y
b.a.x; // variabila membru x a variabilei membru a

Constructorul clasei CB poate fi scris in forma:

pag. 26

Cap. 2 — Mostenirea

CB::CB(int x0,int 0) :a(x0) // forma 1
{

yO; // corpul propriu-zis al constructorului
}

sau in forma echivalenta:

CB::CB(int x0,int y0):a(x0),y(y0) // forma 2

{

}
fn ambele forme remarcam faptul c& constructorul instantei incuibirite a trebuie specificat la
implementarea constructorului clasei CB conform sintaxei prezentate. Daca acest lucru nu este
facut compilatorul foloseste constructorul implicit (sau luand valori implicite) al clasei CA.

Daca acesta nu exista se genereaza eroare la compilare.

Mentionam ca constructia :a(x0) este apel explicit de constructor (deci nu se precizeaza tipul

ci doar valoarea de apel).

Remarcém forma echivalenta de declarare a constructorului (forma2) la care si initializarea
variabilei membru y se face asemanator cu initializarea instanfei incuibarite. Se accentueaza
astfel faptul ca tipurile predefinite si clasele definite de utilizator se comporta la fel (tipurile

predefinite si clasele sunt ambele tipuri de date).

in toate situaliile constructorul instantei incuibarite se executi jinainte de executia

constructorului clasei “incuibaritoare” :-)

Dac o clasi are mai multe instante incuibarite trebuie mentionat la implementarea
constructorului acesteia (si nu la declararea lui) lista apelurilor constructorilor instantelor

incuibarite. Acestia se vor executa (intr-o ordine insa nedefinita) inainte de a trece la

constructorul clasei “incuibaritoare”.

in mod evident daci clasa “incuibaritoare” nu contine o instanta a clasei “incuibarite” ci doar un
pointer spre un asemenea obiect atunci sarcina instantierii si distrugerii obiectului indicat de

acel pointer ne revine in totalitate (de obicei problema se rezolva pe constructorul/destructorul

clasei “incuibaritoare”),

Din punct de vedere al dimensiunii obiectului rezultat avem urmitoarele:

pag. 27

Programare Orientata pe Obiecte - Principii

Dimensiunea unui obiect al clasei obtinute
este egal& cu suma dimensiunii instanfelor incuibarite
plus suma dimensiunii membrilor obisnuifi adaugati

S-a obfinut astfel un obiect mai mare, care include instantele incuibarite, dar sintaxa modului de
acces la membrii astfel adaugati indicd faptul ca membrii obtinuti din instantele incuibarite nu
sunt chiar membrii directi ai obiectului mare.

Ceea ce ar fi de dorit ar fi ca clasa “mai mare” s& preia membrii clasei “mici” intr-un mod
asemanator cazului cAnd acestia ar fi membri proprii (cu sintaxa de acces cea simpla). Acest

lucru este realizat prin mostenire.

2.2 Mostenirea
Reludnd exemplul anterior dar considerénd mostenire avem:

class CBaza

{

public:
int x;
CBaza() {x=0; } // constructor implicit
CBaza(int x0) {x=x0;} // constructor explicit

he

class CDeriv: public CBaza // aici declaram mostenirea

{

public:
int. yi
CDeriv(); // constructor implicit
CDeriv(int x0,int y0); // constructor explicit

7
CDeriv b(1,2);

in acest caz variabilele x si y se pot accesa folosind sintaxa normal, asemAnatoare cazului in
care membrii mosteniti fac parte efectiv din obiectul rezultat (sintaxa este valabilé si pentru

cazul apelului eventualelor metode):

b.y; // variabila membru y ad&ugat&
Dx: // variabila membru x obtinut& prin mostenire

Constructorul clasei CDeriv poate fi scris asemanator in forma:

CDeriv::CDeriv(int x0,int y0):CBaza(x0) // forma 1
{

pag. 28

Cap. 2 — Mostenirea

y=y0;

sau in forma echivalenta prezentata anterior (forma 2):

CDeriv::CDeriv(int x0,int y0):CBaza(x0),y(y0) // forma 2
{
}
Remarcam ca s-a precizat la declararea clasei CDeriv care este clasa (tipul) de baz& CBaza si nu

0 instanta a acesteia (cum a fost in cazul anterior al instantelor incuibarite),

fn acest mod simplu se pot obtine clase noi prin preluarea (si dezvoltarea) celor existente prin

adaugarea de noi variabile membru si metode.

Mai riguros declararea unei clase derivate devine:

spec_clasa nume_clasa_deriv {:tip_mostenire nume_clasa_baza]
{

[public: ] lista_membri_1 J

[private: J lista_membri_2 J

}

[
[
{
} (lista_obiecte];

unde "spec_clasa" poate fi "struct" sau "class" (NU si "union") iar "tip_mostenire" poate fi

(deocamdata) tot "public" sau "private".

Se remarca faptul ca sintaxa prezentata este o generalizare a cazului de definire a unei clase fard

ane baza pe mostenire (caz in care clasa de baz lipseste).

Din punct de vedere al relatiei de mostenire clasele implicate se numesc clasd de baza si clasi
derivata. Se spune ca:

° clasa derivata deriveaza din clasa de baza, mosteneste clasa de bazd

* clasa de baza se deriveaza in clasa derivata, este mostenita de clasa derivati.

Totul se petrece ca si cum obiectul de baza este imbogatit prin derivare cu variabile membru si
metode noi, pastrand (“in sdmbure”) gi tot ce exista in clasa de bazi. Spre deosebire de cazul
instantelor incuibarite, in cazul derivarii membrii mosteniti sunt membri propriu-zisi (directi)
ai clasei rezultate (ca si cand clasa derivata ar fi fost declarata de la inceput, continand toti

membrii — mosteniti sau adaugati).

pag. 29

Programare Orientata pe Obiecte - Principii

Din punct de vedere al dimensiunii obiectului rezultat prin mostenire avem urmatoarele:

Dimensiunea unui obiect al clasei derivate
este egal cu dimensiunea unui obiect al clasei de baz
plus suma dimensiunii membrilor noi adaugati in clasa derivata

Membrii noi adaugati in clasa derivata au drepturile de acces corespunzdtoare modului de
definire a lor (ca in cazul fara mostenire). O problema deosebitd este aceea a drepturilor de
acces pe care membrii mosteniti le au in cadrul clasei derivate in functie de drepturile de acces
avute in clasa de baz si tipul mostenirii folosite. Aceste drepturi sunt prezentate in Tabelul 2.1

(varianta 1, simplificata).

tip mostenire Drept acces avut drept acces rezultat
(derivare) in clasa de baza __ in clasa derivata
public public public
public private inaccesibil !!!!!!
private public private _
private private inaccesibil !!!!!!

Tabelul 2.1 Drepturi de acces rezultate in urma mostenirii

Analizand datele din tabel remarcim:
* indiferent de tipul mostenirii ce era privat este inaccesibil (in fapt esenta incapsularii).

e ce era public respecta tipul mostenirii.

Apare astfel o dilema: cum trebuie alese drepturile de acces in clasa de baza si tipul mostenirii:
e Membrii privati in clasa de bazd nu se mostenesc.
e Membrii publici in clasa de baza nu beneficiaza de incapsulare.

Deci, membrii privati nu pot fi mostenifi iar membrii publici nu asigura incapsulare, astfel incat
oricum am avea de ales (deocamdata) intre doua rele.

fn aceasta dilema privat / public am avea nevoie de un tip de acces care s& se comporte ca si
privat pentru incapsulare (pentru exteriorul clasei) dar ca si public pentru mostenire, In
acest scop s-a introdus tipul de acces protected si tipul de mostenire protected. Tabelul
anterior devine acum Tabelul 2.2 (varianta finala):

Avem deci trei tipuri de derivare:
e derivare publica - membrii privati rman inaccesibili, ceilalti 1si pastreaza tipul.

e derivare protejata — - membrii privati raman inaccesibili, ceilalti devin protected.

pag. 30

Cap. 2 — Mostenirea

tip mostenire Drept acces avut drept acces rezultat
(derivare) in clasa de baz in clasa derivata
public public public |
public protected protected |
_ public private inaccesibil !!!!!! |
|
protected public _ protected |
protected protected protected
_ protected _private inaccesibil !!!!!! |
private public private
private ____protected private
private private inaccesibil |!!!1!

Tabelul 2.2 Drepturi de acces rezultate in urma mostenirii

e derivare privata - membrii privati ramén inaccesibili, ceilalti devin privay

Se recomanda folosirea atributului protected in clasa de baz (evident, pentru membrii care nu
dorim sa fie oricum publici) si derivare publica sau privaté dupa cum dorim sa permitem sau

nu derivari ulterioare ale clasei obfinute prin derivare,

Subliniem ca prin mostenire nu se modifica tipul de acces la membrii pentru clasa de bazi

ci doar accesul la accstia ca si membri ai clasei derivate.

Daca consideram dou clase ca in secventa urmatoare:

class CBase

{
private:
//ia // membrii privati in CBase
protected:
Sf ® // meambrii protected in CBase
public:
//fe // membrii publici in Chase
MF
class CDerived: tip_derivare CBase
{
private:
//a // membrii privati adaugati in CDerived
protected:
//e // membrii protected adaugati in CDerived
public:
JL // membrii publici adaugati in CDerived
Me

pag. 31

Programare Orientata pe Obiecte - Principii

private protected public interfata
CBase a b c >
public |
v Vv
CDerived d be cf >
CBase a b c >
protected }
ys
CDerived d bee f [>
CBase a b c =>
private Sel
CDerived bed e fj; >

Figura 2.1 “Soarta” membrilor in cazul derivarii publice, protejate si private

in Figura 2.1 se prezinta toti membrii clasei Cderived si tipul de acces al acestora in functie de

tipul derivarii folosite.

Dintre avantajele derivarii amintim:

e economia si reutilizarea de cod — se pot folosi usor secvente de cod gata scrise, timpul
de testare scdznd, clasele de baza fiind probabil bine testate. Mai mult, se pot refolosi
clase chiar si fara o cunoastere a modului cum a fost scris codul din spatele lor (si, de
obicei, asa se intémpl4) fiind suficienta descrierea functionarii acelui cod.

e extensibilitatea — 0 clasi disponibilé poate fi extinsd prin derivari ulterioare. Chiar si
clase de biblioteca pentru care nu este disponibil codul sursa pot fi (si de obicei sunt)
extinse.

e polimorfism — se oferé suport pentru polimorfism, cu toate avantajele ce decurg din

aceasta. Se vor trata in capitolul urmator.

pag. 32

Cap. 2 — Mostenirea

* compromisul inchis-deschis — se asigura un compromis intre cele dovd tendinte esentiale
si contradictorii ale programarii orientate pe obiecte: o clas trebuie s4 fie inchisa,
manipulabila doar prin intermediul unei interfete bine precizate avand detaliile de
implementare ascunse, dar in acelasi timp si deschis&, permitand extinderea ei

ulterioara.

Derivarea are si unele particularitati in sensul cd nu se mostenesc:
e constructorii si destructorii.
e functiile, metodele si clasele friend (se vor prezenta intr-un capitol urmator).

Riguros, declararea unei clase derivate devine acum:

spec_clasa nume_clasa_deriv {:tip_mostenire nume_clasa_baza]
{

( [public: ] lista_membri_1 ]

( [protected: ] lista_mambri_2 ]

{ (private: J lista_membri_3 ]

(. +]
}[lista_ob

ecte);
unde "spee_clasa" poate fi "struct" sau "class" (NU si "union") iar "tip_mostenire" poate fi

"public", “protected” sau "private". Daca spec_clasa este “struct” respectiv “class”
tip_mostenire implicit este “public” respectiv “private” (la fel ca tipul implicit al membrilor).

2.3 Constructori si destructori in contextul derivirii

O problema care se pune in cazul obiectelor obtinute prin derivare este modul cum se
construiesc si se distrug aceste obiecte. Regula spune ca:
¢ la instantierea unui obiect dintr-o clasi derivati (global, local sau dinamic, nu are

importanta), intdi se execut (implicit sau explicit) corpul constructorului_potrivit al

clasei de baza gi abia apoi corpul constructorului potrivit al clasei derivate.
e la distrugerea unui obiect dintr-o clasa derivata, intdi se executa corpul_destructorului
clasei derivate si abia apoi destructorul clasei de baza.

Deci, intotdeauna obiectele se construiesc dinspre interior (‘sémbure”) spre exterior

(“coaja”) si se distrug dinspre exterior spre interior.

Aceasté ordine de execufie existenta (impusi de limbaj) este singura utilA pentru ca
constructorul clasei derivate sé se poaté baza pe membrii mosteniti din clasa de baza gata

pag. 33

Programare Orientat& pe Obiecte - Principii

initializati (de catre constructorul clasei de baz&). Deoarece constructorul clasei de baza nu
cunoaste nimic despre eventuala mostenire si existenfa a membrilor adaugati in clasa derivatd

cealalt& ordine de initializare (dinspre exterior spre interior) nu ar aduce nici un avantaj.

Daca exista si instante incuibarite constructorii / destructorii acestora se apeleazi intre apelul
constructorilor / destructorilor claselor de baz si a celor derivate. Ordinea relativa a apelului

constructorilor / destructorilor instantelor incuibarite este nedefinita.

Daca la implementarea constructorului clasei derivate nu se specifica un apel al constructorului
clasei de baz compilatorul genereaz4 implicit (dacd poate, daca nu da eroare) un apel al
constructorului implicit sau luand valori implicite al clasei de baz&. De cele mai multe ori dorim

ins sa initializim explicit obiectul de baza si atunci trebuie sa facem:
2.4 Apel explicit al unui constructor al clasei de baza

conform sintaxei:

nume_cl_deriv::nume_cl_deriv( (lista_param_1])
:nume_cl_baza([lista_param 2])

{

// constructor propriu-zis al clasei derivates

DL isin & tables

Subliniem incé o dat& cA lista_param_l este o declarare de parametri (au deci si tip) iar

lista_param_2 corespunde unui apel de metoda (constructor), deci se pun doar valori (nu si

tipuri).

Apelul explicit al constructorului clasei de baz este obligatoriu atunci cand clasa de baza nu
are constructor implicit sau constructor cu toti parametrii luand valori implicite.

Pentru exemple de apel explicit al constructorilor clasei de baz se va urmari exemplul prezentat

dupa capitolul urmator legat de conversii.

2.5 Conversii de tip intre clase derivate

Deoarece 0 clasa derivata poate fi privité ca o extensie a clasei de baza C++ permite conversia
unui obiect de clas derivata la un obiect de clas de bazi. De asemenea, un pointer la clasa de

baza poate tine adresa unei instante a clasei derivate.

pag. 34

Cap. 2 — Mostenirea

class CA // clasa de baza

{he
class CB:public CA // clasa derivata

{)i

void mein()
{

CA a; // doua instantieri automatice (locale)
CB b;
CA* pa = new CA; // doua instantieri dinamice

CB* pb = new CB;

// conversii corecte
a=b; // operator de atribuire definit implicit de catre C++

pa=pb;

b=a;
pb=pa;
}

Asemenea conversii se incearca a se realiza, implicit, la apelul functiilor cu parametri de tip

diferit de cel asteptat.

Remarcam un fapt interesant. Daca intr-o clasa de bazi un membru era public si, prin derivare,
el devine privat, el poate fi totusi accesat (!!!!) prin conversia forfata (cast) a unui pointer spre
obiectul derivat catre pointer spre clasa de baza. Chiar daca acest lucru este posibil, el nu este

recomandat.

2.6 Exemplu

Pentru exemplificarea apelului constructorilor / destructorilor in contextul mostenirii prezentim

urmétorul exemplu:

#include <stdio.h>

class CA
{
protected:
int! %).Vj // pentru a putea fi mosteniti
public:
CA() {x=0; y=0; printf("\nConstr A implicit") ;}

CA(int »0,int y0=0)
{x=x0;y=y0;printf£("\nConstr A general ");}

pag. 35

Programare Orientata pe Obiecte - Principii

~CA() {

Le
}

class CB: public CA

{

int: i2;ty

public:

CA a;

// CB sc

printf("\nDestructor A

"Yah

obtine prin derivare

// membrii noi

// clasa incuibarita

CB(){z=0;t=0; printf("\nConstr B implicit");}
CB{int x0,int y0,int z0,int t0=9);
CB(int z20,int t0);

~CB()

Vi

CB::CB(int x0,int y0,int z0,int to

{ printf("\nDestructor B");}

:CA(x0,70),z(z0)

t=t0;printf("\nConstr B general 4 parametri");

CBS2CB(int 20,int boys

(20), (t0)

printf("\nConstr B general 2 parametri");

}

CA a,b(1);
CB c,d(2,3),e(4,5,6);

void main()

{

CA £(7,8),*g7
CB h(9,10,11,12),*i,j[2];

printf£("\nInceput cod

g=new CA(13);
isnew CB(14,15,16);
delete g;

printf ("\nSfirsit cod main");

}

main") ;

care va produce urmatorul rezultat (se va urmari si intelege)

Constr
Constr
Constr
Constr
Constr
Constr
Constr
Constr

A
A
A
A
B
A
A
B

implicit
general
implicit
implicit
implicit
implicit
implicit
general 2 parametri

pentru
pentru
pentru
pentru
pentru
pentru
pentru
pentru

instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta

// obiecte globale

// obiecte locale

2a2ngn00n00 08M

// obiecte dinamice

»

»

pag. 36

Cap. 2 - Mostenirea

Constr A general // pentru
Constr A implicit // pentru
Constr B general 4 parametri // pentru
Constr A general // pentru
Constr A general // pentru
Constr A implicit // pentru
Constr B general 4 parametri // pentru
Constr A implicit // pentru
Constr A implicit // pentru
Constr B implicit // pentru
Constr A implicit // pentru
Constr A implicit // pentru
Constr B implicit // pentru
Inceput cod main

Constr A general // pentru
Constr A general // pentru
Constr A implicit // pentru
Constr B general parametri // pentru
Destructor A // pentru
Sfirsit cod main

Destructor B // pentru
Destructor A // pentru
Destructor A // pentru
Destructor B // pentru
Destructor A // pentru
Destructor A // pentru
Destructor B // pentru
Destructor A // pentru
Destructor A // pentru
Destructor A // pentru
Destructor B // pentru
Destructor A // pentru
Destructor // pentru
Destructor B // pentru
Destructor A // pentru
Destructor A // pentru
Destructor B // pentru
Destructor A // pentru
Destructor A // pentru
Destructor A // pentru
Destructor A // pentru

2.7 Mostenirea multipla

instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta

instanta
instanta
instanta
instanta
instanta

instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta
instanta

777000

j[0]
j[O].a
3 [0]
j(1]
jl1}.a
301)

Qe EEG
Ey)

jt)
j{l].a
31)
j[0)
j[0].a
3[0]

*FQ2O902RR RAD OHTTS

Desi pana acum nu am discutat decat cazul in care o clas mosteneste o singura clasa de baza in

C++ s-a introdus si conceptul de mostenire multipla in care o clasd poate mosteni mai multe

clase de baza.

pag. 37

Programare Orientat&é pe Obiecte - Principii

Din punct de vedere al dimensiunii obiectului rezultat prin mostenire multipla (fara a considera

si clase virtuale) avem urmitoarele:

Dimensiunea unui obiect al clasei derivate cu mostenire multipla
este egala cu suma dimensiunii obiectelor de baz
plus suma dimensiunii membrilor noi adaugati in clasa derivata

Prezentém un exemplu in care clasa derivatdé mosteneste doua clase de baza:

class CBasel
{

public:
int x;
int y;
he

class CBase2

{

public:
int x;
int z;

he

class CDeriv:public CBasel,public CBase2

{

public:
int t;

Ve

consideram o instanta de CDeriv

CDeriv d;

in acest caz membrii se pot accesa in felul urmator (simplu):

d.
d.
d

NMS

Deoarece ambele clase de bazi au un membru x accesul urmator este gasit eronat de catre

compilator datorité ambiguitatii apartenentei lui x — la Cbasel sau Cbase2:

d.x

i consté in prefixarea membrului cu numele clasei cdreia fi

ii ambigui

apartine, folosind “scope access operator” (de rezolutie):

Solutia elimini

pag. 38

Cap. 2 — Mostenirea

d.CBas
d.CBa

// un membru x
// celélalt membru x

x

Desi exemplul a fost prezentat doar cu variabile membru totul se petrece la fel si cu metode.
Toat& discutia legata de drepturi de acces in clasa de bazi si in clasa derivata in functie si de
tipul derivarii ramane valabila de la mostenirea simpla.

Evident metodele fiecdrei clase de baz isi acceseaz& in continuare fara probleme propriile

variabile membru.

Ca $i restrictie, o clasd nu poate mosteni o aceiasi clas de baz de mai multe ori, urmatoarele

exemple fiind eronate

class CA
{he

class CB:public CA
{7

class CC:public CA,public CA // eronat
th

class CD:public CA,public CB // eronat
(};

dar situatia urmatoare este considerata valida de catre compilator:

class CA

{

public:
int x;

Me

class CB:public CA
the

class CC:public CA
the

class CD:public CB,public Cc // corect
{

int a,b;
Ve

Din nou avem doua copii ale clasei CA care insA pot fi diferentiate folosind operatorul de

pag. 39

Programare Orientata pe Obiecte - Principii

rezolutie “::” si numele clasei (CB sau CC).

Pentru a realiza o mostenire multipla este suficient sa se specifice dupa numele clasei derivate o
listé de clase de baza, separate prin virgula (evident, acestea pot fi prefixate fiecare cu tipul
mostenirii). Atragem atentia insa ci ordinea claselor de baz nu este indiferenta, apelul
constructorilor claselor de baza facdndu-se fn ordinea enumerdrii claselor de baza
(indiferent de ordinea apelurilor explicite a constructorilor claselor de baza).

Pentru exemplul anterior in cazul declararii unui constructor in felul urmator:

CD::CD(): a(1),CC(2), b(3), CB(4)

succesiunea operatiilor va fi
e apelul constructorului CB(4) (evident, presupunem cA el exista)
e apelul constructorului CC(2) (evident, presupunem c el exist4)

© initializarea membrilor a sib cu 1 si3

2.8 Mostenire multipla virtuala

Daca dorim ca si avem o singurd copie a clasei de bazi mostenita multiplu atunci mostenirea

trebuie facuta virtuala ca In exemplul urmator:

class CB

{

public:
int x;

Me

class CDl:virtual public CB // mostenire virtuala

{
hi

class CD2:virtual public CB // mostenire virtuala

{
Me

class CM:public CD1,public CD2
{
Me

pag. 40

Cap. 2 — Mostenirea

#include <stdio.h>

void main()
{
CM mi;

ml.x=0; printf("\n%a 3a $d",ml.x,m1.CD1::
ml.CD1::x=1; printf("\ntd $a %d",ml.x,m1.CD1::
m1.CD2::x=2; printf£("\ntd 3d d",ml.x,m1.CD1::x,m1.CD2::

}

La executie programul tipareste:

NRO
NRO
NBO

ceea ce dovedeste ca exist o singura copie a variabilei membru x care poate fi accesata prin

cele trei modalitati: ml.x, m1.CD1::x, m1.CD2::x..

Daca cumva “pe o ramura” mostenirea nu este virtuala problema ambiguitatii ramane in

actualitate ceea ce impune folosirea operatorului “::” si prefixarea cu numele clasei.

in cazul existen{ei mostenirii virtuale, ordinea de apel a constructorilor claselor de baza este:
© constructorii claselor de baz virtuale.

© constructorii claselor de baza ne-virtuale.

CLocation CLocation CLocation

v ¥
CPoint CPoint
a v L v
CMessage CMessage
y yv 8
CCircle CCircle
on v
CMCircle CMCircle
Mostenire virtuala { Mostenire “normala”

Figura 2.2 - Exemplu de mostenire multipla

pag. 41

Programare Orientata pe Obiecte - Principii

Constructorul claselor de baz se executa:
© o singura data pentru clasele virtuale.
© cate o data pentru fiecare clas de baza nevirtuala din ierarhia respectiva.

Ca si un exemplu de mostenire multipla am prezentat ierarhia de clase din Figura 2.2. In aceasta
cazul din sténga presupune mostenire virtuala a clasei CLocation (cercul si mesajul au aceeasi
pozitie) iar cazul din dreapta presupune mostenire normala (cercul si mesajul au pozitii diferite).
Pe Figura 2.2 am incercat si sugeram instantele existente nu strict relatiile de mostenire dintre

clase.

Rezumat - de tinut minte :-)

° instante incuibarite

° mostenire - declarare si semnificatie

° tip de acces rezultat pentru membrii mosteniti

e necesitatea si rolul aparitiei tipului de acces protected
constructori si destructori in contextul derivarii

apel explicit constructor clasa de baza

conversii valide intre clase derivate

mostenirea multipla - ambiguitatea si operatorul ::
mostenirea multipla virtual - o singura copie a clasei de baza

pag. 42

Cap. 3 - Polimorfismul

Cap. 3 - Polimorfismul

3.1 Supraincarcarea metodelor in contextul mostenirii

Atragem atenfia ca cele prezentate in acest subcapitol nu reprezint& polimorfism ci doar explica
contextul fn care acesta va fi introdus.

S4 consideram exemplul urmator:

#include <stdio.h

class CA

{

public:
met_1(){printf("\nMetoda 1 clasa CA");}
met_2(){printf("\nMetoda 2 clasa CA");}
met_3(){printf("\nMeteda 3 clasa CA") met_1();met_2();}

Ve

class CB:public CA

{

public:
met_1() {printf ("\nMetoda clasa CB");}
met_2(){printf£("\nMetoda 2 clasa CB");}

a

MF

void main()
{

CA a;

CB b;

a.met_1()
a.met_2();

b.met_1()7
b.met_2();

a.met_3();
b.met_3(); // disponibila prin mostenire

pag. 43

Programare Orientata pe Obiecte - Principii

care va produce urmatorul rezultat:

Metoda 1 clasa CA // a.met_i()
Metoda 2 clasa CA // a.met_2()
Metoda 1 clasa CB // b.met_1()
Metoda 2 clasa CB // b.met_2()
Metoda 3 clasa CA // a.met_3()
Metoda 1 clasa CA Lf,

Metoda 2 clasa CA

Metoda 3 clasa CA

Metoda 1 clasa CA

Metoda 2 clasa CA

Remarcém faptul cé in CA avem definite metodele met_1(), met_2() si met_3(). Clasa CB
supraincarcd (inlocuieste) metodele met_1() si met_2() si pastreaz prin mostenire met_30).
Reamintim c4, daca noile metode prezinta semnaturi diferite (alta valoare returnat& sau alt
numir si tip de parametri) clasa CB va avea prin mostenire $i vechile variante ale metodelor iar
compilatorul va decide prin diferentele de semnatura care varianta este cea apelata.

Clasele din exemplul anterior au deci fiecare cAte trei metode:

CA met_1Q) obtinuta prin definire
met_2() obtinuta prin definire
met_3() obtinuta prin definire

CB met_1() obtinuta prin supraincarcare
met_2() obtinuta prin supraincdrcare
met_3() obtinuta prin mostenire

Interpretand rezultatele rularii remarcim ca apelurile cAtre met_1() si met_2() si chiar a.met_3()
au avut loc conform asteptarii. Rezultatul interesant este acela al apelului b.met_3() care, desi
este metoda in CB si este apelata pentru o instanté a clasei CB, apeleaza met_1() si met_2() ale
clasei de baza. Cauza acestei comportari este legarea staticd a metodelor, dupa cum vom explica

in continuare,

Daca se doreste apelarea metodei met_1() din clasa CA pentru obiecte ale clasei CB se poate

folosi *scope access operator” ca in exemplul urmator:

b.CA::met_1();

pag. 44

Cap. 3 - Polimorfismul

care va produce rezultatul:

Metoda 1 clasa C) // b.CA::met_1()

3.2 Polimorfism — generalitati

Pentru abordarea polimorfismului trebuie plecat de la originea greacd a cuvintelor ce il compun:
“poli” inseamna “multe” si “morphos” inseamna “forma”, in domeniul despre care discutam (al
programérii orientate pe obiecte) acest termen descrie proprietatea unei aplicatii de a se
comporta diferit (in mai multe forme) in functie de conditiile din momentul rulirii,

Mai specific, problema apare cAnd se apeleazi 0 metod& (cu nume si parametrii cunoscuti) a
unui obiect dar nu se cunoaste exact tipul obiectului. Chiar dacd problema pare putin ciudata
(putem ayea instantiate obiecte fara a Je cunoaste tipul 2) ea este reala, existand un (singur)
caz in care acest lucru este posibil: acela al pointerilor la obiecte ale unei clase de bazi care pot
tine adresa atat a unor obiecte ale clasei de baza cat si (mai corect spus sau) a oricaror obiecte

din clase derivate din aceasta clasa de baza.

Rezulta de aici o observatie esentiala: in C++ polimorfismul se manifest doar in_contextul

mostenirii. Acest fapt justificd si ordinea in care notiunile de incapsulare, mostenire si

polimorfism au fost introduse.

3.3 Legare statica si dinamica

in acest context numim legare (“binding”) mecanismul prin care compilatorul genereazi cod
pentru apelul unei anumite metode (cum face legatura intre numele metodei si codul acesteia).

Cele doua solutii consacrate pentru aceasta problema sunt:
legarea statica (timpurie — “early binding”)

fn acest caz compilatorul si editorul de legaturi fac corespondenta intre numele metodei si
adresa acesteia din segmentul de cod. Odata fixata aceasti legatura in momentul compilarii /
link-editarii aceasta este definitiva in tot timpul rularii programului. La nivelul cel mai de jos,
legarea statica corespunde apelului direct al unei proceduri (folosind de exemplu o instructiune
de tip CALL direct), Acesta este modul de legare clasic folosit de toate limbajele structurate
(ne-obiectuale) si de limbajele obiectuale pentru metodele ne-virtuale (clasice).

pag. 45

Programare Orientata pe Obiecte - Principii

legarea dinamica (tarzie — “late binding”)

fn acest caz compilatorul si editorul de legaturi fac corespondenta intre numele metodei si

adresa acesteia din segmentul de cod prin intermediul unui tabel de adrese_posibile de salt.

Alegerea adresei efective de apel se face in momentul rularii, in functie de obiectul pentru care
se apeleazi (deci, decizia legarii este intarziata). La nivelul cel mai de jos, legarea dinamica
corespunde apelului indirect al unei proceduri (folosind de exemplu o instructiune de tip CALL
indirect). Acesta este modul de legare folosit de limbajele obiectuale pentru metodele virtuale.

Existé o butada in stiinta calculatoarelor conform cdreia evolutia sa are la bazi doar doua

alternative: utilizarea unei memorii cache si adaugarea unui nivel suplimentar de indirectare.

Polimorfismul fiind un exemplu de solutie avand la baza indirectare prezintA binecunoscutele
avantaje si dezavantaje ale acestor metode:

¢ avantaje: se asigura (introduce) un grad de flexibilitate

« dezavantaj: ca in orice apel indirect, se inrautatesc performantele de viteza

Compromisul avantaje / dezavantaje (flexibilitate / vitezd) se poate stabili de cAtre programator,
prin posibilitatea alegerii, pentru fiecare metoda in parte, a tipului de legare: static’ sau

dinamica.
3.4 Metode virtuale

Dupa cum rezulta din cele de mai inainte trebuie sa existe o posibilitate de a stabili tipul legarii
folosite pentru fiecare metod& in parte. Aceasta are la bazi cuvantul cheie virtual care

prefixeaza definirea unei metode. Avem deci:
° Metode ne-virtuale (normale, clasice) la care legarea este statica. Sunt definite fara a se

folosi cuvantul virtual.
° Metode virtuale la care legarea este dinamica. Se definesc prin prefixarea definirii

metodei cu cuvantul cheie virtual.

Reluam exemplul de Ja inceputul materialului cu singura modificare c&é met_1() se va defini
virtuala:
#include <stdio.h>

class CA

pag. 46

Cap. 3 - Polimorfismul

{

public:

virtual met_1(){printf("\nMetoda 1 clasa CA") ;}

t_2() {printf ("\nlietoda 2 clasa CA") ;}
t_3(){print£("\nMetoda 3 clasa CA") smet_1();met_2();}

a

class CB:public CA

){printf("\nsetoda 1 clasa CB");)}
() {printf ("\nMetoda 2 clasa CB");}

void main()
{

CA a;

CB b;

a.met_1(
a.met_2(

di
de
b.met_1();
b.met_2();

a.met_3();
b.met_3(); // disponibila prin mostenire
}

care va produce urmatorul rezultat

Metoda 1 clasa CA // a.meat_1() legare dinamica
Metoca 2 clasa CA // a.met_2() legare statica
Metod2 1 clasa CB // b-met_1() legare dinamica
Metoda 2 clasa CP // b.met_2() legare statica
Metoda 3 clasa // a.met_3()

Metoda 1 clasa //

Metoda 2 ciasa //

Meteda 3 clasa // b.met_3()

Metoda 1 clasa // !!!! aici apare modificarea
Metoda 2 clasa // !!!! aiei nu apare modificare

Remarcaém faptul ca, in acest caz, metoda met_1() a fost legaté dinamic (in implementarea
metodei met_3() din CA) si, pentru obiectul CB, a fost inlocuité de noua versiune a acesteia.
Astfel, totul se petrece ca si cnd clasa derivata a modificat cod scris intr-o clasi de baza !!!
(la care evident nu are acces, putdnd fi chiar scris de altcineva si disponibil doar sub forma de
biblioteca). Pentru ca acest lucru sa poati avea loc, este strict necesar “ajutorul” clasei de baza,

pag. 47

Programare Orientata pe Obiccte - Principii

care a “consimtit” (prin declararea metodei ca virtuala) la legarea dinamica a acestei metode.

Atragem atenfia asupra faptului ca “atributul” virtual se aplicd individual functiilor si nu se
“propaga in jos” cum se intémpla cu public, protected si private. Deci, in exemplul anterior

met_2() nu este virtuala.

Aceasté comportare deosebiti a metodelor legate dinamic (virtuale) ridicd urmatoarea

problema:

3.5 Cum se implementeaza legarea dinamica (polimorfismul) ?

Pentru a explica acest lucru s& revenim la exemplul cu clasele CA si CB pe care il mai

dezvoltim putin.

class CA
{
public:
virtual met_1()
virtual met_2(){/* .. */}
met_3(){/* Bf}
virtual met_4(){/* .. */}
met_5() {met_1();met_2();mel_3() ;met_4();}
// variabile membru CA
Me

LA® se Ph

class CB: public CA

{
public:
met_1(){/* .. */}
met_3(){/* .. */)}
met_4(){/* .. */}
// variabile membru CB
hi

CA a,al,a2; CB b,bl; // citeva instantieri
Pentru fiecare clas compilatorul construieste o tabela cu metodele virtuale ale clasei respective

tabeld numité VMT (Virtual Method Table) si include in fiecare instanta (obiect) a clasei un
pointer VMT_ptr spre tabela VMT corespunzatoare clasei.

Accentuam faptul ca pentru o metoda ne-virtuala legarea este neconditionat statica iar pentru o
metoda virtualé legarea este neconditionat dinamica (atat la apel direct prin obiect sau prin

pointer la obiect cat si la apel din cadrul altor metode).

pag. 48

Cap. 3 - Polimorfismul

VMT_CA VMT_CB
/ 0 [GA metl() |g | CBzmet_t() VMT_ptr instanta b
instanta tenable / Vy CAsmet2Q) | 1 | CAsmet2Q) variabile
faa
membre 2] CAzmet 4(Q) | 2 CBemet_4() membru
= CA
variabile
membru
cB
instanta al sha ia
b ,
guembry VMT_ptr | instanta b1
ee variabile
+-— membru
VMT ptr CA
instanta a2 | Y@riabile variabile
, membru membru
CA cB

Figura 3.1 Tabelele VMT

Avand in vedere schema de mai sus putem intelege modul cum se implementeazi, de cétre

compilator, metoda met_S( ):

// apel metoda prin VMT
// apel metoda prin vM?T
// apel metoda

// apel metoda prin VMT

legare dinamica)
legare dinamica)
legare statica)
legare dinamica)

(
(
(
(

Pentru a.met_5() acesta este echivalent cu:

metoda legata prin VMT
metoda legata prin VMT
metoda legata static

metoda legata prin VMT

// 2pel metoda legata prin VMT inlocuita
\3 // apel metoda legata prin VMT neinlocuita
; // apel metoda legata static

de // apel metoda legata prin VMT inlocuita

Mai detaliat avem: pentru apelul VMT|i] se acceseaza, prin intermediul lui this, zona de date a
obiectului si se obtine VMT_ptr. Prin intermediul lui VMT_ptr se acceseazi tabela VMT din

care, prin indexare, se determina adresa metodei VMT[i].

pag. 49

Programare Orientata pe Obiecte - Principii

nu la nivel de instanfa.

Accentuam faptul ca polimorfismul se manifesta la nivel de clas

De asemenea, acum putem explica dimensiunea unui obiect avand metode virtuale: indiferent

de numarul metodelor virtuale ale clasei respective (evident, cel putin una).

Dimensiunea unui obiect cu metode virtuale
este egala cu dimensiunea variabilelor membru (adaugate sau mostenite)
plus dimensiunea unui pointer (VM1T_ ptr)

3.6 Ce poate fi virtual si ce nu

Desi atributul virtual prefixeazi declaratii de metode, nu orice metoda poate fi declaratd
virtuala. Astfel:

e Constructorii nu pot fi virtuali. Oricum, prin mecanismul apelului constructorului clasei
derivate ulterior apelului constructorului clasei de baz, se poate modifica
comportamentul obiectului rezultat (anuland eventuale actiuni nedorite ale
constructorului clasei de baz&). Desi constructorii virtuali ar fi utili pentru evitarea

lipsa constructorilor virtuali poate fi suplinita

apelului constructorului clasei de baz,
prin apelarea unei metode virtuale in corpul constructorului nevirtual.

e Destructorii pot fi virtuali. Neavand tip returnat si nici parametrii este evident cd el este
unic pentru fiecare clasa. Aceasta facilitate este salvatoare in cazul distrugerii uniforme
de masive eterogene.

e Metodele statice nu pot fi virtuale. Acest lucru este evident, avand in vedere faptul ca
ele nu sunt metode propriu-zise, deci nu au pointerul this si deci nu au acces la VMT.

e Metodele friend pot fi virtuale dar nu si functiile friend (care, ca gi metodele statice, nu
apartin unui obiect).

e Metodele operator pot fi virtuale dar nu si operatorii redetini{i prin functii friend.

3.7 Un exemplu distractiv si nu numai....

Sa consideram urmatorul exemplu care, chiar daca aparent este doar distractiv, evidentiazd

interactiunea metodelor virtuale cu celelalte tipuri de metode intr-o ierarhie de clase.

#include <stdio.h>

class fairy _tale

{
public:
void act0(){print£("0 Inceput pevest: criginila\n"); aetl();}
virtual void actl{){printf£("1 Printesa intilnecte brovcoiul\n"); act2()};}
void act2(){printf("2 Printesa saruta bros coiul\a"); t3()7}

pag. 50

Cap. 3 - Polimorfismul

virtual void act3() {prin’/("3 Broscoinl se transforma in Print\n");

virtual v act{(){print£("4 Ci aa trait multi ani fericiti\n");
void act5(){print?("S Sfirsit fericit\n");

iz

class unhapry_tal-: public feiry tele

{

public:
void actOQ (print f("0 Tneeput poveste modificata\n");
void act2()(printf£("2 Frintesa atinge broscoiul\n");
oid 3() (printf ("3 Pvoscoiul ramine broscoi\n") ;
void {Viprint£("4 Printesa fuge ingrozita\n");
void acl (){printe(" Sfirsit nu prea fericit\n");

te

void main()
{
fairy_tale *tale;

printf ("FAIRY TALE\n");
tale = new fai tale;
tale->actO();
delete tale;

printf ("UNHAPPY TALE\n");
tale = new unhappy tale;
tale->actO();

delete tale;

care va produce urmatorul rezultat

FAIRY TALE
Incenut poveste originala
Printess intilneste broscoiul
Printesa saruta broscoiul
Broscoiul se transforma in Print
Si au trait multi ani fericiti
Sfirsit fericit
INHAPPY TALE
Inceput poveste originala
Printesa intilneste brescoiul
Printesa saruta broscoiul
Broscoiul ramine broscoi
Printesa fuge inqrozite
Sfirsit nu prea fericit

AEWNPOCURWNHHO

Analizand rezultatul remarcam urmatoarele:

e functiile virtuale nu trebuie neaparat sa fi Inlocuite. Clasa unhappy_tale nu furnizeazd

inlocuitor pentru act1() dar aceasta este totusi disponibila prin mostenire.

act4();}
act5();}
}

actl();)}
act3();}
act4();}
act5();}

}

pag. 51

Programare Orientata pe Obiecte - Principii

VMT VMT
fairy_tale unhappy_tale
act! () fairy tale:-actl() actl() a
act3() fairy tale::act3() aei3( )

actd() fairy_tale::act4( ) acl4()

Figura 3.2 Tabelele VMT pentru “povesti”

functiile virtuale pot apela functii ne-virtuale. actl() virtuald apeleaza act2() ne-
virtuala. Desi obiectul apelat este o instanté unhappy_tale se apeleaza tot act2() de la
happy_tale. Cauza: legarea statica pentru act2().

functiile ne-virtuale pot apela functii virtuale. act2() ne-virtuala apeleaza act3()
virtuala. act3() efectiv apelata depinde de obiectul pentru care se efectueazi apelul.
Cauza: legarea dinamica pentru act3().

funcfiile virtuale pot apela alte funfii virtuale. act3() virtualé apeleaza act4() virtuala.
act4() efectiv apelata depinde de obiectul pentru care se cfectueazd apelul. Cauza:
legarea dinamica pentru act4().

functiile virtuale pot apela functii supraincarcate. act4() virtuala apeleaza act5() care
este supraincarcata. act5() efectiv apelaté depinde de obiectul in care se efectueazi
(implementeaza) apelul. Cauza: legarea staticd diferita in cele doua implementari ale
lui act4(). Atentie, nu avem legare dinamica pentru act5().

functiile supraincarcate pot fi apelate direct. act0() supraincarcala este apelata direct.
in acest caz avem legare statica a metodei dependenta de tipul pointerului implicat

dupa cum rezulta din exemplul urmator de apel:

unhappy_tale *tale;

printf ("UNHAPPY TALE\n") ;
tale = new unhappy tale;
tale->act0();
delete tale;

care tipareste:

UNHAPPY TALE

Inceput poveste modificata // aici apare modificarea
Printesa intilneste broscoiul

Printesa saruta broscoiul

Broscoiul ramine broscoi

Printesa fuge ingrozita

Sfirsit nu prea fericit

OBRWNHO

pag. 52

Cap. 3 - Polimorfismul

Pentru intelegerea acestor situatii recomandam vizualizarea tabelelor metodelor virtuale pentru
clasele fairy_tale si unhappy_tale (prezentata in Figura 3.2).

Avand aceastd viziune a tabelclor VMT si bazandu-ne pe legarea dinamica a metodelor virtuale
sa se explice (infeleag&) comportamentul claselor fairy_tale si unhappy_tale prezentat anterior.

Ca si regula putem spune ca, Ja apelul unei metode a unui obiect prin intermediul unui pointer
spre obiect, avem:
© daca legarea metodei este statied conteaza tipul pointerului (si nu tipul obiectului a
carui adresa o retine la acel moment pointerul).
© daca legarea metodei este dinamica conteaza tipul obiectului a carui adresa 0 retine la
ace] moment pointerul (si nu tipul pointerului - care ar putea fi de tipul obiectului sau

de tipul unei clase de baza).

3.8 Metode virtuale si ierarhii de clase

fn cele ce urmeazi ne propunem sa analizim modul in care se “propaga” caracterul de

virtualitate al metodelor intr-o icrarhie de clase. Pentru aceasta consideram urmatorul exemplu:

#include <stdio.h>

class CA
{
ual oid met_1 (void) {PP 20x E73
virtual void met_2(void) {/*...*/}
virtual void met_2(void) {/*...*/}
virtual void met_4(void) {/*...*/}

hr

class CB: public CA
{
public:
virtual oid met_l(void) {(/*...*/}
oid met_2(void) {/*...*/}
void met_4 (int) CP ccm FL}
Vi

class CC: public CP
{
public:
virtual void met_4(int) (Ps 6 FV}

pag. 53

Programare Orientata pe Obiecte - Principii

MF

si un program principal care sa instantieze asemenea clase:

void main()

{

CA a,*pa;CB b, *pb;CC
a.met_4(); //
pa=new CA;
pa->met_4(); //
delete pa;

// bemet_4(); //
pb=new CB;

// pbh->met_4(): //
delete pb;

pa=new CB;
pa->met_4(); //
delete pa;

}

lord

corect

corect

incorect, me t

incorect, met_

corecta !!!!!1

Tabelele VMT pentru clasele in discutie arata astfel:

i(void) nu

void) nu

folcseste

taibila

vizibila

VMT CA

VMT CB

| VMr'CC

| CA: :met_I(void)

CB::met_I(void) —

:: met_l (void)

CA::met_2(void)

CB::met_2(void)

smet_2(void)

CA::met_3(void)

CA::met_3(void)

met_3(void)

CA::met_4(void)

CA::met_4(void)

met 4(void)

Din exemplul prezentat putem trage urmatoarele coneluzii:

e redefinirea unei metode virtuale tot cu una avand atributul virtual inlocuieste in VMT cea

veche cu cea noua. Exemplul: met_1.

e redefinirea unei metode virtuale cu una neavand atributul virtual se comporta la fel ca in
cazul anterior. Exemplul: met_2. Deci, odat& impus caracterul virtual, acesta "nu se

mai pierde". Se recomanda totusi rescrierea cuvantului virtual si in clasele derivate

(desi este redundant) pentru o intelegere mai rapida la citirea codului.

e neredefinirea unei metode virtuale pastreazi in VMI ultima varianta disponibila.

Exemplu: met_3.

e redefinirea unei metode virtuale cu una avand acelasi nume dar parametrii diferif{i nu o

pag. 54

Veét-ul lui CB

Cap. 3 - Polimorfismul

pune pe cca noua in VM (deci aceasta nu este virtuala). Exemplu: met_4(int).

¢ met_4(int) poate fi redefinita ulterior ca find virtuala. Exemplu: met_4(int) in CC.

° redefinirea unci metode virtuale cu una avand acelasi nume dar parametrii diferiti ascunde
pe cea yeche (din punct de vedere al domeniului de vizibilitate si nu din acela al
VMT). Exemplu: liniile cronate din programul prezentat. Totusi, metoda veche inc&
exist4 in VMT si poate fi accesata prin pointer la clasa de bazi (unde metoda era

vizibila). Exemplu: ultimul apel din main.

Mai amintim faptul cdi o metoda (deci si o metoda virtualé) nu poate fi redefiniti cu aceiasi

parametrii dar avand tip returnat diferit.

Daca dorim sa evitim mecanismul de polimorfism avem intotdeauna la dispozitie operatorul
scope access resolution” prin care putem impune metoda apelata (fortand lepare static),

3.9 Metode pure si clase abstracte

Uneori se pune problema sa proicctim clase cu un caracter de generalitate, fara a ne referi la o
particularizare a acestora. De exemplu, putem avea in vedere proiectarea unei ierarhii de clase
destinata gestionarii de obiecte cum ar fi cere, patrat, triunghi, etc. Toate acestea au in comun
faptul c& au o pozitie, o dimensiune, o culoate si difera doar prin modul in care se afiseaza si se
sterg. Este de dorit sa “prindem” intr-o clasa de bazi comportamentul comun si, prin derivare si

polimorfism. sa particularizim comportamentul pentru clasele derivate specializate. Clasa de

baza ar putea fi scrisa astfel

class CFigura

{
int x,y,cul, dim; // poz x, y, culoarea si dimens

void Muta(int dx,int dy);
virtual void Afisesza(){}; // exista pentru a putea
virtual void Sterg2(){}; // £i apelate de Muta
// alte metode, constructori si destructori
Mi

Comportamentul comun al tuturor claselor derivate din CFigura poate fi “prins” in metode ale

acestei clase de bazi. Indiferent de ce tip de figura este vorba, mutarea se face la fel

void CFigura::Muta(int dx,int dy)

{
Sterge(); // stergere
xt =dy; // wutare propriu-zisa

pag. 55

Programare Orientata pe Obiecte - Principii

Afiseaza(); // afisare

Prin derivari ale clasei CFigura se pot defini figuri diferite care isi particularizeaza afigarea si
prin

=

Stergerea dar care p&streazi metoda de mutare din clasa de baza (modificati

polimorfism).

Solutia are dezavantajul ca permite instantierea de obiecte din clasa CFigura care nu sunt
complet definite. Pentru a evita instantierea de obiecte din clase “neterminate” s-a introdus

notiunea de metoda pura pentru metodele virtuale.

O metoda virtuala se defineste ca fiind pura daca la declarare corpul ei se face egal cu 0 ca in

exemplul urmator:

class CFigura

{
int x,y,cul,dim; // poz x, v¥, CUuloarsa si dimens
void Muta(int dx,int dy);
virtual void Afiseaza() // aici se defineste ca pura
virtual void Sterge()=0; // aici se defineste ca pura
// alte metode, constructori si destructori
Me

metoda Muta ramanand neschimbata.

O metoda definita pura nu se implementeaza in clasa respectiva. Rolul ei este doar acela de a fi

inlocuita intr-o clasa derivata. Alte metode ale clasei ins& 0 pot folosi ca si cum ar exista deja

implementata.

O clasi cu cel putin o metod& pura se numeste clasi abstract&. Clasele abstracte sunt

importante deoarece compilatorul nu permite instanticrea unci clase abstracte.

Rolul unei clase abstracte este de a fi mostenita intr-o clasd derivata care si inlocuiasca toate
ierarhiei de clase.

metodele "pure". in general asemenea clase se gasesc inspre “radacina

3.10 Tratarea uniforma a masivelor eterogene

Una din cele mai importante aplicatii ale polimorfismului este tratarea uniforma a unor
masive (tablouri, liste, etc...) eterogene in sensul cd sunt constituite din obiecte de tipuri

diferite. Solutia se bazeaza pe posibilitatea unui pointer spre o clasa de a retine adresa unor

pag. 56

Cap. 3 - Polimorfismul

instante ale acelei clase dar si a unor instante ale unor clase derivate. in acest context

polimorfismul permite o tratare uniforma a unor asemenea masive prin legarea dinamica pe care

0 presupune.

In continuare exemplificém aceasta posibilitate:

#include <stdio.h
class CA
{
public: int x;
CA(int x0) {x=x0;)}
virtual ~CA(){printf("\nDestr CA (%d)" x)7}
virtual met() {printf ("\nietoda CA (%d) x) di}
Me
class CB:public CA
{
public: int y;
CB{int x,int +0) :CA(x0) {y=y0;}
sont) (Be atf("\ndestr CB (3d,%d)",x,y);)
et () {printf ("\nMetoda CB (%d,%d) ",%,y)i}
Mi
void main()
{
CA* thlEt [6];
tbhlEt[9] = new CA(1); // construim un masiv eterogen
tbl&t(1] = new CA(2);
tblEt(2) = new CB(3,4);
tblEt[3] = new CA(S);
tolEt [4] new CB(6,7);
thlEt(5] = new CB(8,9);
for(int k=0;k<6;k+?) // aici tratareae uniforma,
tblEt[k]->met(); // comportamentul e polimorf

O7;k<67 k++)
kl; //

for(k
delete tblEt[

distrugere polimorfa

Prezentém in ccle ce urmeaza rezullatele rularii programului in cazul tn care metoda met() si

destructorul nu sunt virtuale (deci nu ne bazim pe polimorfism) si in cazul in care acestea sunt

virtuale (deci ne bazim pe polimorfism).

pag. 57

Programare Orientaté pe Obiecte - Principii

met() si ~CA() met() gi ~CA()
nevirtuale virtuale

(ca in exempta)
Metoda CA (1) // [0 (1) // [0
Metoda CA (2) // {1 Metcda CA (2) // i
Metoda CA (3) f/f [2 Meteda CB (3,4) // [2
Metoda CA (5) #7 [8 Netoda CA (5) // (3
Metoda CA (6) // [4 (€ // [4
Netoda CA (8) “Ff 15 (3 // (5
Destr CA (1) // [0 ) // {0
Destr CA (2) Le (dh 3 ) // (2
Destr CA (3) // [2 Bestr CB (3,4) // (2
Destr CA (5) // (3 Destr CA (3) // (2
Destr CA (6) // [4 Destr CA (5) // (3
Destr CA (8) // [5 Destr CB (6,7) // {4
Bestr CA (&) // [4
Destr CB (8,9) ff (5
Destr CA (@) // [5)

Din analiza rezultatelor observam ca:

* in lipsa polimorfismului toate obiectele se considera a fi de tipul pointerului (CA),
functionarea find deci “defectuoasa”. O posibila solulie ar putea fi mentinerea fn
masiv (tablou) si a unui indicator al tipului obiectului gi apelul explicit al metodelor
clasei derivate cand este cazul (folosind conversia explicité de tip pentru pointerul
respectiv —“cast”). Consideram solutia neeleganta si greoaie, tratarca nefiind uniforma.

in prezenta polimorfismului se apeleazi metoda met() corespunzatoare instantei efective
(CA sau CB). in acest fel s-a reusit o tratare uniforma a masivului (din punct de vedere
al sursei programului). La baza solutiei st& legarea dinamica a metodelor virtuale (deci

polimorfismul).

Un caz deosebit este acela al destructorului virtual cand, tot prin polimorfism (legare dinamica),
se apeleazé destructorul corespunzator instantei respective, distrugerea obiectelor facdndu-se
corect. Remarcam faptul ci, dupa executia destructorului clasei derivate (legat dinamic), se
apeleazé gi destructorul clasei de bazi. Aceasta dovedeste cA, in “interiorul” (la_finalul)

destructorului clasei derivate apelul destructorului clasei de baza se face prin legare statica.

Dup& cum am amintit si la prezentarea polimorfismului, solutia prezentata de tratare uniform’ a
masivelor eterogene este valabila doar in cazul in care masivul retine pointeri catre o clasi de

baza iar “eterogenitatea” masivului apare in aceea ca instantele mentinute gestionate) sunt

pag. 58

Can. 3 - Polimorfismul

instante ale clasei de bazd sau ale unor clase derivate (de oricate ori) din aceasta (pentru ca
polimorfismul -- care a fost cheia solutici - s& functioneze). Evident, tratarea uniforma se refera
doar la apelul unor metode existente in clasa de baza (fie ele si pure), eventual inlocuite in

clasele derivate.

Rezumat - de tinut minte :-)

° supraincarcarea metodelor in contextul mostenirii — nu e inc& polimorfism

° legarea statica si legarea dinamica a metodelor

° cuvant cheie virtual -> legare dinamici => polimorfism

° polimorfism — doar in contextul mostenirii

° implementarea legirii dinamice (polimorfismului) - tabelele VMT

° metode virtuale in ierarhii de clase

° metode pure (inca neimplementate) si clase abstracte (cu functii pure, neinstantiabile)

° masive cterogene — tratare uniforma prin polimorfism

pag. 59

Programare Orientata pe Obiecte - Principii

Cap.4 - Redefinirea operatorilor

4.1 Apel prin valoare, adresa (pointer) si referinta

pentru tipuri de baza

Pentru inceput, ne propunem sa analizim modul de transmitere a parametrilor spre functii

(incluzand in aceasti discutie si metode) pentru cazul transmiterii de date avand tipuri

predefinite.

Dupa cum se cunoaste din limbajul C parametrii se pot transmite prin valoare si prin adres
(pointer). Situatia este descris4 in tabelul urmator:

| “pei pain alone
sintaxa declarare
parametru formal

functie(tip Nume,...)

apel prin adres
(pointer)

functie(Tip *Nume,...

apel prin referinta

functie(Tip &Nume,...)

sintaxa apel functie(variabila,....)

functie(&variabila,....)

functie(variabila,...)

utilizare variabila

_ Nume *Nume Nume
in cadrul fun
ce se transmite | valoarea variabilei adresa variabilei adresa variabilei
efectiv pe stiva parametru parametru parametru
NU DA DA

Remarcdm urmiatoarele:

¢ apelul prin valoare este comod din punct de vedere al sintaxei (nu e nevoie de * si &) dar

nu permite modificarea in cadrul functiei a variabilei trimise parametru (se modifica de

fapt doar copia de pe stiva a acesteia).
e apelul prin adres4 permite modificarea variabilei propriu-zise (avand la dispozitie adresa

ei) dar sintaxa este ingreunata.

pag. 60 -

__Cap. 4 ~ Redefinirea Operatorilor

Pentru rezolvarea acestei “

dileme” s-a introdus in C++ tipul referinti care combina avantajele

celor doua situaii anterioare. Definirea, apelul si utilizarea unui parametru referinfa sunt

descrise in tabelul anterior.

In continuare prezentim un exemplu de apel al unei funcfii avand 3 parametri intregi transmisi

prin cele 3 metode descrise anterior.
#include <stdio.h>

int functie(int y,int *y,int &z) // cele 3 posibilitati

{

int. t;
printf£("\nAm apelat %d $d Bd Gar ty, 2)5
xtH; (Fy) bey cet;
t=xt*y
printf ("\nRevin bd 8d 8d",x,*y,z);
return t;

}

void main()
{

int a=2,b=5,c=8, r=11;

printf ("\nApelez Sd td $d %d",a,b,c,xr);
r=functir (a, &b,c);
printf£("\nAm revenit %d %d %d 3d",a,b,c,r);

Programul va tipari urmatoarele:

Apelez 25811
Am apelat 25?
Revin 3.69
Am revenit 2 6 9 18

Atragem inci o dala atenfia asupra sintaxei declararii, apelului si utilizarii variabilelor

parametru si asupra valorii variabilelor locale functiei main dupa intoarcerea din functie.

4.2 Functii friend, metode friend si clase friend (prietene)

Necesitatea mecanismului de “friend” provine de la imposibilitatea unei metode de a fi
membr4 in mai multe clase. S-a impus existenta unei solutii ca o functie ne-membra si poata
accesa membrii ne-publici ai unei clase. Acest lucru se realizeaza prin prefixarea declaratiei cu

pag. 6]

Programare Orientata pe Obiecte - Principii

cuvantul cheie “friend”.

Chiar daca sunt declarate in cadrul clasei, ele nu sunt metode, deci nu li se transmite pointerul
ascuns this. Esential este ca ele au acces la membrii ne-publici ai clasei cu care sunt prietene.
Evident, functiile friend incalcé (controlat) principiul incapsulirii. Nefiind membre ale
clasei, accesibilitatea lor nu este afectati de sectiunea public, protected sau private in care sunt
declarate (fapt aparent surprinzitor). Inc&lcarea controlataé a incapsularii se refera la faptul ca
functiile friend trebuie declarate in cadrul clasei, deci de catre proiectantul clasei. Clasa isi
stabileste functiile care fi sunt prietene si nu functiile clasele cu care s fie prietene, adicd nu

putem fi prieteni cu o clasa decat daca proiectantul acesteia consimte acest lucru.

in exemplul urmator prezentim modul de declarare a unei funetii MULTIPLY de inmultire a

unui vector cu o matrice, functie declarata ca si functie friend:

class CMatrice;

class CVector

{

int v(10];
friend CVector Multiply(CMatrice &m,CVector’ v);
//

MF

class CMatrice

{
int m[{10] [10];
friend CVector Multiply(CMatrice &m,CVectoré v);
//

hi

CVector Multiply(CMatrice &m,CVectoré v)

{
CVector rez;

for(int i=0;i<10;i++)

{
rez.v[i]=0; // membrul v privat
for(int j=0;3<10;j++)

rez.v[i]t=m.m[iJ[j]*v.v(j]; // membrii v si m privati

}
return rez;

}

fn lipsa mecanismului functiilor friend, inmultirea ar fi putut fi facuta doar prin:

pag. 62

Cap. 4 — Redefinirea Operatorilor

* punerea variabilelor membru ale matricii si vectorului publice, ceea ce ar fi impus
renunjarea completa la avantajele incapsularii (inacceptabil).

e dotarea claselor cu metode de acces la membri (citire si scriere a lor) fapt care ar fi
compromis elegan{a si performantele de vitezd (orice acces la o variabila insemnand

apeluri suplimentare de metode).

Cum ambele solutii prezinta dezavantaje majore mecanismul functiilor friend este justificat.
Remarcam si faptul ca functiile friend au acces la membrii ne-publici ai tuturor obiectelor de
tipul respectiv, nu doar la variabilele primite parametru (cum, eronat, s-ar putea trage concluzia
din exemplul anterior). De cele mai multe ori insd, se folosesc parametri pentru a suplini lipsa

lui this.

Nu numai functiile ci si metodele pot fi declarate friend, ca in cele ce urmeazi:

class CX
{
V4
void o_metoda(/*.
Ve
class CY
{
// .... clasa CY propriu-zisa
friend void © o_metoda(/*....*/);
hi

in acest exemplu metoda clasei CX devine prietené cu clasa CY. fn plus, pentru a evita
declararea individuala a ficcdrei metode a unei clase ca fiind prietend cu o alta clas se poate

declara in acest scop intreaga clas ca fiind prietena cu alt clas& ca in exemplul urmator:

class CX

{
// .... Clasa CX propriu-zisa

class CY

{
preopriu-zisa

// .... Clasa
friend class CX;
Me

efectul fiind acela ci toate metodele lui CX devin prietene cu CY. Relatia de friend intre clase

nu este nici comutativ i si nici tranzitiva (fra declararea explicit a tuturor relatiilor de friend).

pag. 63

Programare Orientata pe Obiecte - Principii

4.3 Redefinirea operatorilor - introducere

Pentru tipurile predefinite semnificatia operatorilor este binecunoscuta, de exemplu este
evident ce inseamna + pentru intregi. O caracteristic& important a programarii obiectuale este
aceea ca permite programatorului sa isi defineasca propriile clase, in fapt propriile tipuri de
date, fn sensul comportarii cat mai naturale a noilor tipuri introduse, devine evidenta utilitatea

definirii si pentru aceste noi tipuri a unor operatori.

Termenul original este acela de “Operators Overloading”, adici supraincdrcarea
operatorilor, pentru a indica dreptul programatorului de a atribui sensuri noi operatorilor
existenti. Din aceasté cauza se foloseste de multe ori termenul de redefinire a operatorilor

pentru a indica acest fapt.

fn limbajele de programare nu exista, de exemplu, un tip numar complex, cu toate ca, in
matematicd, acest tip este bine definit. Putem declara o clasi complex pentru care s&4 avem

definita adunarea astfel:

class complex

{
double real, imag;
complex Adunare (complex &arg); // prima varianta
complex operator+(complex éarg); // a doua varianta
Mi

in care ambele metode aduna separat partea reala si cea imaginara. cu posibilele utilizai

complex a,b,c;

c=a.Adunare(b); // foarte inceomod
c=a.operatort(b); // ceva mai clar, dar tot incomod
c=atb; // in sfirsit natural

fn exemplul anterior remarcam forma incomoda cu apel explicit de metoda (mai ales in cazul
unor expresii complicate, cu adunari multiple) si ultima forma, in sfarsit naturald (ultimele douad
forme de scriere sunt echivalente). A doua forma indic& faptul ci avem de a face de fapt cu

functii (metode) cu nume de operator (“operator" este cuvant cheie al limbajului).

Un exemplu mai complet este prezentat in continuare:

pag. 64

Cap. 4 ~ Redefinirea Operatorilor

#include <stdio.h>

class compl«:x

{
pubiic:
double
doubl« imag;
complex (dceuble re,double im) {real=re; imag=im; }
complex (} {real=-0; imag=0;};
comp] operatort+ (complex &arg)
{
complex rez;
rez.real-realtarg.real; // rez. real=this->real+arg. real
rez.imag-imog+targ.imag; // rez.imag: this->imagtarg.imag
return rez;
}
yi

void main()
{
complex cl(1,2),¢2(5,7),¢3;

printf("\nel ( $lf , #1£ )",cl.real,cl.imag) ;
print£("\nc? ( (1f , *1f )",c2.real,c2.imag) ;
c3=cl+e2;

printf("\nc3 ( tlf , *1f )",c3.real,c3.imag);
}

care produce urmatorul rezultat:

cl ( 1.000000 , 2.000000 )

c2 ( 5.090000 , 7.909000 )

c3 ( 6.000090 , 9.000000 )
in mod evident adunarea este acum mult mai natural astfel incat programul poate fi inteles
mult mai usor si ne putem concentra asupra prelucrarii propriu-zise. Pentru o implementare
reala, utilé clasa complex trebuie completaté cu o multime de alti operatori. Modul cum se

intoarce efectiv rezultatul va fi discutat mai tarziu.

4.4 Redefinirea operatorilor ca si metode sau ca si functii friend

Desi a fost trecut anterior cu vederea, redefinirea operatorilor se poate face atat prin metode cat
si prin functii friend. Apelul functiilor si metodelor operator se face de ctre compilator

conform urmatoarelor echivala


Programare Orientata pe Obiecte - Principii

. . apel pentru operator ‘| apel pentru operator
aritate | sintaxa | , : % z i gels
implementat prin metoda implementat prin functie friend
binar x@y X . operator @ (y )
unar @®x x . operator @( )

in care am notat generic prin @ orice operator binar (cu doi operanzi) si prin © orice

unar (cu un singur operand).

Prezentém un exemplu in care au fost redefiniti 4 operatori acoperind toate situatiile

functie friend si unar - binar:

#include <stdio.h>

class CX
{
public:
int x;
void operatort+(); // unar, metoda
void operator+(int arg?); // binary, metoda
friend void operator--(CXéargl); // unar, friend
friend void operator-(CX&argl,int arg?); // binar, friend
Ve
void CX::operator++() 7} // this->x++
void CX::operator+(int arg2) sarg2;} // this->x+t=arg2
void operator-- (CXéarg1) {argl.x--;) // nu exista
void operator-(CXéargl,int arg2) {argl.x-=arg2;} // this
void main()
{
CX a;
a.x=5; printf ("\na. d",a.x); // aici = este de la intregi

td",a.x) 7
Sd",a.n
d",a.x);
bd ais he) F

att; printf("\na.
at2; printf£("\na.
a--; printf ("\na.
a-3; printf ("\na.x=

he

care tipareste:

// la inceput

// dupa operatorul ++
// dupa operatorul +
// dupa operatorul --
// dupa operatorul -

[]
BUDA

ooo ww
x MX KK

operator

metoda -

pag. 66

Cap. 4 — Redefinirea Operatorilor

Deoarece redefinirea operatorilor are de fapt la baz functii (metode) cu nume de operator
putem beneficia de suprainearcarea numelui functiilor, adic& putem defini diferit acelasi
operator pentru argumente (operanzi) de tipuri diferite (putem, pentru o clasa, defini
adunarea intr-un fel pentru al doilea operand intreg gi in alt fel pentru al doilea operand de tip
long). Aceast& posibilitate este valabila, evident, doar pentru operatorii binari, unde avem
libertatea aleyerii tipului celui de al doilea operand, primul operand este impus a fi de tipul

clasei pentru care redefinim operatorul.

Cu toate ca functiile operator imbunatatesc versatilitatea programelor, trebuie respectate

anumite restrictii:
° nu se pot defini operatori noi (de aceea se si numeste redefinirea operatorilor).

* se pot redefini toti operatorii exisienti in C++ cu exceptia urmatorilor:

z C++ selectie directa a membrilor

* Cit dereferentiere pointeri la metode
ag Ctt “scope access” / rezolutie

2: Operatorul conditional

nu se poate schimba aritatea si prioritatea (precedenta) operatorilor. Operatorii unari
trebuie s& raman& unari, operatorii binari trebuie s& ram4na binari iar precedenta
operatorilor nu poate fi schimbat’, (adica, de exemplu, * ramane prioritar lui +).

comportamentul operatorilor pentru tipurile predefinite nu poate fi schimbat, adica
primul operand operatorului redefinit trebuie sa fie o clas& (cea in discutie).

redefinirea unor opcratori nu redefineste si operatorii aparent combinati, adicd

redefinirea lui + si a lui = nu redefineste si +=, Daca i] dorim si pe cel combinat,

trebuie sa il redefinim explicit.
¢ operatorii =, [ ] si -> trebuie si fie functii membru ne-statice.

ii este acela de a asigura ca:

Scopul acestor restrict
¢ compilatorul de C++ nu trebuie sa tsi modifice regulile sintactice.

° semnificatia operatorilor predefiniti nu poate fi schimbata.

4.5 Distingerea intre formele prefixate si postfixate ale operatorilor

O problema aparuta in primele versiuni de C++ era imposibilitatea distingerii intre formele
prefixate gi postfixate ale operatorilor unari. Problema s-a rezolvat in sensul c4, prin conventie:

¢ varianta prefixata se redefineste “normal” (majoritatea operatorilor unari au doar forma

prefixata).

“pag. 67

Programare Orientata pe Obiecte - Principii

* varianta postfixata se redefineste avand suplimentar un parametru de tip intreg.

Parametrul suplimentar din forma postfixata nu este efectiv folosit, scopul sau este doar de a
asigura o diferenta de semnatura. Prezentim un exemplu in care operatorii de preincrementare
si postincrementare se redefines ca si metode. Daca s-ar defini ca si functii friend ar exista si
un prim parametru referinté la un obiect al clasei respective (din acest motiv s-a folosit
exprimarea de “suplimentar un parametru” pentru forma postfixata). fn exemplul prezentat
operatorul intoarce valoarea dintr-un tablou gi incrementeaza indicele in acesta inainte sau dupa

acces (pre/post incrementare).

#include <stdio.h>

class CX
{
int tab[10];
public:
int poz;
int operator++(int); // varianta postfixata
int operator++(); // varianta prefixata
CX()F
Me
CX::CX()
{
for(int 1=0;1<10;i++) // initializam tabloul
tab[i]=10*i;
poz=3; // initial in pozitia 3
}
int CX::operator++() // vavianta prefixat
{
pozt+;pozt=10; // intii incrementamn
return (tab[poz]); // apoi luam valoares
}
int CX::operator++(int) // varianta postfixatas
{
int t=poz; // pastrain pozitia vech
poz++;poz%=10; // incrementam
return (tab[t]); // lua valoarea din pozitia veche
}
void main()
{
CX a;

int pold,v,pnew;

pag. 68

__ Cap. 4 ~ Redefinirea Operatorilor

pold=s.poz; fat+; pews
printf("\npoz_old=%d valu

poz;
=td poz_new=%d",pold,v, pnew);

peld=a.por; vetta; pnew=a.poz;
printf ("\npoz_old=Sd value=td poz_new=%d", pold,v, pnew) ;

printf ("\npoz_| value="d poz_OLD=%d",a.poz,at+,a -~poz);
printf ( "\npoz_ NLit-%d value=4d poz_OLD=%d",a.poz,++a,a.poz);

Din rezultatele pe care le tipareste programul:

poz_old-3 value=30 poz new=4
poz_old=4 value=59 poz new=5
poz_NEW=6 value=50 poz OLD=5
poz_MEW=7 valuc=70 poz_OLD=¢

remarcam:

» functionarea corecta a celor doua forme pre si post fixate.

* analizand ultimele doua linii tiparite remarcim si ordinea fn care se evalueazi
argumentele in cazul unui apel de functie: dinspre ultimul inspre primul, adic& in
ordinea punerii lor pe stiva (conform conventiei de apel C). Fiind un aspect delicat, nu
se recomanda s& ne baviim pe el, si este preferabila solutia cu apel anterior si variabile

temporare.

4.6 Operatorul de atribuire (=)

Un operator de o importantd deosebita este operatorul = (de atribuire). El trebuie si primeasca
parametru o referinja la un obicct de acelasi tip pe care il copiaz4 in obiectul curent. Prezinta
urmatoarele particularitati:
¢ este folosit in cazul atribuirii de obiecte de acel tip (inclusiv a obiectelor intoarse de catre
functii).
¢ daca mu este definit de citre utilizator compilatorul genereaza implicit unul, care

copiaza membru cu membru (“bit cu bit”) continutul obiectelor.

Din copierea membru cu membru implicita provine si principala sa problema. SA considerim
o clas care are ca si membri un intreg “x” si un pointer “sir” spre un sir de caractere alocat
dinamic si urmiitorul scenariu (prezentat in Figura 4.1):

¢ Jainceput avem doua obiecte a si b, ca in prima parte a figurii. Situatia este in regula.

¢ dupa atribuirea a = b prin copierea adresei membrului sir (la copierea membru cu membru)

pag. 69

Programare Orientaté pe Obiecte - Principii

a b a b a
xl x2 x2. x2
sirl sir2 sir2 sir2
¥
Text I Text 2 Text 1 Text 2 Text 1
initial a, b dupaa =b dupa distrugere b

Figura 4.1 Descrierea scenariului "problema"

unul din siruri devine “pierdut” in memorie (nu mai avem adresa sa pentru a il clibera) iar
ambele obiecte indica spre acelasi sir (o schimbare in unul afectand si celalalt obiect).

¢ dupa distrugerea unuia din obiecte (in figura a obiectului b) obiectul ramas contine un
pointer “eronat”, care indicd spre o zona de memorie deja eliberatd (cu consecinte dintre

cele mai grave, de exemplu blocari).

Problema apare deci datorita copierii membru cu membru a unor membrii a cdror semnificatie
nu este doar valoarea lor propriu zisa, ci ea reprezinta “identificatorul” unei resurse (de exemplu
zona de memorie alocata, figsier deschis, etc.). Pentru a nu pierde / duplica asemenca resurse se

impune _redefinirea_operatorului = in _cazul in care obiectul are_in_componentd asemenea

membri. Operatorul = astfel redefinit trebuie sa elibereze resursa veche gi sa duplice resursa
noua (de obicei prin alocarea uneia noi si copierea continutul acesteia). Un exemplu in care
resursa “vulnerabila” este adresa unui sir de caractere (caz descris anterior) este prezentat in
continuare:

#include <stdio.h>

#include <stdlib.h>
#include <string.h>

class CX
{
public:
int x;
char *Sir;
cx (int ,char *Sir0); // constructor
~CX()F // destructor
CX (CX&v); // constructor copiere
void operator=(CX&v) ; // operatorul de atribuire
void Copiaza(CX&v); // va face cepierea “cu grija”

pag. 70

Cap. 4 ~ Redefinirea Operatorilor

CX::CX(int x0,char *Sir0) // constructor
{
print£("\nConstructor general &d,%s",x0,Sir0);
2=x0;
Ss (char*)malloc(strien(Sir0)+1); // alocare pentru text
strepy (Sir,Six0); // copiere sir text

}

CX::~CX() // destructor
{
printf("\nDestructor td,%s",x,Sir);
free (sir), // eliberare sir text
}
CX::CX (CXav) // constructor copiere
{
printf ("\nConstructcr copiere %d, $s",Vv.x,v.Sir);
Copiazalv); // copiere “cu grija”
}
void operator= (CX&v) // opexator = redefinit
{
printi("\nOperator= %d,3s",v.x,v.Sir);
free (Six); // eliberare sir vechi
Ccpiaza(v); // copiere “cu grija”
}
void CX::Copiaza (CXiv) // copierea “cu grija”
{
XEV LNG // copiere simpla

Sire(char*)malloc(strlen(v.Sir)+1); // alocare sir
strepy (Sir, v.Sir); // copiere sir

}

void main()

{

CX a(i,"sir_1"),5(9,"sir_2"),c(3,"sir_3");

CX d=a,*e; // constr. copiere pentru d

b=c; // operator atribuire

e=new CX(4,"sir_4 Dinamic"); // NU ESTE OPER. REDEFINIT
// SE ATRIBUIE POINTERI

// operator atribuire

c=*e;
delete e;
printf("\nb.>
printf ("\nc.

as",b.x,b.Sir);

Ss" ovwere, Sixr)s

pag. 71

Programare Orientata pe Obiecte - Principii

Rezultatul rularii acestui program se prezint& in continuare:

Constructor general 1,sir_1 // pentru a
Constructor general 2,sir 2 // pantru b
Constructor general 3,sir 3 // pentru c
Constructor copiere 1,sir_1 // pentru d=a
Operator= 3,sir_3 // pentru b=c
Constructor general 4,sir_4 Dinemic // pentru e
Operator= 4,sir_4 Dinamic // pentru c=*e
Destructor 4,sir_4 Dinamic // pentru e

b.x=3 b.Sir=sir 3
c 4 c.Sir=sir_4 Dinamic

Destructor 1,sir_1 // pentru a

Destructor 4,sir_4 Dinamic // pentru c

Destructor 3,sir 3 // pentru b

Destructor 1,sir 1 // pentru a
Remarcam:

* alocarea pe constructor si eliberarea pe destructor a memoriei pentru “resursele” sir.

* copierea “cu grija” se face alocand in prealabil o noua “resursa” si abia apoi copiind
continutul acesteia. Evident, ne rémane si sarcina copierii celorlalti membri (“normali”),
deoarece operatorul de copiere membru cu membru nu se mai genereaza.

e probleme asemandtoare (datorate copierii membru cu membru) se pun si pentru
constructorul de copiere solutia fiind identicd (doar ci “resursa” veche nu este inca alocat&
24 prima data),

¢ diferenta intre constructorul de copiere (utilizat pentru variabila d, cénd = egalul apare

deci nu trebuie intai eliberata — fiind constructor abia acum se alc

chiar la declararea variabilei destinatie) si operatorul de atribuire (cand » apare ulterior

declararii variabilei destinatie).

4.7 Operatorii new si delete

Alti doi operatori ce merité remarcati sunt operatorii new gi delete. Rolul acestora este acela ca
proiectantul clasei s4 poata decide locul in care se face alocarea de memorie pentru obiectele
dinamice ale acelei clase. De cele mai multe ori se alocd 0 vond de memorie mai mare care se
gestioneaza de catre acea clasa “scurtcircuitand” mecanismul clasic de alocare (cAstigul fiind in
principal de vitez&). Operatorii trebuie sa fie metode (nu functii friend) statice (deci mu pot fi

virtuali) si trebuie si aiba urm&torii parametrii:

void* operator new(size t nr);
void operator delete(void* adr, size_t nr);

in care:

pag. 72

__ Cap. 4 — Redefinirea Operatorilor

° nr este dimensiunea memioriei solicitate / eliberate (este stabilita de citre compilator).
Prezen{a sa, aparent inutila deoarece putem cunoaste dimensiunea clasei (folosind
operatorul sizeof), este necesara in cazul obiectelor derivate din aceasta, obiecte care
pot avea alta dimensiune (evident, doar mai mare).

* adr este adresa de memoric care se elibereaza (adresa obiectului ce se distruge).

Operatorul new intoarce adresa zonei unde s-a alocat memorie pentru aceasta instanta a clasei

(unde se va construi obiectul).

Operatorul new se apeleaz’ inaintea apelului constructorului instantei respective iar operatorul

delete dupa apelul destructorului. Daci operatorul new intoarce NULL (nu s-a putut aloca
memorie) nu se mai apeleazi constructorul (care ar face in acel caz accese de memorie

eronate).

La 0 alocare dinamica de obiect lucrurile se petree deci astfel:
° se apeleaza operatorul new (redefinit) care intoarce o adresa (si o notém AdrOb).

e dacé AdrOb este diferita de NULL
© this ‘= AdrOb
® apel constructor corespunzator (cu this parametru ascuns)

e se intoarce adresa AdrOb.

fn exemplul simplu prezentat in continuare clasa X dispune de un buffer static (in care se pot
aloca doar dou obiecte dinamice) si de doua variabile care indic& starea de alocare a fiecdrei

jumatati a bufferului.

#includs <stdio.h>

class X
{
dank 2.F // clemeate utile
public:
X::X() {printf("\nConstr. implicit") ;x=0;y=0;}
X(int «0,int 0) {printf£("\nConstr. %d $d",x0, yO) ;x=x0;y=y0;}

~X() {print£("\nDestr. 4d cd",x,y)i}
void* operator new(size_t nr);
void operstor delete(void* adr, size_t nr);

private:
static char Buffer[);
static int liberl, liber2;

Vi

char X::Buffer[2*sizeof (x) ]}; // memorie pentru 2 alocari

pag. 73

Programare Orientata pe Obiecte - Principii

int X::liberl=1,X::liber2=1; // initial libere

void* X::operator new(size_t nr)

{

printf ("\nOperator new %d",nr);

if(X::liberl) return(X::liberl--0, Buffer ):// pozl
if(X::liber2) return(X::liber2=0, Bul fert+sizeof (X))i// poz2
return NULL; // nu s-a putut aloca

)

void X::operator delete(void* adr,size_t nr)
{
printf ("\nOperator delete 2%d",nr);
Buffertsizeof(X)) X::liber2-1; // poz2 eliberata
Buffer ) X::liberl=1; // pozl eliberata

void main()

{
X a(0,1),*b,*c,*d; // a este obiect STAYIC

b = new X(2,3); // alocare in povitia 1
c = new X(); // alocars in pozitia 2
d = new X(5,6); // alocarea va esua

delete b; // eliberare pozitia 1
dad = new X(7,8); // alocare in pozitia 1

}

Corespunzator, programul va tipari:

Constr. 01 // pentru a4
Operator new 4 // pentru b=new (2,3)
Constr. 2 3 // mmase ME
Operator new 4 // pentru cenew X()
Constr. implicit // fees OY pesos
Operator new 4 // pentru d=ne. fara constructor
Destr. 2 3 // pentru delet:
Operator delete 4 ffi core ME sem
Operator new 4 // pentru d=ner
Constr. 7 8 // Beet  iSsSee
Destr. O1 // pentru a

Pentru infelegerea acestor rezultate trebuie precizat cd programul a fost rulat pe un compilator
pentru care dimensiunea reprezentarii unui intreg este 2 octeti (deci 4 reprezintA dimensiunea

obiectelor de tip X - adica 2 intregi).

Atragem atentia asupra faptului ca, pentru alocare dinamica de tablouri, trebuie redefiniti

pag. 74

__Cap. 4 ~ Redefinirea Operatorilor

operatori specifici:

void* cperator new[] (size t nr)?
void ope or deletel[] (void* adr, size_t nr);

diferit fa{4 de cei anteriori (pentru elemente individuale), la care semnificatia parametrilor este

aceeasi.

4.8 Alte exemple de redefinire a operatorilor

Primul este “hazliu”, redclinind operatorul virguld , cel mai simplu (si cel mai neglijat)

operator:

#include <stdio.h>
class CX
{
public:
int operator, (int x)
{printf ("\nOP virgula param=3d",x); return (x+3) ;}

jt;

void main()
{
printf("\nRezultatul<Sd", (t,2) );

}
secvenla care va tipari:

OP virgule param-2
Rezultatul=5
Putem remarca inca o data distinctia inte operatorul virgula si separatorul virgula utilizat

pentru separarea argumentelor unei functii (situatie in care nu este operator).

Un alt exemplu interesant este urméatorul, in care se redefineste operatorul de indexare

pentru indexarea intr-un tablou folosind “indici” de natura diferita:

#include <stdio-h:
#include <string.h

class Ctst
{

public:
char* Nume(4]; // aici se cauta

~ pag. 75

Programare Orientata pe Obiecte - Principii

int Matricol[4]; ff (PEE WO mone

int operator[] (char *nume) ; // prima varianta operator

int operator[] (int matricol); // a coua varianta operator
}tstObj;

int Ctst::operator[](char *nume) // indexare dupa nume
{
for(int x=0;x<3;x++)
if(strcemp(Nume[x],nume)==v0) // cel cautat ?
return x; 7// gqasit aici
return -1; // negasit

int Ctst::operator[](int matricol) // indexeure dupa matricol

for(int x=0;x<3;xt++)

if (Matricol [x] ==matricol) // @ cel cautat ?
return x; // gasit aici

return -1;

}

void main()
{
tstObj.Nume[0]="unu"; tstObj .Matricol [0]
tstObj.Nume[1]="doi"; tstObj.Matricol{1
tstObj.Nume[2]="trei"; tstObj.Matricol [2
tstObj.Nume[3]="patru"; tstObj.Matricol [3

// initializari

printf("\nRezultate cautare\n 42d \n $2d \n %2d \n 22d",
tstOb} ["unu"),tstObj [2], tstCbj["tre"],tstObj[7]);

}

Operatorii [ ] redefiniti in exemplul anterior intorc indexul in tablou al elementului cdutat sau -1

daca nu este gasit, programul tiparind:

Rezultate cautare

0 // gasit in pozitia 0
2 // gasit in pozitia 2
-1 // negasit

-1 // negasit

4 unaritatii lui si modului de

Probleme deosebite pune redefinirea operatorului -> dator
interpretare. Astfel, compilatorul interpreteazd expresia a->m ca (a.operator->())->m.
Din acest motiv operatorul trebuie s4 intoarca o entitate asupra careia se poate aplica apoi din
nou operatorul -> deci trebuie sa intoarca fie un pointer (cand urmatorul -> este cel standard)

fie un obiect (cand urmatorul -> este un operator redefinit pentru acel obiect).

pag. 76

“ap. 4 — Redefinirea Operatorilor

Un exemplu simplu de aplicare este prezentat in continuare:

#include <stdio.h>

cla CElem
{
public:
int Be
static int comuta;
CElem frate; // adresa obiectului “frate”
CElem* operator->()
{return (comuta) ?frate:this; } // fratele sau el
M

// fanionul pentru comutare

int CElem: :comuta

void main()
{

CElem a,b, *pa, *pb;

1; frate=&b; pa=&a;
=3; b.frate=&éa; pb=4b;
printf ("\ncomuta=\do a.x-id < 1 pa->x=td b.xeSd b->x=td pb->x=id",

scomuta, a.x, a->x, b.x, b->z, pb->x );

a.comuard;
print f("\ncowutas do 4.
a.comuta, Aa.X, A->x, pa->x, b.x,

d  b.x=td b->x=id pb->x=td",
+ po->x );

c=5 4d oso xsid pas>:

}

Dupa cum se poate remarca din codul anterior, am redefinit operatorul -> pentru comutarea
intre cele doua obiecte definite, in functie de o variabila staticd. in esenta, se permite astfel

existenta unui nivel de indirectare, aplicarea lui -> putand face referire indirect la alt obiect.

Functionarea operatorului redefinit este demonstrata de rezultatul rularii programului si anume:

comuta=0 a.

b->x=3 pb->x=3
comuta=1 b

->x=1 pb->x=3

Se remarca faptul c& operatorul -> redefinit se refera doar la cazul aplicdrii lui asupra

obiectelor de acel tip nu si aplicarii lui asupra pointerilor de acel tip.

Redefinirea operatorilor << si >> sti la baza bibliotecii de intrare / iesire a limbajului C++

(biblioteca de lucru pe stream-uri) care va {i tratat& separat.

pag. 77

Programare Orientata pe Obiecte - Pri

4.9 Apel prin valoare, adresa (pointer) si referint&
pentru tipuri definite de utilizator

Revenim asupra exemplului prezentat la inceput, dar in acest caz considerand tipul implicat (ca
si parametru si valoare intoarse) unul definit de utilizator (0 clasa). Dup& cum vom constata este
util pentru infelegere sé avem definit constructorul de copiere si redefinit operatorul = (de

copiere).

#include <stdio.h>
#include <conio.h>

class CX

{

public:
int a;

CX() {printf ("\nConstructor
CX (CX&m) {printf ("\nConstructor
CX(int a0) {printf ("\nConstructor

~CX() {printf ("\nDestructor
void operator=(CX &d)
{printf ("\nOperator= %d=>:d",d.a,a); a-d.a;
Mi
CX functie(CX x,CX *y,CX &z) // cele 3 posibilitati
{
CX t;

printf("\nAm apelat %d td id",x.a,y->a,2.4);
Katt; y-Ppatt; Z.att;
t.a = x.aty->atz.a;
printf ("\nRevin $d td 8d",%.8,y->a,z.a);
return t;

}

void main()
{
CX a(2),b(5),c(8),r(11);

printf ("\nApelez $d 4d td £d",a.a,b.a,c.a,4v.a);
r=functie(a, &b,c);
printf("\nAm revenit %d %d $d td",a.a,b.a,c.a,r.4)

}

Larulare, programul tipareste urmatoarele:

pag. 78


Constructor 2 // pentru a
Constructor 5 // pentru b
Constructor 2 // pentru c
Constructor 11 // pentru r

Apeles

Constructer // pentru x (parametru)

Constructor // pentru t

Am apelat 2 5 8

Revin 369

Constructor cepie 18 // pentru obiectul intors (temporar)
Destructor 18 // pentru t

Destructor 3 // pentru x (parametru)

Operators 18 >71 // copiere obiect intors in r
Destructor 18 // pentru obiectul intors (temporar)
Am revenit 2 6 $9 18

Destructor 18 // pentru r

Destructor 9 // pentru c

Destructor // pentru b

Destructor // pentru a

Interpretand acestea remarcam (in ordinea “desfigurarii evenimentelor”):

se instantiaza obiectele locale functici main (normal),

se execuld corpul functiei main gi se apeleazd functia (cu obiectele avand valorile
asteptate).

se instantiaza un obiect temporar folosind constructorul de copiere (0 copie temporara

a parametrului transmis prin valoare).

se instanfiaza variabila locala (cu constructorul corespunzitor declararii),

se incepe executia corpului funcliei, parametrii avand valorile asteptate.

se efectueaza actiunea efectiva a functiei - se incrementeaza toate variabilele membru ale
obiectelor implicate si se determina rezultatul.

se paraseste corpul functiei, parametrii avand valorile actualizate.

se instantiaza, folosind constructorul de copiere, un obiect temporar in care se
intoarce rezultatul (corespunzator intoarcerii prin valoare).

se distruge variabila local (rezultatul).

se distruge obicctul temporar corespunzator parametrului transmis prin valoare.

se copiaza, folosind operatorul de atribuire, obiectul creat temporar pentru intoarcerea
rezultatului fn obiectul destinatic.

se distruge variabila temporara in care s-a intors rezultatul.

se revine in main, obiectul parametru transmis prin valoare fiind insé nemodificat,

se distrug obiectele locale functiei main (normal).

Daca insa functia intoarce rezullat prin pointer sau prin referinta, este evident c& constructorul

pag. 79

Programare Orientata pe Obiecte - Principii

de copiere si operatorul egal nu se mai folosesc (in momentul intoarcerii din functie). In acele
cazuri trebuie ins avut grijé ca adresa intoarsa sA nu refere un obiect care va fi distrus la

parasirea functiei.

Analiza atenta a remarcilor de mai sus, a codului si a rezultatelor tiparite permit intelegerea
modului cum se face apelul prin valoare, pointer si referina. Revaizand exemplul prezentat chiar
la inceputul capitolului (pentru tipurile predefinite) putem infelege tntr-o lumina noua (mai
generala) acel exemplu. Remarcam inca o dati comportarea uniforma a tipurilor predefinite si
a celor definite de utilizator si importanta deosebiti a constructorului de copiere sia
operatorului de atribuire in cazul apelului functiilor si intoarcerii rezultatului prin valoare.

Rezumat - de tinut minte

e apel prin valoare, adresa si referinta pt. tipuri de baza - particularitati

° friend — functii, metode si clase — au acces la membrii ne-publici

e redefinirea operatorilor = metode (sau functii friend) cu nume de operator
restrictii la redefinire operatori
operatorul de atribuire (si constructorul de copiere) — probleme specifice

° operatorii new si delete, virgula, [ ], ->

e apel prin valoare, adresa si referinta pt. tipuri definite de utilizator, rolul

operatorului de atribuire si constructorului de copiere in acest caz

pag. 80

Cap. 5 ~ Stream-uri

Cap. 5 - Stream-uri

5.1 Introducere

Dupa cum se cunoaste, limbajul C in sine nu includea nici 0 instructiune pentru operatiile de
intrare-iesire. Toate funciiile aferente acestor operatii sunt disponibile sub forma unei biblioteci,
deci nu sunt parte propriu-zisa a limbajului. Cu toate acestea si biblioteca de intrare-iesire este
standardizata, fiind disponibila in forma respectiva pe toate implementarile existente (fapt care
duce la ideea ca ar fi parle a limbajului, cand de fapt avem de a face doar cu functii de
biblioteca). Pentru utilizarea functiilor de intrare-iesire trebuie inclus antetul <stdio.h>

(STDandard Input Output).

Limbajul C++ urmeaza acciasi cale si nu include operatiile de intrare-iesire in limbaj ci intr-o
bibliotecd creataé chiar cu ajutorul limbajului. Deci, in cele ce urmeaza, ne indepartam de la
prezentarea principiilor programarii obiectuale pentru a lua in discutie o anumit biblioteca,
Acest fapt este justificat de:

* necesitatca i utilitatea acestei biblioteci, cu facilitati inexistente in C-ul standard.

e biblioteca in sine reprezinta un exemplu foarte bun de implementare eficienta si eleganta a

unei ierarhii de clase.

Modelul fundamental pentru constructia bibliotecii de I/O este cel de “stream”, vazut ca si flux
de date de la o surs& spre o destinatie. Chiar dac& termenul a ap&rut incd din C-ul standard

consacrarea sa a venit odata cu biblioteca C++.
Prezentam in continuare un cxemplu simplu de tiparire bazat atat pe biblioteca standard C cat si
pe cea C++;

#include <stdio.h> // pentru functia printf
#include <iostream.h> // pentru obiectul cout

char e='6';
int i=123;
long 1=12345678;

float £=12.3456;
double d=1234.456769;

pag. 81

Programare Orientata pe Obiecte - Principii

main ()

{
printf("%c %d %ld %f $1f\n",c,i,1,£,d);
cout<<c<<™ "<<i<<" "K<1<c" NK E£<KK" Ned;

care va produce urmatorul rezultat:

c 123 12345678 12.345600 1234.456749
c 123 12345678 12.3456 1234.456789

Din acest exemplu simplu constatam:
e biblioteca standard C este in continuare disponibila.
tream.h>.

¢ pentru utilizarea bibliotecii C++ trebuie inclus <io:

e exista un obiect cout instantiat implicit.

* iesirea in C++ se bazeaza pe operatorul redefinit «~< (intrarea se va baza pe >>).

¢ funcfionarea este aproape identica (la tipul float apare o diferenfad in ceea ce priveste
numéarul de zecimale folosite implicit).

* solutia C++ este mai simpla, scdpand de necesitatea cunoasterii detaliilor descriptorilor de

format (neplacuta in C).

La baza bibliotecii de streamuri sta redefinirea operatorilor << gi >> care, in acest context, se
ctie (>). Aceasté denumire sugercaza,

numesc operatori de insertie (<<) si respectiv de ex
impreuna cu simbolul, semnificatia operatorului: variabila respectiva se insereaza in respectiv se

extrage din streamul respectiv.

Pentru a evita dificulttile aparute la apelul unor functii de iesire s-a impus folosirea unor
operatori. De ce s-au ales << i >> ?. Intdi, trebuia tinut cont ci nu pot fi adaugati operatori noi.
Apoi, se doreau operatori asemanatori si totusi diferiti pentru intrare si iesire. Semnificatia de
“mai mic” respectiv “mai mare” fiind profund inradacinata pentru operatorii < gi > s-a preferat
utilizarea operatorilor << si >>, mai putin folositi in mod curent (pentru tipurile standard).
Acestia sugereaza sensul transferului de date (inspre stream respectiv de la stream), au
asociativitate bund (de la sténga la dreapta) si prioritate destul de redus& incat si accepte

operanzi expresii aritmetice uzuale, de exemplu:

cout << “x+y ™* zz =" << ie ty * 24

in situafiile in care operatorii implicati in expresii au prioritate mai mic& decdt << si >> se

impune utilizarea parantezelor, de exemplu:

pag. 82

Cap. 5 — Stream-uri

ios

Streambase istream Jos ream strstreambase

A

ifstream || ofstream istrstream | |ostrstream

[ fstream strstream

istream_withassign | [iostream_withassign ostream_withassign
streambuf
ov —s
filebuf strstreambuf

Figura 5.1 Cele doua ierarhii de clase
cout << “x |] y 44 2 =“ << (x |] y && z);

Degi nu vom putea prezenta o descricre completa a celor doua ierarhii de clase ale bibliotecii
I/O din motive de spatiu (oricum, nici nu ne propunem asa ceva) prezentém fn Figura 5.1 aceste
ierarhii, cu notatia consacrata in UML in care sensul sdgetilor indica spre clasa parinte.

Cu toate c& asupra unora vom mai reveni prezentém in Tabelul 5.1 0 scurt& descriere a acestor

clase (atentie la numele lor, care este destul de sugestiv).

in acest moment putem aborda intrebarea: ce sunt cin, cout, cerr si clog ? Raspunsul il gasim

in urmatorul cod existent in “iostream.h>:

extern istream_withassign cin;
extern cstream_wi 2sSsign cout;
extern ostream _withassign cerr;
extern ostream_vithassign clog;

pag. 83

Programare Orientata pe Obiecte - Princi

ios input-output stream clasa de baza a ierarhiei

istream inputstreaim rtual public dinios _|
; responsabila cu operatiile de intrare

ostrearn outputistieati ~~ deri ata virtual public din ios

responsabila cu operatiile de iesire

iostream input-output stream "| derivat multiplu din istream si ostream

fstreambase file stream base a 7

ifstream input file stream ~ pentru stream-uri implementate

ofstream output file stream pe fisiere

fstream file stream

strstreambase string stream base

istrstream input string stream pentru stream-uri implementate

ostrstream output string stream | pe siruri

strstream string stream

istream_withassign [input stream with assign ‘cu posibi litatea de a atribui

ostream_withassign | output stream with assign ° (modifica) streambuf-ul asociat

iostream_withassign | input-output stream with assign

streambuf stream buffer “gestionare buffer asociat cu stream-ul -

filebuf file buffer “| general si specializat pentru fisiere si

strstreambuf string stream buffer siruri

Tabelul 5.1 Scurt& descriere a claselor

Deci cin, cout, cerr si clog sunt obiecte, instan{ieri implicite ale claselor istream_withassign
respectiv ostream_withassign. Pentru intelegerea comportamentului acestora vom analiza
clasele lor de baz& din cadrul ierarhiei. Oricum, in problema bibliotecii de stream-uri, avem
intotdeauna posibilitatea, foarte recomandabila, de a analiza fiyierele antet (<iostream.h> si
altele asociate). In cele ce urmeaz vom prezenta asemenea sec enfe cu mici modificari (doar
simplificari, pentru a renunfa la detalii care nu prezinta semnificafie, dar ar ingreuna
lizibilitatea, cum ar fi declaratii Cdecl, FAR etc si traduceri ale unor comentarii).

5.2 Starea unui stream

Pentru a explica acest concept prezentém o secven{d de cod existenta in ~iostream.h>:

class ios
{

pag. 84

Cap. 5 — Stream-uri

public
// biti de stare a stream-ului
num io_. {
goodbit = 0x00, // nici un bit setat, totul e ok
eofbit = 0x01, // la sfirsit de fisier
failbit 0x02, // ultima operatie I/O a esuat
badbit Oxl4, // s-a incercat o operatie invalida
hard£ = «90 // eroare nerecuperabila
Me
// aflarea/ sterii curente a stream-ului
int rdstate(); // intoarce starea stream-ului
int eof (); // non-zero la sfarsit de fisier
int fail(); // non-zero dac& o operatie a esuat
int bad(); // non-zero dac& a apdrut o eroare
int good (); // non-zero dac& nici un bit setat
void clear(int 0); // seteaza stare stream
protected:
int state; // aici se memoreaza starea (pe biti

roid setslate(int);// seteaza toti bitii de stare
//
i
Consideram ca explicatiile anterioare sunt suficiente pentru a intelege metodele disponibile
pentru operarea cu starea stream-ului. Mai facem doar urmatoarele observatii:
* metoda clear nu poate opera asupra flagului hardfail iar diferenta intre fail() si bad()
ifii “badbit” si “hardfail” iar fail() testeaza in

consta in accea ca bad() testeazi
plus si bitul “fai lbit”.

e Dupa cum era de asteptat, variabila state este declarata protected, pentru a fi
accesibila claselor derivate dar inaccesibila pentru acces direct.

e Remarcém faptul ci membrii si metodele destinate gestionarii starii unui stream se afla in

clasa ios deci, prin mostenire, in toata ierarhia de clase.

5.3 Operatii de intrare-iesire fara format

Pentru aceste operatii clasele istream respectiv ostream pun la dispozitie un set de metode
publice de citire respectiv scricre descrise in continuare (conform <iostream.h>):

class istream : virtual public ios
{
ffows
public:
// extrage caractere intr-un tablou
istreamé get( signed char *, int, char = '\n');

pag. 85

Programare Orientata pe Obiecte - Principii

istream& get(unsigned char *, int, char = '\n');

istream& read( signed char *, int);

istream& read(unsigned char *, int);

// extrage caractere intr-un teablou pind la terminator
istream& getline( signed char *, int, char = '\n');

istream& getline(unsigned char *, int, char = '\n'); |
// extragere un singur caracter |
istream& get (unsigned char ‘);

istreams get( signed char ‘);

int get();

int peek(); // urm&torul caracter fart extragerea sa
istream& putback(char); // pune inapot caracterul

// extrage si neglijeazd, cu oprire la delimitator
istream& ignore(int = 1, int = BOF);

class ostream : virtual public ios

{

public:
ostream& put (char); // insercazS caracterul
ostream& write(const signed char *, int); // insereaz& tabl
ostream& write(const unsigned char *, int); // - '% -

Vi

Dupa cum se constata transferul datelor se face fara a tine cont de semnificatia acestora,
metodele disponibile fiind echivalente cu cele existente in C (getc, pute, read, write), numite
acolo de nivel coborat. Aceste operatii sunt specifice situatiilor in care datele sunt binare (gi, de

cele mai multe ori, fluxul implicat este un fisier).

in leg&tur cu metodele prezentate anterior mai facem urmatoarele remarci:

e get si getline sunt functii cu un parametru luand valoare implicita.

 diferenta intre get si getline avand 3 parametri const in aceva ca, desi ambele se opresc la
citirea  numarului dorit de caractere (parametrul_2 minus 1) sau la atingerea
terminatorului (parametrul_3), get nu extrage terminatorul din istream pe cand getline
il extrage. Astfel, get va continua (la un apel ulterior) cu citirea terminatorului iar
getline dupa acesta (tipic, cu linia urm&toare, daca terminatorul are valoarca implicita).

e metodele get de extragere a unui singur caracter extrag primul caracter din istream
indiferent care este acesta, chiar daca este caracter de tip spatiu. Amintim ca, conform
functiei isspace, acestea sunt: horizontal tab (‘\t’, 9), newline (‘\n’,10), vertical tab

pag. 86

Cap. 5 — Stream-uri

(\w’,11), formfeed (\P 12), carriage return (‘\r’,13, space (* *,32).
* casi in C, operatiile de intrare-iesire fara format se folosesc mai ales cand avem de a face

cu fisiere.

5.4 Operatii de intrare-iesire cu format

Dup& cum rezulla si din primul exemplu prezentat, pentru operatiile de intrare-iegire cu format
se folosesc operatorii de extractie >> si respectiv insertie <<, definiti pentru fiecare din
tipurile de date standard existente in limbaj. Declaratiile acestora se gisesc in <iostream.h> si

le prezentaim in continuare:

class istream : virtual public ios

{
fr
public:
// extrage un caracter
istreams operator>> ( signed char &);
istream& operator>> (unsigned char 4&)
// extrage valori numerice din reprezentarea lor ascii
istream& o Lor>> (short &);
istream& operator>? (unsigned short &);
istreams operator>> (int &);
istream& operator>> (unsigned int &);
istreamé crerator> (long &);
istream& operator>> (unsigned long &);
ist m& operntor>> (float &);
istreams operator>> (double &);
istream& operator>> (long double &);
// extrage un sir
istreams operator>> ( signed char *);
istreamé operator>> (unsigned char *);
ff
Ve
class ostream : virtual public ios
{
UL we a = we %
public:
// insereaz’ un caracter
ostream& operator<< ( signed char);

ostream& operator<< (unsigned char);

// insereax4 reprezentarea ascii a valorii numerice
ostreams ope (short);


Programare Orientata pe Obiecte - Principii

ostream& operator<< tunisiigne i shert);
ostream&é operator<< (int)

ostream&é operator<< (unsign ed int);
ostream& operator<< (long) ;

ostream& operator<< (unsigned long);
ostreamé& operator<< (float);
ostream& operator<< (double);
ostream& operator<< (long double);

// insereaz& un sir
ostream& operator<< (const sioned char *);
ostream& operator<< (const unsigned char *);

// insereaz& reprezentarea ascii 2 valorii pointerului
ostream& operator<< (void *);

Mie
Deci, existenta acestor declaratii (si implementarea operatorilor in cadrul bibliotecii) este cea
care ne permite s& utilizim operatorii de insertie si de extractie. Faptul ca acesti operatori intore
o referinta la streamul respectiv ne da dreptul si cascadim acesti operatori in seevente de

genul:
cout << x<< y << z << t; // la fel cascadam si pentru cin

Fiecare operator, incepand cu al doilea, se aplicd asupra a ceea ce intoarce cel anterior (acelasi

stream). Operatorii se aplicd de Ja stanga la dreapta, conform asociativitatii acestora.

Facem urmatoarea remarca referitoare la operatiile de intrare cu format:
¢ de aceasta data la extragerea unui singur caracter se extrage primul caracter din istream
diferit de caracter de tip spatiu (care au fost amintite anterior) ~ deci se sar caracterele
de tip spatiu. Faptul este explicabil deoarece acum avem de a face cu o extragere

formatata.

si urm&toarea remarca valabila atat pentru cele fara formatat cat si pentru cele cu format:
¢ extragerea (formatata si neformatata) este definita pentru istream iar inserarea (formatatd
si neformatata) pentru ostream. Clasa iostream mosteneste multiplu istream si ostream
deci va avea disponibile atat extragerea cat si inserarea (dar acestea nu vor putea fi

folosite alternat in aceeasi secventa de extrageri sau inserari).

5.5 Format de insertie-extractie. Stare a formatului

Prin redefinirea insertiei si extractiei s-a reusit realizarea de opera{ii de intrare-iesire cu format.

pag. 88

Cap. 5 — Stream-uri

Dar, deocamdata, formatul folosit este cel implicit. Pentru ca insertia si extractia si fie cu

adevarat utile trebuie ca formatul folosit si poatd fi modificat dupa dorinté (asemanator cu

descriptorii de format din familia de functii printf). Pentru a realiza acest lucru in clasa ios

gasim urmatoarele:

class ios
{
th
public:
// fanioane de formatare
enum {
skipws ©x0001, // sare spatii (la intrare)
left 020902, // aliniere la st4nga (la iesire)
right 0.0004, // aliniere la drespta (la iesire)
internal 0»0008, // spatiu dup& semn sau baz&
0=0010, // conversie in / din zecimal
oct x conversie in / din octal
hex = conversie in / din hexazecimal
showbase = indicd baza (la iesire)
showroint = forteaz& afisare punct zecimal
uppercase hexazecimal cu litere mari (iesire
showpos = 0x0400, // adaug& '+' la intregi pozitivi
scientific= Oxus00, // foloseste notatia 1.23452
fixed 021000, // foloseste notatia 123.45
unitbul = 0«2000, // goleste (flush) toate streamurile
dupa insertie
stdio = 0x1000 // goleste (flush) stdout si stderr
dupé insertie
je // sfargsit enumerare
// citire / sstare / stergere fanioane de formatare
long flags();
long flags (long);
Leng set£(long _setbits, long field); // pe biti
long setf (long); // pe biti
lony unsetf (long); // pe biti
// constante pentru parametrul 2 din setf()
static const long basefield; // dec | oct | hex
static const longa adjustfield; // left | right | internal
static const long floatfield; // scientific | fixed

// citire / setare dimensiune camp
int width ();

int width (int);

// citire / setare caracter de umplere
char fill();

“pag. 89

Programare Orientata pe Obiecte - Principii

char fill (char);

// citire / setare digiti precizie la flotant

int precision();
int precision (int);
protected:
long x_flags; // bitii fanioaneclor de formatare
aint x_width; // dimensiune cémp la iesire
int x_fill; // caracter de umplere la iesire
int x_precision; // precizia la iegire flotant%
//
Me

Consideram ca explicatiile anterioare sunt suficiente pentru a intelege metodele disponibile
pentru operarea cu formatul streamului. Facem doar urmiatoarele observatii:

© dupa cum era de asteptat, variabilele care memoreaza starea sunt declarate prot ected,
direct.

pentru a fi accesibile claselor derivate dar inaccesibile pentru ac
© pentru membrii “dimensiune cAmp”, “caracter umplere” si “digiti precizie flotant” avem
metode de citire a valorii si de setarea a acestora.
e pentru diferitii biti de stare a formatului avem la dispozitic metode de citire, scriere si
scriere pe biti. La cele pe bifi amintim:

e setf(long) seteaza in format (x_f12 Ss) biti pe 1 din valoarea parametrului.

© unsetf(long) sterge in format (x_f lags) bitii pe | din valoarea parametrului,

e setf(long, long) sterge in format (x_ flags) bilii pe | din valoarea celui de al
doilea parametru si apoi seteaz4 in format (=_zZlags) biii pe 1 din valoarea
primului parametru. Se asigura astfel un mecanism comod de a evita setari
eronate de biti (de exemplu aliniere atat spre sténga cat si spre dreapta).
Utilitatea constantelor basefield, adjustfield si floatfield

si modul lor de definire devine acum evident.

Prezentém in continuare un exemplu de iegire formatata cu formate diferite:

#include <iostream.h>
main ()

{

double a=987.654;

cout<<a<<"\n"; // format. implicit, insertia 1

cout.setf(ios::left, ios::adjustfield); // aliniere

pag. 90

Cap. 5 — Stream-uri

is

cout.wid

(20);
cout. fill('t_')

cout.precisien (4);

cout<<a<<"\n";

cout.setf(ios::

cout.width (1
cout.
cout.preci

cout<<a<<"\n";

cout<<a<<"\n"7

Se

ill('>');

cout.width(15);

cout<<a<<"\n";

Analizand si rezultatele tiparite:

987.654

987.654
>>>>>>>5>987.65
DET6S
>>>>>>>>>987.65

constatém urmatoarele:

© insertia implicité se face pe nwnarul minim de caractere necesare (corespunzator valorii

insertia
insertia
insertia
insertia
insertia

implicite 0 pentru x_width).

© toate modificarile de format facute func{ioneazd corespunzator.
e analizand insertia 4 se pare ci modificdrile de format sunt valabile doar pentru urm&toarea
insertie. Inscrlia 5 dovedeste contrariul: toate modificarile de format se pastreazd cu

exceptia_dimensiunii

fiecare insertie.

5.6 Manipulatori predefiniti

Dupa cum am vazut anterior se poate controla formatul folosit la operatiile de intrare-iegire.
Solutia prezentata are insd o limitare importanta: nu se pot inlintui insertii (sau extractii) cu
formate diferite - pentru acest lucru s-au introdus manipulatori_de format (pe scurt

manipulatori).

aceasta se pune automat pe 0 (valoarea implicité) dupa

Manipulatorii predefiniti se impart in douad categorii:

© fara argumente

ios: :adjustfield);

UBUnp

fd.
//
//
//

AL
//
//
//
eA

//

//

e dec, hex. oct — scteaz’ fanioanele cu acelasi nume din x_flags.

dimensiune
umplere
precizie
insertia 2

aliniere
dimensiune
umplere
precizie
insertia 3

insertia 4
dimensiune
insertia 5

pag. 91

Programare Orientaté pe Obiecte - Principii

© ws -— elimina toate caracterele de tip spatiu din intrare.
e endl —insereaza ‘\n’ apoi goleste (flush) streamul.
e ends —insereaza un terminator de tip NULL.
° flush — goleste (flush) streamul.
e cuun argument
e setbase — seteazd baza de numeratie pentru intregi (8, 10 sau 16).
e setiosflags, resetiosflags — seteazi respectiv sterge in x_flags bitii pe 1 din
argument.
e setfill, setw, setprecision — atribuie argumentul variabilelor membru x_width, x_fill,

x_precision.

Prezentém in continuare un exemplu de iesire formatata cu mani pulatori:

#include <iostream.h>
#include <iomanip.h> // pentru manipulatorii cu parametri

main()

{

int x=127;

double a=987.654;

cout<<x<<end1l<<hex<<x<<endl<<oct<<x<<endl<<endl;

cout<<a<<endl // de fapt totul e o singura intrnctiune
<<setiosflags (ios::left) <<setw(10)
<<setfill('_')<<setprecision(4)
<<a<<endl
<<setiosflags(ios::right) <<setw(15)
<<setfill('>')<<setprecision (2)
<<a<<endl
<<a<<endl<<setw(15)
<<a<<endl;

}

Analizand si rezultatele tiparite:

127 // implicit
Tf // in hexazecimal

177 // in octal

987.654 // implicit

987.654 // cu un format (1)

>>>>>>>>>987.65 // ca alt format (2)

987.65 // cau formatu? (2) dar l&time minima (0)

pag. 92

Cap. 5 — Stream-uri

>>>>>>>>>937.€65 // ca formatul (2)

constatam urmiatoarele:
© s-a reusit inserarea cu formate diferite (sursa s-a *rupt” pe mai multe linii doar din motive
de lizibilitate)
° pentru folosirea manipulatorilor cu parametri trebuie inclus <iomanip.h>.
¢ chiar si in cazul formatarii cu manipulatori formatul se pastreaza de la o insertie la alta dar

x_width se reseteaza dupa fiecare insertie.
Acum, dupa ce am vazut cA mecanismul manipulatorilor functioneaza, si vedem si de ce

functioneazi _? (ce constructii ale limbajului au in spate ?). Pentru acest lucru trebuie tratati

separat cei fara paramctri si cei cu un parametru deoarece mecanismul este diferit.

Manipulatori fari parametri —in <iostream.h> gasim urmatoarele definitii:

“tual public ios

class istr
{
(| on ee
public:
// manipulatori

istream& operator>> (istream & (*_f) (istream &));

istream& operator>> (ios & (*_f) (ios &) );
a
Mi

class ostré virtual public ios

{
public:
// manipvlatori
ostream& © (ostream& (*_f) (ostreamé&));
ostream& operator<< (ioss (*_f) (ios&));
le

// manipulatori

iosé dec(ios &); // baz conversie zecimala

iosé hex(ios &); // bazA conversie hexadecimala

Loss ect (ios &); // bazA conversie octala

istreama ws(istream &); // extrage caracterele de tip spatiu

ostreams end! (ostream *); // insereaz& ‘\n’ si goleste (flush)
ostreamé ends(cstream 5); // insereaza terminator sir (null)
ostreams flush(ostream %);// goleste (flush)

Constatdm ca, pentru a permite existenfa manipulatorilor, clasele istream gi ostream redefinesc

pag. 93

Programare Orientata pe Obiecte - Principii

operatorii de inserfie si extractie pentru tipul pointer la functie cu prototipurile descrise
anterior si se definesc 7 asemenea functii manipulator. in momentul insertici unui asemenea
pointer metoda operator apeleaza functia respectivi cu streamul curent (indicat de this) ca si
parametru, functia putand deci face inserari, extractii sau modificdri de format in / din streamul

primit parametru.

Aparitia unui manipulator fara parametri intr-o secventa de insertii / extractii echivaleaza deci
cu insertia / extractia unui pointer la functie, pentru care exista redefiniti operatorii de

inserfie / extractie.

Manipulatori_cu_parametri ~ in <iomanip.h> (atentie, trebuie inclus pentru a putea folosi

manipulatorii cu parametri) gasim urmatoarele defin

class smanip_int {
jos& (*_fn) (ios &, int); // mew>ru pointer la functie

int _ag; // memoru intres
public:

smanip_int(ios& (*_f)(ios&, int), int _2):_fn(_f),_ag(_a) {}

friend istream& operator>>(istreamé 8, const smenip. inté f)

{ (* fe fn) {sy _:
friend ostream& operator<<
{ (*_f._fn) (_s,

49); return _s; }
ostream& s,const smanip ints _f)
_9)7 return os; }

|
Hen Pye S

Mr
// asemanadtor se defineste smanip_long {cu long in loc de int)

// baza de conversie 8, 10 sau 16
smanip_int setbase(int _b);

// sterge in x_flags bitii pe 1 din argument

smanip_long resetiosflags(leong _b);
// seteaz& in x flags bitii pe i din argument
smanip_long setiosflags(long _b);

// seteaz& caracterul de umplere
smanip int setfill(int f);

// seteaza digiti precizie la flotant
smanip_int setprecision(int _n);

// seteazd dimensiune camp
smanip_int setw(int n);

in acest caz, datorita prioritafii superioare a operatorului () de apel de functie fata de prioritatea

pag. 94

Cap. 5 - Stream-uri

operatorilor de insertie / extractie. se executi intai un simplu apel de functie (manipulatorul),
care intoarce, prin valoare, un obiect de tipul smanip_int sau smanip_long pentru care exist
redefiniti operatorii de insertie / extractie. In acest fel in / din stream se insereazi / extrage
obiectul intors de functia manipulator. Dupa cum constatam, toti manipulatorii cu un
parametru predefiniti (descrisi mai sus) se conformeaza acestei reguli si intorc un obiect de tipul
smanip_int sau smanip_long. Vom discuta in continuare doar cazul obiectelor smanip_int,
pentru smanip_long lucrurile petrecdndu-se asem&nator (in fapt, ambele clase sunt definite
simultan, printr-un mecanism de tip #define sau folosind clase template, in functie de

implementare).

Analizand clasa smanip_int constatém existenta unui constructor public cu 2 parametri — un
pointer la o functie si o valoare intreag’ — care doar salveazi valorile parametrilor in variabile
membru, Mai gasim redefinifi (ca si functii friend) operatorii de insertie si extractie pentru
aceasta clasd, operatori care apeleazi functia indicata de pointerul la functie avand ca si
parametri streamul si valoarea intreagd. Prin acest mecanism al obiectului temporar de tip
smanip_int se reugeste “pasarea” valorii parametrului manipulatorului spre functia care executa
efectiv operatia, pastrand eleganta inlantuirii operatorilor de insertie / extractie. Detalii

suplimentare se vor gasi la tratarea manipulatorilor cu parametri definiti de utilizator.

5.7 Manipulatori definiti de utilizator

Pe lang& manipulatorii predefiniti existenti programatorul poate defini si manipulatori noi, care
pot opera schimbari complexe asupra formatului folosit in continuare. Desi exemplele care vor
fi prezentate nu indica acest lucru, amintim ca dimensiunea campului (parametrul x_width din
format) este resetat dupa fiecare insertic. Deoarece mecanismul manipulatorilor este diferit

pentru cei fara parametri si pentru cei cu parametri, vom prezenta in cele ce urmeaza cate un

exemplu de definire de manipulator propriu pentru fiecare tip.

Definire manipulator fara parametri:

#include <iost t.A>
#include <iomanip.h>

ostream& formatl(ostream& o_str) // primul manipulator
(

o_str<<endl<<"format1"<<endl;

o_str< ‘setiosflags(ios::left);

o_str<<setw(10);
o str<<setfi 11(
o_str<<setpre

');

ion(4);

"pag. 95

Programare Orientaté pe Obiecte - Prine

return o_str;

}
ostream& format2(ostream& o_str) // a1 doilea manipulator
{
o_str<<endl<<"format2"<<endi;
o_str<<setiosflags(ios::right);
o_str<<setw(15);
o_str<<setfill('>');
o_str<<setprecision (2);
return o_str;
}
main ()

{
double a=987.654;

cout<<a<<endl<<format1<<a<<end1l<<format2<<a<<endl;

care va produce la iesire:

987.654

formatl
987.654

format2
>>>>>>>>>987.65

Analizénd exemplul constatam:

e manipulatorii fara parametri se definesc simplu ca functii avand un prototip impus:
primesc si intorc referinté la un stream (istream sau ostream dupa cum dorim sa fi
folosim la extractie sau insertie) sau la un obicct de tipul ios.

pentru folosire se insereaza / extrage pointerul la functia respectiva (numele functiei).

e apelul lor se face (de citre operatorul de insertie / extractie redefinit de ostream / istream)

cu streamul curent ca si parametru.

Remarc4m mecanismul simplu de apel al manipulatorilor fara parametri, bazat doar pe
redefinirea, in clasele ostream respectiv istream, a insertiei / extractiei pentru tipul pointer la

functie.

Definire manipulator cu parametri:

pag. 96

Cap. 5 -- Stream-uri

#include <iostraam.h>
#includse <iomanip.h?

jos& my_manip f(iosé +, int nr) // functie auxiliar&’
{

if (nr==?2) { // formatul 1
i.setf£(ios::left, ios::adjustfield);
i.width (10);
i.fill('_');
i.precision (4);

}

Lf (nxre=2) { // formatul 2
i.set£(ics::cight, ios::adjustfield);
i.width(15);

2 cELLL( "2
i.pr i)e
}
return 7;
}
smanip_int my_manip(int n) // manipulator propriu-zis
{
return smanip_int(my manip _f, n); // prin valoare
}
main ()

{
double a=987.654;

endl<<my_manip (1) <<a<<endl<<my_manip (2) <<a<<endl;

care va produce Ja iegire:

987.654
987.654
>>>>>>>>98

x

654

Analizind exemplul constatam:
© manipulatorii cu paramctri (in exemplul nostru my_manip) se definesc ceva mai
complicat, ca gi functii avand un prototip impus: primesc un parametru intreg gi intorc
(prin yaloare) un obiect de tipul smanip_int (sau smanip_long) construit pe baza unui
constructor avand ca $i parametri: un pointer la o functie auxiliara asociata (cu prototip
impus) si valoarea parametrului manipulatorului.
e in functia auxiliara, avand prototipul impus si primind parametru o referint& la ios, nu

putem folosi operatorii de inserfie / extractie si manipulatorii predefiniti deoarece

pag. 97

Programare Orientata pe Obiecte - Principii

acestia sunt definiti doar in clasele derivate istream si ostream gi suntem obligati si
facem formatarile cu metodele lui ios.

° efectiv se va insera / extrage in / din stream acest obiectul smanip_int intors prin valoare
de catre functia manipulator

© functia auxiliari asociaté se apeleazi (de catre operatorul de insertie / extractie redefinit

de smanip_int) avand ca si parametri streamul curent si parametrul intreg.

Remarcim mecanismul laborios de apel al manipulatorilor cu parametri, mecanism necesar
pentru:

¢ a putea “cascada” asemenea manipulatori impreuna cu insertia / extractia altor tipuri.

® a transmite parametrul manipulatorului spre o functie operator de insertie / extractie care

sa aiba acces la streamul implicat.

5.8 Intrare — iesire pentru tipuri definite de utilizator

O alta facilitate a bibliotecii de streamuri este c4 permite (pe langa insertia / extractia tipurilor

predefinite) in mod unitar insertia / extractia tipurilor definite de utilizator.

Desi mecanismul nu este prea complicat, avand la baza simpla redefinire a operatorilor de
inserfie si / sau extractie pentru tipul definit de utilizator, eleganta acestui mecanism face ca

aceasta facilitate sa fie una foarte raspandita,
Exemplificdm in cele ce urmeaz& acest mecanism pentru 0 clas proprie PUNCT (simplificata):

#include <iostream.h>

class PUNCT
{
int x,y;
friend istream& operator>>(istream& i, PUNCT? ob)
{ i>>ob.x; i>>ob.y; return i; }
friend ostream& operator<<(ostream& 9, PUNCTé ob)
{ 0<<"x="<<ob.x<<" y="<<ob.y; return 90; }

yi
PUNCT un_punct;

main ()

{
cout<<"introduceti x si y "; cin>>un_punct;

cout<<"coordonatele sunt "; cout<<un_punct;

pag. 98

Cap. 5 — Stream-uri

care va produce la rulare:

introduceti x si y 12 // acum un_punct.x=1, un_punct.y=2
coordonatele sunt

Facem urmatoarele observatii:

e valorile 1 si 2 cu bold si italic au fost introduse de Ja tastatur (nu afisate direct de catre
program).

© operatorii de insertic si extractie s-au redefinit ca si functii friend pentru clasa PUNCT.

© citirea respectiv scrierea obiectului un_punct s-a realizat simplu $i elegant. Avantajul este
si mai vizibil pentru clase mai complexe (cum ar fi tablouri, liste, arbori) si pentru
streamuri de tip fstream.

e desi nu apare vizibil in acest exemplu, insertia / extractia tipurilor predefinite si a celor

definite de utilizator poate fi facuta “amestecat”, in orice succesiune.

5.9 Legarea streamurilor (“Tying of streams”)

O problem specifica care apare intr-o secventa de genul:

int x;
cout<<”introduceti x:” // insertie
cin>>x; // extractie

este urmatoarea: cum avem garantia ca textul inserfiei apare pe ecran inainte de executia
extractiei (avand in vedere ca streamurile sunt “buffer-ate”, dup& cum se va descrie mai

amanuntit ulterior) ?. Solutia acestei probleme se gaseste in clasa ios in urmatorul cod:

class ios

{

public:
// citir / scrierea ostream-ului legat de acest stream
ostream' “ie(); // citeste x_tie
tie(ostream*); // atribuie x_tie si intoarce
// vechea valoare
protested:
ostream* x tis; // ostream-ul legat, dac& exista

Vi

Exist deci un pointer (evident protected) spre ostream-ul asociat stream-ului curent si metode
de citire si scriere a acestui pointer. Acum, orice extractie (>>) forteaz& golirea ostream-ului

pag. 99

Programare Orientata pe Obiecte - Principii

legat (“tied stream”, dacd el exist&), asa incdt suntem siguri ca insertiile (<<) anterioare au ajuns

efectiv "la destinati

O secventa de genul:

cout<<”introduceti x:” // insertie
cin>>s // extractie

este deci echivalenta cu:

cout<<“introduceti x:“ // inserti+
cout.flush(); // golire (flush) a ostream-ului legat
cin>>x; // extractie (pr data de golirea

// ostream-ului legat)

deoarece cout este legat de cin printr-o secventa cin. tie(&cout). Un apel de genul

is.tie(0) anuleaza legatura anterioara a streamului.

5.10 Buffer-area streamurilor

Operatiile de intrare — iegire sunt specificate fara mentiunea tipului streamurilor, dar nu toate
dispozitivele pot fi tratate uniform in ceea ce priveste strategia de buffer-are. Solutia este aceea
de a furniza la initializare diferite tipuri de buffere. Asupra acestora exist ins un set comun de
operatii, asa incat istream si ostream nu cunosc detaliile implementarii. Evident, pentru aceasta
problema exist solutia, extrem de potrivité, a mecanismului metodelor virtuale - metodele
underflow si overflow sunt declarate virtuale. Aceste metode sunt suficiente pentru a actualiza,
dupa dorint&, strategia de buffer-are si sunt un exemplu foarte bun de utilizare a mecanismului
metodelor virtuale pentru a permite tratarea uniforma a unor facilitati logice echivalente, avand

ins& implementari diferite. In <iostream.h> gasim:

class streambuf {

“i seb ew kw s
public:
// constructori si destractori
streambuf (); // streambul gol
streambuf(char*, int); // streambuf cu un tablou dat

virtual ~streambuf ();

// extragere caractere din buffer

int sgetc(); // extrage 1 caracter
int sgetn(char*, int); // extrace n caractere
virtual int underflow(); // umple bufferul gol

pag. 100

Cap. 5 — Stream-uri

// punere caractere in buffer

int sputc(int); // pune 1 caracter
int sputn(const char*, int); // pune n caractere
virtual int overflow(int = EOF); // goleste (flush) buffer
protected:
// oO multime de metcde de accesat membrii privati de c&tre
// clasel ivate

privete:
short alloc ;
short unbuf.

// nenul daca buffer trebuie eliberat in final
// nenul dac& nu este buffer-at

char* / inceputul zonei buffer
char* // sfirgitt+l al zonei buffer
char* / urmstoarea locatie pentru pus (inserat)
char* // urm%toarea locatie pentru luat (extras)
//
Me
//
class ios
{
//
public:
streambuf* rebuf(); // intoarce streambuf asociat
protected:
streambuf* bp; // streambuf-ul asociat
//
Mi

Clasa streambuf are metode de citire (extragere) din buffer a unuia sau mai multor caractere si
de scriere (insertie) in buffer a unuia sau mai multor caractere. Pentru tratarea cazurilor cand

bufferul devine plin respectiv gol existé metodele virtuale overflow tespectiv underflow.

Clasele strstreambuf si filebuf vor mosteni streambuf si vor adduga constructori mai deosebiti,
metode specifice si vor redefini metodele virtuale overflow si underflow astfel incat clasa ios

(si cele derivate) sa utilizeze aceste clase buffer in mod transparent.
Clasa ios (clasa de baza a ierarhiei) are un streambuf asociat pe care il va folosi in operatiile de
intrare-iesire.

§.11 Streamuri si siruri

Pentru lucrul cu streamuri reprezentate pe siruri exista, in cadrul celor doua ierarhii, clasele

strstreambase, istrstream, ostrstream gi strstream gi respectiv strstreambuf. Pentru utilizarea

"pag. 101

Programare Orientata pe Obiecte - Principii

acestora este necesara includerea antetului <strstrea.h>. Acesta, la randul sau, include implicit

‘iostream.h> care nu mai trebuie inclus explicit (dar clasele descrise in el sunt vizibile),

in <strstrea.h> gasim:

class strstreambase : public virtual ios {

public:
strstreambuf* rdbuf(); // implenentat {return 4this->buf;}
private:
strstreambuf buf; // strstreambuf-ul ciat
protected:
// 2 constructori si destructor
bi // declararea asei e prezentata complet

Mai merit amintite metodele str(), existente in ostrstream si strstream, care intore adresa sirului
pe care este implementat bufferul asociat (si deci si streamul). Clasa ostrstream mai are in plus
doar o alta metoda (pe care nu o mai descriem); in rest avem doar constructori si destructori (si

mostenirile corespunzatoare ierarhiei).

Un exemplu de utilizare a unui stream reprezentat pe siruri este dat in continuare:

#include <strstrea.h> // pentru ostrstrean
#include <stdio.h> // pentru printi

main ()

{

char un_sir[100],*
int x=1,y=2,2=3;

ostrstream my_str(un_sir,100); // instantiez un ostrstrean

my_Str<<"x="<<x<<" yel<<y<e™ zo"<<zcsends; // scriu in el
Pp _str.rdbuf()->str(); // ian adresa sirului
printf ("sirul = '$s'",p); // o folesesc la ceva (tiparire)
}
care tipareste
sirul = 'x=1 y=2 z2=3'

Constatém astfel cé programul a facut inserfii in mod clasic, fara a conta ca este un stream pe
sir. Sirul astfel rezultat (formatat) poate fi apoi folosit la alte operafii. Mecanismul este oarecum
corespunzator functiei sprintf din C standard doar ca acum este transparent pentru cel care face

insertia care este destinatia (stream pe ecran, pe sir sau pe fisier).

pag. 102

Cap. 5 — Stream-uri

5.12 Streamuri gi fisiere

Pentru lucrul cu streamuri reprezentate pe fisiere exist, in cadrul celor douad jerarhii, clasele
fstreambase, ifstream, ofstream si {stream si respectiv filebuf, Pentru utilizarea acestora este
necesara includerea antetului <fstream.h>. Acesta, la randul sau, include implicit <iostream.h>
care nu mai trebuie inclus explicit (dar clasele descrise in el sunt vizibile).

In “iostream.h> gasim

class ios

{

public:
// moduri de desciidere a unui stream
enum open_mode {
in = 0x01, // pentru citire
out = 0x02, // pentru scriere

= 0x04, // pozitionare la sfarsit
0x08, // in append

0x10, // trunchiaz& dac& exist&

0x20, // eguare dac& fisierul nu exist&
= 9x40, // esuare dac& fisierul exist4%

0x30 // fisier binar (nu text)

// pentru pozitionare
_dir { beg=0, cur=1, end=2 };

iar in <fstream.h:> gasim:

class fstreambase : virtual public ios {
public:
// 4 constructori si destructor

void open(const signed char*, int, int = filebuf::openprot) ;
void attach(int);

void close();

filebuf* rdbuf(); // implementat& { return buf; }

private:
filebuf buf; // filebuf-ul asociat

di

Clasele ifstream, ofstream si fstream au doar constructori, destructori si metodele rdbuf si open

cu aceleasi prototipuri, dar uneori avand valori implicite pentru parametri.

pag, 103

Programare Orientata pe Obiecte - Principii

Prezentim in continuare un exemplu de inserarea unui obiect de tip definit de programator (0
matrice rudimentara) intr-un stream de tip fisier.

#include <fstream.h>

class M // o clasa "matrice”
{
int m{3] [3];
friend istream& operator>>({istreams i, Mé a);
friend ostream& operator<<(ostream< o, M& a);
public:
M();
ja; // si o instanta de matrice

M::M() // constructor

for(int y=0; y<3;y++)
for(int x=0;x<3;xt+)
m[y][x]=10*y+x; // o initielizare carecare

ostreamé operator<<(ostream& 0, Mé m)
{
for(int y=0;y<3;yt+,o<<endl) // atentie la endl
for(int x=0;x<3;xt++)
o<<m.m([y}][x]<<" ";  // insereaza un clemant
return 0;
}

istream& operator>>(istream& i, M& m)
{
for(int y=0; y<3;yt+)
for(int x=0;x<3;x++)
i>>m.m(y] [x]; // extrage un element
return i;

}

main()
{

// insereaza matricea in stream (fisier)

ofstream ofs("matrice.txt"); // instantiere un ofstream
ofs<<a; // inserare a pe stream
ofs.close(); // inchidere stream

// extrage matricea din stream (fisier)
ifstream ifs("matrice.txt"); // instentiere un ifstream
ifs>>a; // extvagere a de pe stream
ifs.close(); // inchidere stream

pag. 104

Cap. 5 — Stream-uri

}

fn urma rularii matricea va avea (dupa extractic) tot valorile cu care a fost inifializata in

constructor. Vom avea insa fisierul matrice.txt cu urmatorul continut:

012
10 11 12
20 21 22
Atragem atentia ca, in mod normal (desi nu obligatoriu), insertia si extractia sunt gandite in

pereche, pentru a putea ulterior extrage de pe stream obiectul inserat.

5.13 Concluzii

Dup& cum am amintit la inceputul capitolului, putem privi biblioteca de streamuri ca si un
exemplu de aplicare a notiunilor intalnite in capitolele anterioare. Astfel avem:
e incapsulare (de exemplu: starea streamului si a formatului sunt memorate in variabile
protected, pointerii din streambuf sunt privati, metodele sunt in majoritate publice).
° instanti incuibarita (membrul buf din clasa strstreambase).
* mostenire (a se vedea ambele ierarhii din Figura 5. 1).
© mostenire multipla virtuala (in clasa iostream, de aceea starea streamului si starea
formatului sunt unice atét pentru operatiile de intrare cat si pentru cele de iegire).
© polimorfism (metodele underflow si overflow de la clasa streambuf sunt virtuale pentru
a putea fi inlocuite).
° redefinire de operatori (utilizarea << si >> se bazeaza pe redefinirea, in clasele ostream

si istream, a acestor operatori).

Rezumat - de tinut minte :-)

° insertie (:) si extractie (>>)

e ierarhiile de clase, ce sunt cin si cout

° starea unui stream

° operatii de intrare-iesire fara format si cu format, cascadare

° format de insertie-cxtractie, stare a formatului

° manipulatori fra parametrii si cu parametrii: utilizare, definire, mecanism

° legarea si buffer-arca streamurilor

° streamuri si clase proprii - redefinirea inserfiei si extractiei ca si functii friend
° streamuri pe sir si streamuri pe fisier

° exemplu real de scriere a unei ierarhii de clase

pag. 105

Programare Orientata pe Obiecte + Principii

Cap. 6 - Tratarea exceptiilor

“Exceptie” este unul din acele cuvinte care au semnificatie dilerita pentru persoane diferite.
Mecanismul C++ de tratare a exceptiilor este proiectat pentru a permite tratarea erorilor, mai
ales in programe ce folosesc componente soft scrise independent. F'xceptiile sunt privite in acest

context ca si o generalizare a notiunii de eroare, putand avea si exceptii care nu sunt erori.

Mecanismul de tratare a excepfiilor este destinat doar tratarii excepliilor sincrone. El este de
fapt o structura de control ne-locala (“non-local control structure”) care poate fi privité ca un
mecanism de intoarcere. Totusi, principalul scop al mecanismului C++ de tratare a exceptiilor

este tratarea erorilor.

6.1 Originea problemei

Problema apare la interfata intre un modul “client”, care foloseste niste servicii, si un modul
“nucleu”, care le ofera. fn acest context, prin modul nucleu intelegem orice modul care
furnizeaza servicii, fie el modul de biblioteca (si, eventual, strans legat de nucleul sistemului de
operare) sau modul al propriei aplicatii, iar prin modul client intelegem orice modul care
apeleaza (invoca, utilizeaza) servicii ale modulului nucleu.

Problema {si are originea in urmatoarea contradictie:
e Modulul nucleu - poate detecta o exceptie dar nu stie cum doreste clientul sa o trateze
(poate detecta, nu poate trata).
° Modulul client — stie cum trebuie tratat& exceptia nu poate detecta aparitia ei

(poate trata, nu poate detecta).

6.2 Solutia traditionala de tratare a erorilor si exceptiilor

fn mod traditional pentru tratarea erorilor aveam de ales intre urmatoarele variante:
e terminarea aplicatiei — acest Jucru se intémpla implicit cand o exceptie nu este “prins&”
de nimeni. De cele mai multe ori insd putem (si trebuie) si facem mai mult.
e intoarcerea unei valori reprezentand “eroare” — aceasta este o solutie des intalnita, cu
tofii am testat vreodata daca un apel de functie de bibliotecd nu a intors cumva eroare.

Uneori ins, cand toate valorile intoarse sunt semnificative, se impune alegerea pentru

pag. 106

). 6 - Tratarea exceptiilor

variabila intoarsi a unui tip “mai mare” pentru a putea discerne codul erorii.
Necesitatea testarii permanente a valorii intoarse stricd ins eleganta codului.

© pozitionarca unui indicator global — este si ea des intalnita (impreuna cu solutia
precedent sau singura). Necesitatea testarii acestui indicator global devine repede

suparatoare astfel incat cleganta codului are din nou mult de suferit. fn prezenja

concurentei (a procesiirii paralele) solutia este evident inacceptabila.

© ignorarca excepfiei — este amintita mai mult ca si posibilitate, decat ca solutie propriu-
zisé. Din momentul ignordrii unei exceptii nu mai avem nici o garantie asupra
predictibilitatii functionarii ulterioare a programului.

* invocarea unui handler specializat — este 0 solutie corecta, cand se genereaza un apel
spre o funcfie de tratare a exceptici. Principala problema este aceea ci mecanismul nu

este general, fiind valabil doar pentru un numéar foarte restrans de exceptii.

6.3 Solutia C++ de tratare a exceptiilor

Deoarece toate solutiile amintite anterior prezinta dezavantaje majore, in C++ s-a introdus un
mecanism nou de tratare a exceptiilor, care are la bazd 3 cuvinte cheie:

¢ try — delimiteazi un bloc la nivelul clientului care confine instructiuni susceptibile de a
genera excepfii (semnificafia fiind “incearca s4 executi acest cod”),

e throw ~ se executd de ciitre modulul nucleu cand se detecteazi o situatie exceptionala
pentru a semnala modulului client acest lucru (semnificatia fiind de “arunca exceptia
modulului client”).

¢ catch ~ delimiteazi un bloc la nivelul clientului care se va executa in momentul

interceptarii unei exceptii (semnificatia fiind “prinde acest tip de exceptie”).

in sarcina modulului client intra:

© punerea codului “vulnerabil” intr-un bloc try.

° tratarea fn blocuri catch a diferitelor exceptii ce ar putea s& apara,
in sarcina modulului hucleu intra (uneori tot noi scriem si acest modul):

© detectarea situatici deosebite si generarea exceptiei corespunzatoare cu throw.

Un exemplu tipic de utilizare este prezentat in Figura 6.1.

6.4 Discriminarea exceptiilor
tn continuare prezent&ém un exemplu care doreste s4 evidentieze majoritatea situatiilor care apar

in practica:

pag. 107

Programare Orientati pe Obiecte - Principii

Modul CLIENT

// instructiuni

// anterioare
try a
{ ee

// apel “vulnerabil”

// apel “vulnerabil”
// apel “vulnerabil”
} }
catch(tip_1)
{

// tratare exceptie tip_l
}
catch(tip_2)

{
// tratare exceptie tip _2

Modul NUCLEU

tol soe on |

// la detectare exceptie
throw tip i;
//

} ; T] apel resurs& vulnerabila
// tratare alte exceptii

catch (tip_N) [2] daci nu a aparut exceptie

{ urmeaza intoarcere la instr
// tratare exceptie tip_N urm4toare din blocul try

} ——

CABCH (ew «2! [3.| Gacd a rut exceptie se

{ trece la tratarea exceptiei
// tratare exceptie alt tip .

} evident, variantele 2 si 3

. 7 7 1 eal cs
// instructiuni ie: exclud Zeciproc
// urmatoare
Figura 6.1 Exemplificarea mecanismului de tratare a excepliilor

#include <stdio.h>
#include <conio.h>

class CErrorl {};
class CError2 {};
class CError3
{

public: int err;

CError3(int ecode)

Mi
class CError4 {};
void functie(int ch) //
{

printf ("\nIntrare functie

// diferite c

{e

heer",

lase de exceptie

rr=ecode; }

pe post de modul nucleu

ch );

pag. 108

Cap. 6 — Tratarea exceptiilor

}

switch (ch)

{
case 'A': throw CErrorl(); // se genereaza
case '' throw CError2(); // diferite
case 'C': throw CError3(7); // exceptii
case 'D': throw CError4();
}

print£("\nIesire functie '$c'", ch );

void main()

{

}

printf ("\nINCSEUT") ;

try
{
printf ("\nApelez") ;
functie(getch()); // aici trebuie apasata o tasta
printf ("\nSucces");
)
catch (CErroerl) // urmeaza discriminarea
{ // exceptiilor

print£("\nCatch CError1");

seeemommeess )

printf£("\nCatch CError2");

1 adh lai "ror3& e)

printi("\ncatch CError3 td", c.err);

— ze) // vamura “default”

printf£{"\nCatch general");

| date Pi endisitecam’y // de aici se continua dupa
// constructia try-catch

Programul se va comporta diferit in functie de tasta pe care 0 apasém in momentul rularii. Vom

considera pe rand toate situatiile semnificative, vom indica ce tipareste programul si vom

incerca sa explicéim acest Jucru:

Se apasa tasta E

INCEPUT
Apelez

Intrare functie

tot

pag. 109

Programare Orientata pe Obiecte - Principii

Iesire functie 'E'
Succes
SFIRSIT

Totul se desfasoara normal, mecanismul de exceptii nu inlervine in nici un fel.

Se apasa tasta A

INCEPUT

Apelez

Intrare functie 'A!'
Catch CErrorl
SFIRSIT

fn switch se genereaz’ o exceptie de tipul CErrorl, se abandoneaza corpul functici si blocul try,

se continua cu blocul catch cu potrivire de tip si apoi cu instructiunile de dupa blocul try-catch.

Se apasa tasta B

INCEPUT

Apelez

Intrare functie 'B'
Catch CError2
SFIRSIT

Lucrurile se petrec asemanator cu cazul anterior, singura diferenja este aparitia variabilei e in

catch-ul corespunzator.

Se apasa tasta C

INCEPUT

Apelez

Intrare functie 'C'
Catch CError3 7
SFIRSIT

Lucrurile se petrec asemanator cu cazul anterior, dar variabila c apare prin referin(a. In plus,

avem accesibila si valoarea 7 transmis& din corpul functici (de la throw).
Se apasa tasta D
INCEPUT

Apelez
Intrare functie 'D'

pag. 110

Cap. 6 ~ Tratarea exceptiilor

in situafia th core nu avem nici wn bloc eateh pentru tipul respectiv, se intra la catch(...).

Din exemplele anterioare reies si urmStearele observafii generale:
ifia oricitei exceptii se abandoneaza corpul functiei si corpul blocului try. Daca

e Laaps

ultima instructiune din blocul try ajunge s& se execute, avem garanfia ca intreg blocul

Iry s-a execulat cu succes (aici am putea da un mesaj de genul “operatia a fost

executat’ cu succes”),
¢ La generarea unei exceptii(de un anumit tip) se continua cu blocul catch eu potrivire de
tip. \ceasti discriminare a exceptiilor se poate face doar dupa tip (cazul CErrorl)
exceptie (cazul Cerror? $i CError3).

sau poate fi utilizat si un obi
 Situatia scaming cu un apel de funcfie (apelul blocului catch) $i transmitere de parametru

~ prin valoare pentru cxemplul CL rror2 si prin referinf4 pentru exemplul CError3.

ror s-a reslizat ceca ce se numeste “tratare de exceptii cu schimb de
informat Desi aceast’ facilitate este deosebit de utilé (modulul nucleu poate
transmite informatit modulului client cu detalii despre cauza exceptiei) ea are la bazd o

a ca objectul exceptic poate avea membri (date gi

e in situatia Ch

inume al

idee destul de simpli

metode). Modul in care se va genera obiectul de exceptie si se va transmite el blocului
catch va fi tratat ulterior.

© Tratares exeeptiilor cu schimb de informatii se poate face atat in cazul variabilei exceptie
transferaté prin referinta (ca in exemplul CExror3) cat si in cel cu transfer prin valoare

i pentru CError2) dar, evident, nu se poate face cand

r fi putut fac
fice dear dup i tip (eazul CError1).

(de exemplu.

discriminarea
¢ Pentru un anumit tip discriminarea se poate face:
© prin tip (de exemplu CErrorl)

° prin tp si variabilii transmisa prin valoare (de exemplu CError2)
® prin lip 3! variabild transmis prin refering (de exemplu CError3)
dar numai prin una din aceste posibilitati. In cazul existenfei mai multor blocuri catch pentru

i eroare I compilare.

acelasi tip, se gene!
¢ Daca nu exist’ nici un bloc catch cu potrivire de tip se intré pe la catch(...), Daca acesta
sla, exceptia va {i tratata de ciitre modulul client ierarhic superior (apelantul

nu ©

acestui cod).
exccula un singur bloc catch, Dupa terminarea corpului

ja unei exeeptii

e La ape
acestuia se continua cu instructiunile de dupa constructia try-catch.


Programare Orientaté pe Obiecte - Principii

6.5 Detalii despre implementarea mecanismului de tratare a exceptiilor

Acum, dupa ce am vazut cum se utilizeazd mecanismul de tratare a exceptiilor, sd incercim sa

infelegem si ceea ce se intampla “intern”. Pentru aceasta am considerat exemplul urmator:

#include <stdio.h>

class X // clasa oarecare
{
public: int x;
X(int x0){x=x0; printf("\nCME id", x);}
a

~X() { printf ("\nb Ba", 4);)
Mi

class E // clasa exceptie
{
public: int e;
E(int e0) {e=e0; printf ("\nCEE %d",e);}

E(E& ee) {e=ee.e; printf("\ncEC Sd",e);}
~E() { printf ("\nDE a"; @) 7}
Mi
void functie() // pe pest de modul “nucleu”
{
X a(l);

throw E(11);
}

X b(2);
void main()
{

X ¢(3)3

printf ("\nINCEPUT") ;

try
{
xX d(4);
printf ("\nApelez") ;
functie(); // apelare modul “nucleu”
printf ("\nSucces") ;
}
catch(E e)
{
xX £(5)e
printf("\ncatch E sd",¢.e);
}
catch(...)

pag. 112

Cap. 6 — Tratarea exceptiilor

X g(G)z
printf("\neatch ...");7
}
printf ("\nSFIRSIT") ;

}

La rularea programului se va tipari:

CXE 2 // antiere b

CXE 3 // instantiere c

INCEPUT

CXE 4 // instantiere d

Apelez

CXE 1 // instantiere a

CEE 1 // instantiere obiect exceptie “local”

CEC 11 // instantiere (copiere) obiect exceptie “global”
DE 11 // distrugere obiect exceptie “local”

DX 1 /iad tugere a —- parasire fortata a functiei
DX 4 i d - parasire fortata bloc try
CEC li antiere e (#28)

CXE 5 // instantiere f

catch F 11 // utilizar2 e (e.x=11)

DX 5 trugere £

DE 11 // distrugere (3e4)

DE 11 // distrugere ebiect exceptie “global”
SFIRSIT

DX 3 tes

DX 2 b

Din analiza rezultatelor observam:

¢ Se apeleaza constructorii instantelor b, c, d sia in mod normal.

¢ La generarea unei exceptii - throw E(11) - se instantiazd un obiect de clasi exceptie (E)
folosind constructorul cu parametri (deoarece am considerat un caz de exceptie cu
transfer de informatii). Dac& am fi avut un caz de exceptie fara transfer de informatii,
s-ar fi instantiat obiectul exceptie folosind constructorul fara parametri (dar, totusi,
trebuiau puse parantezcle dupa throw). Remarcém aseminarea sintaxei cu cea a
definirii unui obicct, dar cu throw in loc de new (si deosebirea in ceea ce priveste
parantezele).

° La parasirea corpului functici se face 0 copie, cu constructorul de copiere, obiectului
excepfie instantiat la throw (mecanism asemanator intoarcerii de obiect prin valoare),

e Dup& generarea excepliei se parascste fortat corpul funcfiei si corpul blocului try si se
distrug toate obiectele locale ale acestor blocuri — cel de exceptie “local”, apoi a si d.

e La intrarea in blocul catch se instantiaza obiectul e, folosind constructorul de copiere,

pag. 113

Programare Orientata pe Obiecte - Principii

prin copiere din obiectul de exceptie “global”. (***)

* Se instantiaza f si se utilizeaza obiectul e (cu valoarea 1] transmisa in momentul generarii
exceptiei si memorata fn obiectele exceptie).

¢ La parasirea corpului blocului catch se distruge f.

¢ La parasirea corpului blocului catch se distruge e. (***)

¢ La pardsirea constructiei try-catch se distruge obiectul exceptie “global”.

¢ Se continua cu instructiunile urm&toare constructiei try-catch si cu distrugerea instantelor

csib.

Constatém ca in modulul nucleu se instantiaz’ efectiv un obiect exceptie, care apoi se aruncad

(paseaza) spre modulul client, care il prinde si fl poate folosi (accesa).

Daca discriminarea exceptiei s-ar fi facut prin referin{a - adicd am fi avut catch(E& e) -
atunci nu s-ar mai fi copiat obiectul exceptie “global” in e ci s-ar fi folosit direct acesta (referit

prin numele e), iar liniile notate cu (***) nu ar mai fi aparut.

Deoarece constatém ca obiectul exceptie instantiat la throw ajunge in blocul catch dupa copieri
succesive, trebuie avutd in vedere eventuala redefinire a constructorului de copiere pentru
clasa exceptie (pentru a evita problemele care pot sa apara prin copierea membru cu membru

realizata implicit si analizata intr-un capitol anterior).

Dup& cum am vazut, inainte de intrarea in blocul catch se parasesc fortat functia gi blocul try.
De aceea trebuie obligatoriu eliminata orice dependenta a obiectului exceptie de variabile

locale ale acestor blocuri (care nu mai exista in momentul utilizarii obiectului exceptie).

6.6 Tratare polimorfica a exceptiilor

in situatia in care avem nevoie de multe clase de exceptie iar acestea au multe in comun, putem
pune ceea ce este comun intr-o clas de baza si folosi apoi mostenirea. Acest lucru simplificd
declararea claselor de exceptie dar impune atentie suplimentari in ceea ce priveste ordinea

secventelor catch. Daca consideram clasele urmatoare:

’aza
ivata 1

class CB {}; // clasa
class CD1 : public CB {}; // clasa de
class CD2 : public CB {}; // clasa derivata 2

atunci, pentru o discriminare corecta a exceptiilor, trebuie puse inti blocurile catch ale claselor

derivate si abia apoi blocul catch al clasei de baza:

pag. 114

Cap. 6 — Tratarea exceptiilor

2h 2 0 Ae
catch(CD1){} // aici se discrimineaza doar exceptia CD1
catch(CD2){} // aici discrimineaza doar exceptie CD2

catch (CB) {} // aici discrimineaza doar exceptia CB

In cazul in care ordinea este cea a declararii claselor, functionarea ar fi urmitoarea:

ELVES Pek Z}
catch (CB) {}
catch (CD1)

catch (CD?)

// aici se discrimineaza exceptiile CB,CD1,CD2
(} // aict nu se mai discrimineaza nici oO exceptie
{} // aici nu se mai discrimineaza nici o exceptie
probabil diferita de ceea ce ne-am dorit. Cauza acestui comportament este conversia pe care o
face compilatorul dinspre clascle derivate CD1 si CD2 inspre clasa de baza (conversie aici

nedorita).

Aceasta discriminare a diferitelor clase intr-o unica secven{a catch poate fi si foarte folositoare
daca aranjam ca tratarea dorita si se realizeze printr-o metoda virtuala a obiectului exceptie. In
acest fel vom avea o singura secventa catch iar tratarea va fi corespunzatoare tipului efectiv al

excepfiei (la baza solutiei fiind polimorfismul, prin legarea dinamica oferit’ de metodele
virtuale).
class CB { virtual void rezolva();

;
class CDl : public CB { void rezolva(); }
class CD2 : public CB { void rezolva(); }

try{/*.. }
catch(CBé e)
{

e@.rezoiva(); // rezolva corect toate exceptiile (prin
} Lh polimorfism)

Prin aceasta solufie putem rezolva una din cele mai mari probleme ale mecanismului C++ de
tratare a exceptiilor: pe misuri ce numarul secventelor catch creste, se compromite
eleganta modulelor client. Putem avea acum un singur bloc catch indiferent de numarul

exceptiilor discriminate.

6.7 Achizitie si eliberare de resurse

O situatie care poate pune probleme deosebite este aceea a achizitionarii si eliberarii de resurse.
Deoarece solutia este identica oricare ar fi resursa implicati, exemplificam in cele ce urmeazi

pe cazul in care resursa este un figier. O situatie tipica este urmatoarea:

pag. 115

Programare Orientata pe Obiecte - Principii

void FolosesteFisier(char *nume)

{
FILE* fp = fopen(nume, “r”);

// se foloseste fp
fclose(fp); // inchidem fisierul

}

Daca insa, pe parcursul utilizarii fisierului, se genereazd o exceptie se paraseste functia (dupa
cum am vazut anterior) si nu se mai face inchiderea fisierului. Pentru a evita aceasta problema
(se “uit” eliberarea resursei, ceea ce poate duce la epuizarea resursei) se impune utilizarea

unui bloc try-catch astfel:

void FolosesteFisier(char *nume)

{
FILE* fp = fopen(nume, “r”);

try
{
// se foloseste fp
}
catch(...)
{

fclose(fp); // inchidere in situatia de excentie

throw; // pasam exceptia “mai sus”
}
fclose(fp); // inchidere in situatia normals

}

Solutia este corecté dar neeleganta, mai ales in situatia in care folosim mai multe resurse in

corpul functiei, Exist ins o solutie mult mai eleganta, aplicabila in situatii de genul:

void achizitie()

{
// achizitie resursa 1

/f

// achizitie resursa n
// utilizare resurse

// eliberare resursa n

//

// eliberare resursa 1
}

in care este esential faptul cé resursele se elibereazi in ordine inversi alocdrii. Situatia

aminteste foarte mult de apelul constructorilor si destructorilor pentru obiectele locale ale

pag. 116

Cap. 6 ~ Tratarea exceptiilor

functiei. De aceea putem aborda aceasti problema prin folosirea de obiecte ale unei clase care

are constructori si destructori.
Pentru exemplul anterior cu fisiere, definim clasa FilePtr care se comporta ca si FILE*:

class FilePtr
{
FILE* fp;
public:
FilePtr(char* n, char *a) {£p=fopen(n,a);}
FilePtr(FILE* pO) {fp=p0;}
~FilePtr() {fclose (fp) 7}
operator FILE*() {return fp; }
Mi

in acest moment utilizarea devine ct se poate de simpla:

void FolosesteFisier(char *nume)

{
FilePtr fis(nume, “r”);

// se foloseste fis

}

in acest caz inchiderea fisierului (eliberarea resursei) se face automat, pe destructorul obiectului,
la parsirea funcfiei (normala sau prin aparitia unei exceptii) fara a mai trebui si “ne batem

capul” cu aceasta problema.

Remarcim redefinirea operatorului “cast” (conversie fortata de tip) pentru a ne permite
utilizarea eleganta a obiectului ca si cum ar fi FILE*. in acest fel este valida o expresie de genul
int c=fgetc(fis);

deoarece compilatorul face conversia de la FilePtr (care este furnizat Ja apel) la FILE* (cum
este asteptat de functia fgetc) folosind operatorul cast redefinit (care intoarce valoarea fp din

obiect).

Tehnica prezentata poarti numele de “resource acquisition is initialization”. Ea este o tehnicd
generala, care se bazeaza, dupa cum am vazut, pe proprietitile constructorilor si destructorilor si
pe interactiunea acestora cu mecanismul de tratare a exceptiilor. Pe scurt, se echivaleaza
alocarea de resurse cu initializarea de obiecte bazandu-ne apoi pe faptul c& se vor distruge

automat toate obiectele care au fost deja initializate (construite).

pag. 117

Programare Orientata pe Obiecte - Principii

Un obiect nu se considera complet construit atat timp cat constructorul sau nu s-a executat in
intregime. Acest fapt trebuie accentuat deoarece apelul destructorului se face doar pentru
obiectele construite complet. (Afirmatiile anterioare sunt valabile si pentru obiecte “tncuibirite”

sau “incuibaritoare”).

Daca este necesar ca intr-un constructor sa se aloce doua (sau mai multe) resurse, este posibil sa
apara o exceptie la alocarea celei de a doua si obiectul sa se afle intr-o stare “construit pe
jumitate” periculoasd deoarece, obiectul nefiind construit complet, nu i se va apela
destructorul. Situatia se rezolva aplicénd tehnica “resource acquisition is initialization”

membrilor, ca fn exemplul urmator:

class X
{
FilePtr £1; // vesursa 1, de exemplu fisier
FilePtr £2; // vesursa 2, de exemplu fisier
X(char *nl, char *n2):
£1(n1), // achizitie resursa 1
£2(n2) // achizitie resursa 2
{} // corp propriu-zis
bhow «a

Mi

fn aceasta situatie, chiar dacd se genereazi exceptie la alocarea celei de a doua resurse, se va

apela destructorul primei instante incuibarite, deoarece aceasta fusese construita complet.

O alta situatie la fel de periculoasa este aceea in care in corpul constructorului, dupa alocarea
unei singure resurse, mai trebuie facute si alte operatii (fara leg&tura cu resursele). Daca aceste
operatii duc la generarea unei excepfii, obiectul nu se considera construit si nu se apeleazad
destructorul, deci se pierde resursa. Solutia este tot aceea de a aplica tehnica “resource

acquisition is initialization” si de a face alocarea resursei intr-un obiect incuibarit.

Se recomanda aplicarea acestei tehnici pentru fiecare resursa (deci, alocarea / eliberarea
fiecdrei resurse este trecuta in corpul constructorului / destructorului unui obiect) astfel ca
in restul codului si nu mai avem alocdri / eliberari de resurse ci doar cod fara legatura cu
alocarea / eliberarea acestora. in acest fel orice exceptie care apare determina eliberarea
corespunzatoare a tuturor resurselor pentru care s-a reusit deja alocarea.

6.8 Optiuni la tratarea unei exceptii

in cele ce urmeaza prezentam situafiile care pot fi intdlnite la tratarea unei exceptii:

pag. 118

Cap. 6 — Tratarea exceptiilor

int f(int arg)
{
try[(
g(arg);
}
catch (x1) {
// repara ceva si reia (daca stim sigur ca va fi OK)
g(arg);
}
catch (x2) {
// calculeaza si intoarce un rezultat

return 2;
}
catch (x3) {
// arunca mai sus exceptia
throw;
}
catch (x4) {
// converteste x4 in alta exceptie si arunc-o mai sus
throw xxii;
J
catch (x5) {
// vezolva definitiv si treci la linia urmatoare

}
catch(. . .){
// venunta
terminate (); // terminare anormala a programului
} // (“Abnormal program termination”)

tf

Atragem atentia doar asupra urmatoarelor situatii:
¢ throw simplu re-aruncd aceeasi exceptie (acelasi obiect exceptie) “mai sus”, exceptia
urmand a fi tratata de blocul try ierarhic superior (al apelantului).
« din punct de vedere al limbajului o exceptie se considera tratat4 in momentul intrarii in
blocul catch corespunzator, deci orice exceptie care apare in blocurile catch este tratat’

de blocul try ierarhic superior (al apelantului),

6.9 Observatii privind mecanismul de tratare a exceptiilor

Dupa cum am mai amintit, acest mecanism nu este altceva decat un mecanism de salt (de
intoarcere). Desi a fost proiectat in principal pentru tratarea erorilor, el poate fi folosit si in
situatii cand nu avem efectiv erori. in fapt, in toate exemplele prezentate anterior nu am avut
erori ci am generat exceptii care nu aveau semnificatie de erori. in bibliotecile reale ins&
generarea excepfiilor nu ne mai revine noua, iar semnificatiile excepfiilor sunt de cele mai multe

ori aparitii de erori.

pag. 119

Programare Orientata pe Obiecte - Principii

Spre deosebire de exceptiile hardware la care se asigura implicit restartarea instructiunii care a
generat exceptia, mecanismul C++ de tratare a exceptiilor prezentat in acest capitol permite
doar discriminarea erorii si tratarea ei in blocul catch corespunzator, nu si restartarea
instructiunii. Daca dorim restartarea instructiunii care a provocat excepfia trebuie si facem

acest lucru explicit.

Desi mecanismu! de tratare a excepftiilor nu este strict obiectual (tratarea nu se face la nivel de
clas&) si nu are eleganta celorlalte constructii ale limbajului C++, el este totusi in stransa
leg&tura cu caracterul obiectual al limbajului, mecanismul de instantiere gsi transfer al obiectelor

exceptie fiind tipic obiectual.

Rezumat - de tinut minte :-)

° Modul nucleu (nu stie trata), modul client (nu poate detecta)

° Solutia C++: try, throw, catch

Discriminarea excepftiilor dupa tip

Implementarea mecanismului, obiect exceptie

Tratare polimorfica a exceptiilor

“Resource acquisition is initialization”, achizitie in constructor
Optiuni la tratarea unei exceptii

° Se asigur& doar discriminarea excepfiei nu si restartarea ei

ee ee

pag. 120

Anexa

/*
7*
7*
/*
/*
/*
/*
/*
7*
y*
7*
/*
/*
7*
/*
/*
7*
/*
/*
7*
/*
7*
7*
7*
7*
/*
/*
/*
/*
/*
/*
/*
/*
/*
y*
/*
7*
7*
7*
/*
1*
ik
i
7*
7*
/*
7*
7*
/*
7*
/*
7*
/*
/*
/*
7*
/*
/*
/*
]*
/*
7
7*
/*
/*

©BIHVEUNH

SPSSPS SSI EE eee eee e nee nuuuuouonna vn
Perea eee ene SSR EEELLLLLLECOLELEIT TET Tint hry eens

af
*/
ay
A
*/
af
“7
ef,
ay
at
af
ay
+f
*/
*/
*/
mf
*/
wh
af
af
af
aS
Bed
*/
*/
*/
*/
af
+f
af
ay
ay
*f-
*/
as
LA
af
*/
Bed
+/
LA
/
+
af
af
*/
*/
*/
bi4
af
WA
af
*/
nh
af
ay
ay
+f
*/
af
*/
af
*/
+f

Anexa

Se considera programul urmitor:
#include <stdio.h>
#include <iostream.h>
class CA

1
public: int x;

ca() {xel;  print£("\ncar 4d",x) ;};
CA(int x0) {x=x0; print£("\nCAE &d")x) ;};
CA(CA gp) {x=p.x;print£("\ncAc %d",x) ;};
void operator=(CA &p) {x=p.x;print£("\nAOP= &d",x) ;} ;
~CA() { Print£("\nDA %4",x) ;};

void meti (int r) (print£("\nMETIA 4d %d",x,r) smet2 (x) ;met3 (x) ;}
void met2 (int r) {print£("\nMET2A td 4d" .x,r) ;}

virtual void met3(int r) {printé("\nMET3A 4a &a" ,x, 2) 7}

te

CA Incrementeaza (CA x, CA &y, CA z, CA &t)

{

CA r; print£("\n++++") ;
Xoxet? Youths gachhy tame; mie © xo tye to +} toa;
return xr;

}

class CB:public CA
{
public: int y;

CB() {y=2; print£("\ncBr $d %d",x,y) ;};

~CB(){ printf£("\nDB %d 4d",x,y) ;);
CB(int x0,int y0=3);

int operator+(int p) {printé ("\nBOP+I") ;return (x+p+1) ;} ;
int operator+(Ca 6p) {print£("\nBOP+a") ;return (x+p.x) ;} 7

friend int operator+(CB 601,CB 602) {print ("\nBOP4B" return (ol. x+02.x) ;};

void operator=(CB &p) (y=p.x;printé("\nBOP= %d 8A" x,y) 7);
void met2(int r) {print£("\nMET2B 4d %d",2,r) ;)

virtual void met3(int x) {print£("\nMETSB %d 4a" xr) 5}

ye

struct CC:public CA
{

cA a;
CC(int x0) {x=x0;print£("\nccR ¥d %d",x,a.x) ;);
~ec() { printf ("\nDC %d %d",x,a.x) ;};

ee

CB::CB(int x0,int yO) :CA(x0+4)
{ Y=y0+5 ;print£("\nCBE 4d %d",x,y) ;};

CA a, b(10); CB c, *i, d(11,12);

void main()
{
CA e(13), *£, g=d; CB h(14); cc #x;

print£("\nsTaRT") ;
f=new CA(20) ;i=new CB(21,22) ;

b.meti (31); d.meti (32);

printf ("\nOP %d td td &d",d+tb,htd,h.xt2,d41) ;
print£("\nX¥2T td 4d td 4d",a.x,b.x,e.x,g-x) ;
g=Incrementeaza (a,b,e,g) ;

print£("\nxy2t td td 4d %d",a.x,b.x,@.%,g.x) ;
delete £; delete i;

dehjprint£("\nd=h 4d td %d 4d",d.x,d.y,h.x,h.y) ;
(cout<<endl<<e, x) <<2<<0. y<<"<<"K< (0.x<<2) <<. y}
x=new CC(22); delete x;

print£("\nsTop") ;

pag. 121

Programare Orientata pe Obiecte - Principii

Prezent&m in continuare rezultatul rularii sale.
Se va urmiri (de cate ori este nevoie ©) pana se intelege . . .

CAI 1 // pentru obiectul global a
CAE 10 // pentru obiectul global b

cAI 1 // pentru obiectul global c

CBI 12 // pentru obiectul global c

CAE 15 // pentru obiectul global d

CBE 15 17 // pentru obiectul global d

CAE 13 // pentru obiectul local e

cAC 15 // pentru obiectul local g |
CAE 18 // pentru obiectul local h

CBE 18 8 // pentru obiectul local h

START // abia aici incepe codul functiei main

CAE 20 // pentru obiectul alocat dinamic £

CAE 25 // pentru obiectul alocat dinamic i

CBE 25 27 // pentru obiectul alocat dinamic i |
METIA 10 31 // b.x, x

MET2A 10 31 // bx, ©

MET3A 10 31 // b.x, ©

METIA 15 32 // ax, ©

MET2A 15 32 // d.x, x met3 nu este virtuala, deci este legata static
MET3B 15 32 // d.x, cr met3 este virtuala, deci este legata dinamic
BOP+I // pentru expresia d+1 (CBtint) d.x +1441 => 17

BOP+B // pentru expresia htd — (CB+CB) hx td.x => 33

BOP+A // pentru expresia d+b (CB+CA) d.x+b.x => 25

OP 25 33 2017 // ordinea evaluarii este data de conventia de apel C
xY¥ZT1101315 //

cac 13 // copiere obiect e in obiect (parametru) z

cac 1 // copiere obiect a in obiect (parametru) x

cAI 1 // pentru obiectul local r rexel

ttt // corp functie, x.x=2, y.x(b.x)=11, z.x=14, t.x(g.x)=16, 4r.x=43
cAC 43 // copiere obiect r in obiect temp temp .x (=r. x) =43
DA 43 // pentru obiectul r (local)

DA 14 // pentru obiectul z (parametru)

DA 2 // pentru obiectul x (parametru)

AOP= 43 // copiere obiect temp in obiect g g.x(=temp.x)=43
DA 43 // pentru obiectul temp

XYZT 11113 43 // atentie cine s-a modificat si cine nu

DA 20 // pentru obiectul £

DB 25 27 // pentru obiectul i

DA 25 // pentru obiectul i

BOP= 15 18 7/ pentru d=h d.y(=h.x)=18
deh 1518188 //

122<<42 // atentie la tipurile implicate si la ordinea operatorilor
CAI 1 // pentru obiectul x x->xe1

CAI 1 // pentru obiectul incuibarit x->a x->a,x=1

CCE 221 // pentru obiectul x x->x=22

pe 22 1 // pentru obiectul x

DA 1 // pentru obiectul incuibarit x->a

DA 22 // pentru obiectul x

STOP // aici se termina codul functiei main, tiparirea insa nu

DB 18 8 // pentru obiectul local h

DA 18 // pentru obiectul local h

DA 43 // pentru obiectul local g

DA 13 // pentru obiectul local e

DB 15 18 // pentru obiectul global d

DA 15 // pentru obiectul global d

DB 1 2 // pentru obiectul global c

DA 1 // pentru obiectul global c

DA 11 // pentru obiectul global b

DA 1 // pentru obiectul global a

pag. 122

Bibliografie

Bibliografie

. Brian W. Kernighan, Dennis M. Ritchie - "The C programming language",
Second Edition, ISBN 0-13-110362-8, Prentice Hall, 1988

. Ionut Muslea - "Jnifiere in C++. Programare orientatdé pe obiecte", Editura
Microinformatica, ISBN 973-95718-2-4, Cluj-Napoca, 1992

. Ioan Jurca - "Programarea orientaté pe obiecte in limbajul C++", Editura

Eurobit, Timisoara, 1992

. Bjarne Stroustrup - "The C++ programming language", Second Edition, ISBN 0-
201-53992-6, Addison-Wesley, 1993

. Dan Roman - "Jngineria programdrii obiectuale", Editura Albastra, ISBN 973-
9215-30-0, Cluj-Napoca, 1996

. *** - Documentatie compilatoare C++

4 pag. 123


